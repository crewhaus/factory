# `crewhaus-factory` — Module Catalog Plan

## Context

`crewhaus-factory` is a **modular meta-harness tool** — a compiler + control plane that compiles a single high-level harness spec into multiple runtime targets (graph, event-driven workflow, pipeline DAG, managed runtime, CLI bundle, channel bot, eval task, batch worker, voice service, browser-driver, research-runner). The repo is currently in design phase: 38KB ecosystem analysis, two architecture deep-dives (Claude Code + OpenClaw), and 17 reference harnesses cloned locally.

This plan delivers the **complete module catalog** that `crewhaus-factory` must implement and compose. Each module entry pins down **what to study** (reference impls in `reference-repos/`) and **what to test** (which test layers apply, with concrete scenarios). The catalog is organized into factory-level modules (the meta-harness itself) and composable runtime modules (the building blocks of generated harnesses).

The goal of this plan is to lock in the catalog before any implementation, so individual modules can then be specced, researched, and built independently.

---

## Implementation status

> Status as of 2026-05-08. 116 of ~190 catalog modules implemented across 111 workspace packages (+ `apps/cli`); full suite is 1601 tests across 113 test files plus 1 skipped contract test gated on env var, all green; `tsc -b` clean; `biome check` clean. **All 11 target shapes ship today** (cli/workflow/channel/graph/managed/pipeline/crew/research/batch/voice/browser). Sections 1–26 of `docs/build-roadmap.md` are landed: tool framework, persistence, hooks/skills/slash, sub-agents + Task tool, expanded tool catalog (web/image/fetch), observability (trace bus + OTel + metrics + pretty/JSON printer), eval stack with `crewhaus eval` subcommand, multi-provider model layer (Anthropic/OpenAI/Gemini/Bedrock + local), production safety floor (sandbox + tool-code-execution + prompt-injection-detector), GRPH stateful graph runtime with checkpointing/HITL/branch-history/durable-execution, MGD multi-tenant managed runtime with JWT-authed gateway + policy-engine + tenancy + hash-chained audit-log, RAG pipeline DAG with retrieval primitives, CRW multi-agent crew with handoff + A2A messaging, RES autonomous research agent with planner/crawler/citation-tracker/report-writer, BATCH queue worker with idempotency-keys + visibility-timeout, VOICE realtime audio with VAD + barge-in, BROW computer-use over chromium, and Studio authoring/inspection UI with trace-viewer + graph-visualizer + wizard + plugin SDK. Sections 27–31 (production hardening, deployment + canary, evaluation depth + EVAL target, backend adapter completions, Studio v1) are scoped but not started.

Modules in the per-layer tables below are prefixed with status markers:

- ✅ — implemented and tested (has `src/` + tests + at least one consumer)
- 🚧 — in progress (open PR)
- *(unmarked)* — not started

### Implemented (✅)

| Module | Layer | Notes |
|---|---|---|
| `spec-schema` | F1 | Discriminated union over `target`: `cli` (agent + optional tools[]) and `workflow` (top-level model + steps[] with optional per-step model/tools). PR #2, expanded in PR #7 and PR #11. |
| `spec-parser` | F1 | YAML → Zod-validated spec. PR #2. |
| `spec-validator` | F1 | Bundled with parser via Zod (`.strict()`). PR #2. |
| `ir-model` | F1 | `IrV0` (cli) carries name, agent, tools, mcp_servers, permissions; `IrWorkflowV0` carries name and steps[] with each step's `model` resolved at lower-time; `IrChannelV0` adds channels + routing + secret-ref normalisation. Exported as `IrNode = IrV0 \| IrWorkflowV0 \| IrChannelV0` discriminated union. T1 coverage on every shape + the discriminated-union narrowing + MCP/permission/secret variants. PR #2, expanded in PR #7, #11, #14. |
| `compiler-core` | F2 | Pipeline: parse → lower → emit; `lower()`/`emit()` dispatch on target (`cli` → target-cli, `workflow` → target-workflow). PR #2, expanded in PR #7 and PR #11. |
| `target-cli-bundle` | F2 | Single-file `agent.ts` codegen for CLI target; emits grouped tool imports + `defaultCatalog.register()` when `ir.tools` is non-empty, an `McpHost` boot block + try/finally cleanup when `ir.mcp_servers` is non-empty, layered permission rules when `ir.permissions.rules` is non-empty, and the unconditional Section-11 extension surface (hooks/skills/slash commands) on every emit. T1 covers shebang, escaping, every wiring branch (tools / MCP / permissions / extensions), unknown-tool error path, and the combined-everything ordering invariant. PR #2, expanded in PR #7, #14. |
| `target-workflow` | F2 | Single-file `agent.ts` codegen for workflow target; emits sequential `runChatLoop({ singleTurn: true })` calls with per-step instructions, model, tools, and the prior step's terminal text threaded forward as a synthetic user message. PR #11. |
| `codegen-templates` | F2 | Inline string templates in target-cli and target-workflow. PR #2, expanded in PR #11. |
| `spec-cli` | F4 | `compile`, `init`, `run`, `doctor` subcommands; deploy/eval/watch pending. PR #2, expanded in PR #10. |
| `runtime-orchestrator` | R1 | Streaming chat REPL with state-machine-driven inner loop, `tool_use` execution, and pre-turn compaction (snip → autocompact). Single-shot `singleTurn` + `seedMessages` mode added for workflow target. PR #2, #7, #9, #11. |
| `adapter-anthropic` | R2 | Refactor of the original `model-adapter` into a discrete package. Owns the `ProviderAdapter` interface, `ProviderRequest` / `StreamEvent` shapes (Anthropic-isomorphic so the JSONL transcript stays wire-compatible), `resolveAuth` / `createAnthropicClient` (OAuth + API key), and `consumeStream` / `collectFinalMessage` helpers. `features = { caching: "explicit", tool_use: true, vision: true, thinking: true, web_search: true }`. Section 17. |
| `adapter-openai` | R2 | OpenAI Chat Completions implementation. Translates canonical request → OpenAI messages/tools, SSE stream → canonical `StreamEvent`s. `features = { caching: "automatic", tool_use, vision, web_search: false }`. Honors `OPENAI_API_KEY` + `OPENAI_BASE_URL` (the `local/<model>@<url>` router path reuses the OpenAI client against any OpenAI-compatible local endpoint — Ollama, vLLM, llama.cpp server). Section 17. |
| `adapter-gemini` | R2 | Google Gemini implementation via `@google/genai`. Maps `tool_use` ↔ `functionCall`, `tool_result` ↔ `functionResponse`. `features = { caching: "explicit", tool_use, vision, thinking: true, web_search: false }`. `GEMINI_API_KEY` / `GOOGLE_API_KEY`. Section 17. |
| `adapter-bedrock` | R2 | AWS Bedrock implementation via `@aws-sdk/client-bedrock-runtime`. Per-family marshalling under `src/families/`: `anthropic.*` reuses the `adapter-anthropic` raw-event translator; `meta.llama*` and `mistral.*` render their native chat templates and decode their respective stream chunk shapes. `features` is family-specific (Anthropic-on-Bedrock keeps `caching="explicit"` + `tool_use`; Llama/Mistral are text-only). Standard AWS credential chain. Section 17. |
| `model-router` | R2 | Parses `agent.model` into `{ providerId, modelId, baseUrl?, family? }` and lazy-loads the matching adapter via `await import(...)`. Anthropic-only specs never trigger imports of `@aws-sdk/*`, `@google/genai`, or `openai` on disk. Caches one adapter instance per `(providerId, baseUrl, family)` key. Strict prefix grammar (`claude-*` / `openai/*` / `gemini/*` / `bedrock/*` / `local/<model>@<url>`); malformed input throws `ConfigError` with a human-readable hint. Section 17. |
| `error-types` | F-foundations | Typed `CrewhausError` hierarchy + `toJSON`. PR #3. Extended with `"tool"` code in PR #6. |
| `logging` | F-foundations | `pretty/json` formats, level filtering, `child()` bindings. PR #3. |
| `infra-utils` | F-foundations | `escapeJsonString`, `parseArgs`, `assertNever`. |
| `tool-catalog` | R3 | `ToolDefinition`/`RegisteredTool` interfaces, `ToolCatalog` registry, `defaultCatalog` singleton. PR #6. |
| `tool-builder` | R3 | `buildTool()` factory with fail-closed safety defaults. PR #6. |
| `tool-validate` | R3 | `validateToolInput()` returning typed `ValidationResult`; `ToolValidationError` with Zod issues. PR #6. |
| `tool-permission-matcher` | R3 | `compilePattern()` + `matchesPattern()`; supports `Bash(git *)`, `Read`, `Write(**/src/**)` glob syntax. PR #6. |
| `tool-executor` | R3 | `executeTool()`: validate → permission-check → invoke → normalized `ToolResult`. PR #6. |
| `tool-fs` | R4 | `Read`/`Write`/`Edit`/`Glob`/`Grep` sandboxed to `process.cwd()`; atomic writes via temp + rename; `ToolPermissionError` for traversal. PR #8. |
| `tool-bash` | R4 | `Bash` via `Bun.spawn` with default 30s timeout (max 10min); captures stdout + stderr; returns formatted exit/timeout report. PR #8. |
| `tool-todo` | R4 | `TodoWrite` over a per-process module-level list; renders a markdown checklist with status/priority. PR #8. |
| `turn-state-machine` | R1 | Pure state machine: `NeedModel`/`NeedTools`/`NeedCompaction`/`NeedRecovery`/`Done`; exhaustive transition tests over the (state, event) cartesian product. PR #9. |
| `run-context` | R1 | `RunContext` type (`runId`, `sessionId`, `turnNumber`, `abortSignal`, `logger`) + `createRunContext()` factory; logger child-bound to run/session ids. PR #9. |
| `token-budget` | R2 | `estimateTokens()` (char/4 heuristic over text, tool_use, tool_result blocks) + `TokenBudget` class with `add()` / `isApproachingLimit()` (default threshold 0.85). PR #9. |
| `compaction-snip` | R6 | Pure middle-message removal with `[Context compacted: N messages removed]` marker; tool-use/result orphan defense walks boundaries until pairs are intact. PR #9. |
| `compaction-autocompact` | R6 | Model-summarize-then-replace; returns `[user-marker, assistant-summary]` pair so the next user input appends naturally. PR #9. |
| `recovery-engine` | R1 | Pure decision function `recover(error, state) → RecoveryAction` over the Anthropic taxonomy: `prompt_too_long → compact`, `max_output_tokens → continue`, `overloaded/5xx → retry` (exp backoff 1–30 s), `invalid_request → tombstone`, with per-turn budgets. T4 fixture replay over 10 representative SDK error shapes. PR #12. |
| `permission-engine` | R8 | Modes `default`/`plan`/`auto`/`bypass` over a 5-source `RuleSet` (flag → settings → yaml → hooks → builtin). `evaluate(call, mode, rules) → "allow" \| "deny" \| "ask"`. `parsePermissionsConfig` rejects `mode: bypass` from any non-flag source (T8 security test). PR #12. |
| `abort-controller` | R1 | Parent/child abort tree with WeakRef cascade so abandoned children don't pin parents; sibling-independent. T3 spawns `sleep 30` and verifies SIGTERM cascade. Wired into runtime-core for SIGINT (first cancels turn, second exits). PR #12. |
| `tool-orchestrator` | R3 | `partitionToolCalls(calls, lookup) → { concurrent: ToolUse[][], serial: ToolUse[] }` splits calls into concurrent (`concurrencySafe && readOnly && !destructive`) batches and a serial list. T9 property test fuzzes 100 random tool/flag mixes. PR #13. |
| `tool-loop-detection` | R3 | `detectLoop(history, windowSize=10, threshold=3)` over a sliding window of canonical-JSON `(toolName, input)` signatures; runtime injects a one-shot warning user message per signature. PR #13. |
| `tool-result-store` | R3 | `storeAndPreview(result, opts)` persists outputs >10 KB to `.crewhaus/tool-results/<runId>/<toolUseId>.txt` (write-exclusive flag for idempotent retry); preview = first 100 lines + `[truncated, full output at <fullPath>]`. Path-traversal guards on `runId`/`toolUseId`. PR #13. |
| `streaming-tool-executor` | R3 | `executeStreaming(stream, opts)` dispatches `tool_use` blocks during the SDK stream via the `contentBlock` event with a concurrent-safe `canExecute()` gate; sibling-abort on destructive failure. Accepts a `runTool` callback so the runtime can plumb permission + abort + result-store through. T7 load test with 50 partial blocks. PR #13. |
| `mcp-host` | R5 | `McpClient` / `McpHost` shell over `@modelcontextprotocol/sdk` (stdio + SSE transports); exp-backoff reconnect (1 s → 30 s cap, ±10% jitter, no max attempts) with proactive `onclose` detection and a queue-capped (16) `connectedDeferred` for in-flight calls. `addServer()` is synchronous; `connect()` runs inside `registerMcpServer()`. Reduces `tools/call` content blocks (text/image/audio/resource) to a single string. Section 9. |
| `tool-mcp` | R4 | `registerMcpServer(host, serverName, catalog, opts?)` lists remote tools and registers each via `@crewhaus/tool-builder` under `<serverName>__<toolName>`. `inputSchema: z.unknown()` paired with `jsonSchema` carrying the server's JSON Schema verbatim (runtime-core forwards it to the model). Per-tool flag overrides via `opts.perTool`. Remote tool names validated against `[a-zA-Z0-9_-]+`. Section 9. |
| `state-store` | R7 | Tiny zustand-style `createStore<T>(initial)` returning `{ get, set, subscribe, select }`. `set` shallow-merges (functional or partial form); root listeners fire on every actual change, `select(selector)` returns a derived view whose listeners fire only when `Object.is(selector(next), selector(prev)) === false`. Listener exceptions isolated via `console.error`. Per-`runChatLoop` instance; consumed by hooks/skills/tools in Section 11+. T9 property test fuzzes 100 random `set` sequences. Section 10. |
| `session-store` | R7 | File-backed JSON metadata at `.crewhaus/sessions/<id>.json`. Session shape `{ id, createdAt, updatedAt, name, target, model, lastTurnIndex }` with id format `sess_<16 hex>`. Atomic writes via `<id>.json.tmp` + `rename`. `list()` evicts (`unlink`s `.json` AND sibling `.jsonl`) any file whose **mtime** is older than `ttlDays * 86_400_000` ms (default 30 days) — mtime-keyed so `touch -t YYYYMMDD0000 <id>.json` forces expiry from the shell. Path-traversal guard rejects malformed ids before any I/O. T3 integration test backdates mtime + asserts eviction over a `mkdtempSync` root. Section 10. |
| `event-log` | R7 | Append-only JSONL transcript at `.crewhaus/sessions/<sessionId>.jsonl`. `openEventLog(sessionId, opts)` returns `{ append, read, close }`. Each event line: `{ ts, version: 1, kind, payload }` where `kind` ∈ `user_message | assistant_message | tool_use | tool_result | error | compaction`. Append uses `appendFileSync(..., { mode: 0o600 })` (owner-only, mirroring `claude-code/utils/sessionStorage.ts`). Read streams via `fs.createReadStream` + `node:readline`, supports `since`/`until` filtering. Replay walks `user_message`+`assistant_message` events back into a `MessageParam[]`; the rest are audit-only. T7 load test: 10 K appends round-trip in <1 s. Section 10. |
| `hooks-engine` | R9 | Lifecycle hooks fired at every meaningful runtime moment. Events: `session-start | stop | pre-tool | post-tool | pre-model | post-model | pre-compact | post-compact | pre-slash`. `loadHooks` reads user-then-project layered `.crewhaus/settings.json`; `runHooks` spawns each via `Bun.spawn(["sh", "-c", cmd])` with a **restricted env** (PATH=`/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin`, HOME, USER, LANG, TERM, TMPDIR, LC_*; strips `ANTHROPIC_AUTH_TOKEN`/`AWS_*`/`GH_TOKEN`/`OPENAI_API_KEY`). Each hook gets a 5 s default timeout (SIGKILL on miss, drain-grace pattern from `tool-bash` for orphan grandchildren). Decision: `{ decision: "allow"\|"deny"\|"block", reason?, mutate? }`. `aggregateDecisions` short-circuits on first deny/block; mutate honoured only for `pre-slash` (the `expanded` field). T8 verifies the env stripping at both unit and spawned-subprocess layers. Section 11. |
| `skills-registry` | R9 | Lazy-loaded SKILL.md catalog with a synthetic `Skill(name)` tool. `discoverSkills` walks `~/.crewhaus/skills/*/SKILL.md` then project then plugin dirs (project overrides user by `name`); reads frontmatter only (yaml lib). `loadSkillBody(ref)` reads the full body on demand. `formatSkillsForPrompt` renders the `Available skills:` system block. `createSkillTool` builds a `RegisteredTool` named `Skill` whose `execute` calls `loadSkillBody`. Lazy-load contract verified by T3 spy: discovery touches frontmatter only; the body lands only when the model calls `Skill({ name })`. Section 11. |
| `slash-commands` | R9 | Markdown-templated user-input shortcuts. `loadCommands` reads `<cwd>/.crewhaus/commands/*.md` (basename = command name; optional `description`/`argument-hint` frontmatter). `expand(input, commands)` matches `^\/(\S+)\s*([\s\S]*)$`, looks up the command, substitutes via `body.split("$ARGUMENTS").join(args)` — non-recursive, regex-special-safe, multi-line-safe. `pre-slash` hook fires from `runtime-core` before substitution; `mutate.expanded` overrides. T9 property test runs 200 random body+args pairs against the substitution invariant. Section 11. |
| `target-channel-bot` | F2 | Multi-file codegen for the `channel` target — first target shape that emits >1 file (`daemon.ts`, `gateway.ts`, `session-router.ts`, `agent.ts`). The daemon boots `Bun.serve`, instantiates configured channel adapters, registers built-in tools (including the new permission-gated `sendMessage`), and wires SIGINT/SIGTERM for graceful shutdown. The gateway is channel-generic — verifies signatures, routes URL-verification challenges, dedups by adapter-supplied idempotency keys (Slack `event_id`), and dispatches into the session router. The session-router derives a deterministic `sess_<16hex>` id from `sha256(routingKey)` (per `routing.sessionKey`: `thread`/`user`/`channel`), creates the session on first inbound, and resumes via `session-store` + `event-log` thereafter. The agent wrapper calls `runChatLoop({ singleTurn: true, resume })` per inbound message — Section 12 lifted that mutex in runtime-core for exactly this use case. Secrets in the spec encoded as `$VAR_NAME` lower into `process.env.VAR_NAME` reads with a startup-time fail-loud check (no silent empty signing secrets). Section 12. |
| `channel-adapter-base` | R13 | Concrete `ChannelAdapter` interface lives in `@crewhaus/channel-adapter-slack` and is shared by future adapters: `{ id, verify(req), parseInbound(req), sendReply({event, text}), setTyping({event}) }`. `parseInbound` returns a tagged union (`event` / `challenge` / `skip`) so the gateway handles URL-verification challenges and known-uninteresting payloads (bot self-mentions, types outside `app_mention` / `message`) without channel-specific knowledge. `idempotencyKey` is supplied by the adapter so the gateway dedups generically. Section 12. |
| `channel-slack` | R13 | Slack adapter (no `@slack/*` SDK dep — plain HTTP + Node `crypto`). HMAC-SHA256 signature verification of `v0:${ts}:${body}` against `X-Slack-Signature` with constant-time `timingSafeEqual` and a ±5 minute timestamp tolerance. Parses `event_callback` envelopes for `app_mention` and `message` events, surfaces `event_id` as the gateway's dedup key, and skips bot self-mentions. `sendReply` POSTs `chat.postMessage` with a Bearer token; `apiBaseUrl` is overridable via constructor / `SLACK_API_BASE_URL` env so smoke tests intercept outbound traffic. T8 covers tampered body, tampered signature, wrong secret, expired timestamp, future timestamp, missing headers, malformed timestamp, length mismatch (timing-safe-equal guard). Section 12. |
| `tool-message-channel` | R4 | `SendMessage(channel, text)` — opt-in built-in tool registered when an `agent.tools: [sendMessage]` entry appears in the spec. Permission-gated: declares `destructive: true` so the engine evaluates fail-closed (`ask` in default mode, `deny` in plan mode). Routing key format `<adapterId>:<workspaceId>:<channelId>[:<threadTs>]`; the tool dispatches to the adapter `daemon.ts` registered at boot via `registerChannelAdapter`. T8 verifies fail-closed without an explicit `alwaysAllow SendMessage` rule and the source-priority override semantics (alwaysDeny@flag wins over alwaysAllow@yaml). Section 12. |
| `agent-context-isolation` | R10 | `createIsolatedContext(parent, opts)` plus the shared `RuntimeBridge` / `SubAgentDefinition` / `ParentRunHandle` / `SpawnSubAgentFn` / `SubAgentResult` types. Mints a fresh `RunContext` (new `runId` + `sess_<16hex>`), opens the child's own `EventLog` under the same `sessionRootDir`, builds an isolated `state-store`, and roots a child `AbortTree` on the parent's signal so SIGINT cascades down without leaking the reverse direction. The runtime stuffs a typed `RuntimeBridge` into `ToolExecuteContext.bridge` once per run; the Task tool casts it. Section 13. |
| `sub-agent-spawner` | R10 | `spawnSubAgent(parent, opts)`. Sequence: `createIsolatedContext` → append `sub_agent_start` to parent log → child `runChatLoop({ singleTurn: true, runContext: child.runContext, … })` → re-read child event log to assemble `transcript` + `toolCalls` → append `sub_agent_end` → return `SubAgentResult`. `runChatLoop` opens its own atomic-append handle to the same JSONL so the spawner's pre-opened handle co-exists. Test injection point `_client` lets unit tests substitute a scripted Anthropic client without touching env auth. Section 13. |
| `sub-agent-permission-inheritance` | R3 | `resolveChildPermissions(parent, def)`. Modes: `inherit` copies parent rules verbatim; `scoped` keeps only parent rules whose toolGlob matches at least one name in `def.tools`; `{allow, deny}` rebuilds yaml-source rules and preserves `BUILTIN_DEFAULT_RULES` as the safety floor. Bypass non-propagation: parent `bypass` + `def.inherit_bypass !== true` → child mode `default`. T9 property test over 100 random `(mode × permissions)` combinations; T8 verifies the bypass guard. Section 13. |
| `tool-task` | R4 | `Task(description, prompt, subagent_type?)` — see the implemented row in the layer table for the full description. Section 13. |
| `tool-web` | R4 | `WebFetch(url, prompt?)` — fetch http(s) URL, parse HTML via cheerio, convert to markdown via turndown; 5 MB body cap, 30 s timeout, ≤5 redirects, optional allow-list via `tools.WebFetch.allowed_domains`. `WebSearch(query, allowed_domains?, blocked_domains?)` — provider dispatch via `CREWHAUS_SEARCH_PROVIDER` (brave\|tavily) + `CREWHAUS_SEARCH_API_KEY`; returns `{title, url, snippet}[]` as a numbered list; clean refusal when env unset. Anthropic server-side `web_search` deferred to Section 17. Section 14. |
| `tool-image` | R4 | `ReadImage(path)` — resolves under `process.cwd()` (mirrors tool-fs traversal defense), validates content via magic bytes (PNG/JPEG/GIF/WebP), enforces 5 MB per-image cap, returns an Anthropic `image` content block (base64). The runtime forwards the block verbatim into `tool_result.content` (cross-cut: widened `RegisteredTool.execute` return type from `Promise<string>` to `Promise<string \| ToolResultContent>`). 20-images-per-turn cap deferred to a follow-up that needs `RunContext.turnMetrics`. Section 14. |
| `tool-fetch` | R4 | `Fetch(url, method?, body?, headers?)` — generic HTTP for API integrations. **Fail-closed allow-list**: empty `tools.Fetch.allowed_origins` denies every URL. Origins matched scheme+lowercase-host+non-default-port exactly. Layered SSRF defense rejects loopback, link-local, RFC1918, and mDNS targets *even when allow-listed*; DNS lookup catches rebinding. ≤5 redirects (re-checked per hop), 5 MB body cap, 30 s timeout, `Cookie`/`Set-Cookie`/`Authorization` headers stripped from responses. Section 14. |
| `trace-event-bus` | R15 | In-process pub/sub bus on `RunContext.eventBus`. `TraceEvent` discriminated union over 15 kinds (turn, model, tool, mcp, hook, compaction, permission, recovery, sub-agent boundaries) with envelope `{runId, sessionId, turnNumber, traceId, spanId, parentSpanId?, timestamp}`. 5000-event ring buffer (`recent({since?, kinds?})`); `publish(ev, {ephemeral})` skips the buffer for `model_stream_token` so 10k-token responses don't evict useful events; subscriber failures isolated and counted via stub logger. W3C trace-context propagation (`TRACEPARENT` env, `currentTraceparent()`); sub-agents inherit `parent.bus.traceId` so OTel stitches one trace. Section 15. |
| `otel-exporter` | R15 | Dependency-free OTLP/JSON exporter. Pairs lifecycle events into spans using `gen_ai/*` semantic conventions; `gen_ai.system` is provider-driven (Section 17 — `"anthropic"` / `"openai"` / `"gcp.gemini"` / `"aws.bedrock"`) via `genAiSystem(providerId)`. Other attributes: `gen_ai.request.model`, `gen_ai.usage.input_tokens`/`output_tokens`, `gen_ai.response.finish_reason`, `gen_ai.operation.name="chat"`. Tool spans use `code.function = <toolName>` plus `crewhaus.tool.*` extension keys; MCP spans use `mcp.server.name`/`mcp.tool.name`; `model_stream_token` events ride as span events on the model span (never per-token spans). 5s batch flush + sync flush on shutdown. Honors `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_HEADERS` (`k1=v1,k2=v2`), `OTEL_SERVICE_NAME` (default `crewhaus`). Section 15, extended in Section 17. |
| `metrics-collector` | R15 | Counters (`crewhaus_turns_total`, `crewhaus_tool_calls_total{tool}`, `crewhaus_tokens_total{direction}`, `crewhaus_errors_total{kind}`) + histograms (`crewhaus_turn_duration_seconds`, `crewhaus_tool_duration_seconds{tool}`, `crewhaus_model_ttft_seconds` computed from first-token-after-`model_request` per traceId) using Prometheus default buckets. Three sinks: `prometheusTextfile(path)` (atomic write on flush), `stdoutJson()` (**buffers + emits one JSON dump on flush** to avoid interleaving with assistant text on stdout), and `httpServer(port)` (pull-based `/metrics` exposition). Selected via `CREWHAUS_METRICS=stdout\|textfile\|textfile:/path\|http:PORT`. Section 15. |
| `structured-event-printer` | R15 | Pretty (`CREWHAUS_TRACE=pretty`) — color-coded events on stderr with one-line-per-kind formatters; `model_stream_token` deltas collapse into a rolling line via `\r` rewrites on a TTY (or one summary line on `model_response` when stderr is piped). JSON Lines (`CREWHAUS_TRACE=json`) — one JSON object per event on stdout. `NO_COLOR` disables ANSI escapes. Section 15. |
| `eval-dataset` | R15 | Loaders for JSONL, CSV, YAML, and HTTP-fetched datasets. Schema: `{ name, samples: Array<{ id, input, expected_output?, expected_tools?, metadata? }> }`. Lazy iterator API so 100k-sample datasets stream rather than load fully. T1 unit per loader format. Section 16. |
| `eval-grader` | R15 | Deterministic graders: `exact_match`, `contains`, `regex`, `json_path`, `schema` (Zod), `tool_call_sequence` (matches the trace event sequence against an expected pattern). Composers: `all([...])`, `any([...])`, `weighted([...])`. Each grader: `(sample, runResult) → { passed, score, rationale }`. T1 per built-in grader; T9 over `weighted` + `all`/`any` composition. Section 16. |
| `eval-judge` | R15 | LLM-as-judge with configurable judge model (resolved through `model-router`; default same as agent). Structured output via Zod; returns `{ score: 1..5, rationale }`. Rubric format: YAML with named criteria, each carrying a description and a 1–5 anchor per level. Prompt-injection defense: judge prompt explicitly templates the sample's expected output as untrusted data. T8 13-payload prompt-injection corpus locks in the structural defense. Section 16. |
| `eval-runner` | R15 | For each sample (concurrency configurable, default 4): create fresh `sessionId`, instantiate `runChatLoop` with the sample input as the seed user message, subscribe to the trace bus to capture events, await completion, apply each configured grader, record per-sample result. Persists raw run artifacts to `.crewhaus/evals/<runId>/<sampleId>/{transcript.jsonl, events.jsonl, grades.json}`. Honors `--seed` where the provider supports temperature reproducibility. T7 200-sample concurrency-8 SLO completes <60s. Section 16. |
| `eval-report` | R15 | Renders results as HTML (sortable per-sample table, drill-down panel with transcript + trace timeline + grader rationales) and JSON. Aggregates: pass rate, mean score, p50/p95 turn count + latency, total token cost (per-provider pricing table). Diff mode: `eval-report diff <prev-runId> <new-runId>` highlights samples that flipped pass/fail between runs. Wired into `crewhaus eval` and `crewhaus eval-report diff` subcommands. Section 16. |
| `sandbox` | R8 | Containerised exec environment. `createSandbox(opts)` → Sandbox with `exec(command, stdin?, env?)` returning `{stdout, stderr, exitCode, durationMs}`. Backends: `docker` (default; assumes daemon), `podman`, `noop` (in-process; tests only — opt-in via `CREWHAUS_SANDBOX=noop`). Defaults: network "none", 512 MB memory, 1.0 CPU, 60s timeout, read-only root, scratch tmpfs at `/tmp`. Mount whitelist; image allowlist via `CREWHAUS_SANDBOX_ALLOWED_IMAGES`. T8 escape attempts (mount traversal, --privileged via env, image-tag injection). Section 18. |
| `tool-code-execution` | R4 | Sandboxed Python/JavaScript/Shell REPL tools. Each spawns a sandbox with the matching curated image (`python:3.13-slim`, `node:22-alpine`, `alpine:3.19`). Warm pool (configurable; cold-start ≤500ms target). All flags: `concurrencySafe=false, readOnly=false, destructive=true, requiresSandbox=true`. Streams stdout/stderr through trace bus as `tool_stream_chunk` events. Section 18. |
| `prompt-injection-detector` | R8 | Output classifier with 3 layers: (1) regex rules over OWASP LLM Top-10 corpus + 50 hand-crafted vectors; (2) structural heuristics (trailing imperative blocks, BOM tampering, role-marker injection); (3) optional LLM-as-classifier for "suspicious" tier (gated by `CREWHAUS_PI_CLASSIFIER_MODEL`). Returns `{ classification, score, hits: [{ rule, span, severity }] }`. Wired into runtime-core's post-tool path: malicious → strip + redaction notice; suspicious → kept-but-warned. Section 18. |
| `checkpoint-store` | R7 | Resumable graph state. `CheckpointStore` interface: `save(graphRunId, nodeName, state)` → CheckpointId; `load(graphRunId, checkpointId?)`; `list(graphRunId, opts?)`; `branch(graphRunId, checkpointId)` → `{ newGraphRunId, head }`. File-backed JSONL under `.crewhaus/graphs/<graphRunId>/<checkpointId>.json`; structured-clone state serialisation; references to large objects (tool-result-store paths) preserved. T7 5000-checkpoint stress; T9 property tests over branch + diff invariants. Section 19. |
| `graph-engine` | R11 | Node/edge runtime. Graph builder: `addNode(name, fn)`, `addEdge(from, to, condition?)`, `addParallel([...])`, `setEntry(name)`, `compile()`. `compile()` returns RunnableGraph with `run(input, opts)` → `AsyncIterable<NodeEvent>` over `node_start | node_end | edge_taken | branch | checkpoint | hitl_pause`. HITL: nodes can `await ctx.requestApproval(prompt)` — engine pauses, persists checkpoint, returns. `runnable.resume(checkpointId, decision)` reattaches. Each node receives RunContext so trace events publish naturally. Section 19. |
| `branch-history` | R7 | Time travel over `checkpoint-store`. `BranchHistory(store).branchAt(graphRunId, checkpointId)` returns a fresh RunnableGraph that re-runs from a prior checkpoint without mutating the original timeline. Diff API: `diff(graphRunIdA, graphRunIdB)` → `NodeDiff[]` showing which nodes diverged. Section 19. |
| `durable-execution` | R11 | Wraps `graph-engine` with exactly-once node-execution semantics under crash. On restart, replay restarts from the last successful checkpoint; idempotency keys per node so retried tool calls do not double-execute side effects. T4 replay test: kill mid-node, restart, confirm exactly-once. Section 19. |
| `target-graph` | F2 | Codegen for `target: "graph"`. Spec carries `entry: string`, `nodes: { [name]: { instructions, tools?, model?, hitlPrompt? } }`, `edges: [{ from, to }]`, optional checkpoint adapter config. Single-file `agent.ts` imports `graph-engine` + `checkpoint-store`, builds the graph, runs against `process.stdin` (or webhook in daemon mode). Section 19. |
| `gateway-protocol` | R16 | JSON-RPC IDL for the managed daemon. Methods: `runs.create`, `runs.continue`, `runs.cancel`, `runs.subscribe` (SSE), `sessions.list`, `sessions.fork`, `audit.tail`. Versioned envelope (`protocol: "crewhaus.v1"`); Zod schemas exported as the wire contract. T2 contract tests against fixture envelopes; T9 round-trip property tests. Section 20. |
| `gateway-server` | R16 | `Bun.serve` daemon speaking `gateway-protocol`. Auth via short-lived bearer tokens minted by an external IDP (JWT with `tenant_id` claim). Per-tenant rate limit + budget enforcement; refuses requests over budget. Routes runs into `runtime-core`, threading `RunContext.tenantId` through. T8 expired-JWT, tenant-id mismatch, budget exhaustion. Section 20. |
| `policy-engine` | R8 | Side-effect classification + audit. Tool flags: `sideEffect: "none" | "filesystem" | "network" | "external" | "messaging"`; tools without an explicit flag default to `"external"` (fail-closed). `evaluatePolicy(call, mode, rules, tenantPolicy)` → `"allow" | "audit-and-allow" | "deny"`. `audit-and-allow` decisions append a structured record to the tenant's audit log. Composes with `permission-engine` (Section 7) — policy runs after permission grants, before exec. Section 20. |
| `tenancy` | R17 | Per-tenant isolation. `Tenant`: `{ id, sessionRoot, evalRoot, toolResultRoot, policyOverrides, budget }`. `withTenant(tenantId, fn)` rebases all storage paths under the tenant's roots; cross-tenant reads throw at every storage layer (sessions, evals, tool-results). Lookup by `tenant_id` JWT claim from the gateway request. T8 cross-tenant read attempts rejected at every storage layer. Section 20. |
| `audit-log` | R17 | Append-only audit trail. One JSONL per tenant per day; append-only, owner-only mode, hash-chained (each line carries `prevHash`) so any tampering is detectable. Records: every policy decision, every model call (model+tokens+cost), every tool classification, every gateway request. `crewhaus audit verify <tenant>` re-walks the chain and reports the first broken link. T4 hash-chain verification under deliberate tamper. Section 20. |
| `target-managed` | F2 | Codegen for `target: "managed"`. Spec emits `daemon.ts` + `gateway-server` boot, wires `policy-engine` into the run loop, registers the configured audit-log adapter. Multi-file output (extends Section 12 channel-bot pattern). Section 20. |
| `pipeline-engine` | R11 | Component DAG runtime. Pipeline builder: `addComponent(name, component)`, `connect(from, to, mapping?)`, `setOutput(name)`, `compile()`. Components are pure functions `(inputs) → outputs`; engine schedules topologically and parallelises independent branches. Streaming components yield events the pipeline forwards through the trace bus as tool spans. T9 topological-order invariant under random component ordering. Section 21. |
| `chunker` | R12 | Document chunking. `chunk(doc, opts: { strategy: "fixed" | "semantic" | "markdown", size, overlap })` → `Chunk[]`. Strategies: fixed-size (chars or tokens), semantic (sentence boundaries via `Intl.Segmenter`), markdown-aware (header-bounded). T9 reconstruction property test (no chars lost, overlap correct). Section 21. |
| `embedder` | R12 | Embedding model adapter. `embed(texts, opts)` → `number[][]`. Backends: OpenAI (`text-embedding-3-small/large`), Voyage (`voyage-3`), Cohere, local (Ollama / sentence-transformers via HTTP). Selection mirrors `model-router` prefix grammar: `embedder.openai/text-embedding-3-small`, `embedder.local/...@<url>`. Batches ≤100 texts per call; honors provider rate limit. Section 21. |
| `vector-store` | R12 | Vector index. `VectorStore`: `upsert(id, embedding, metadata)`, `query(embedding, k, filter?)` → `Hit[]`, `delete(id)`. v0 backend: `in-memory` (flat L2 index). Other backends (lance, qdrant, pinecone, weaviate) stubbed; constructors throw "not implemented in v0". `Hit`: `{ id, score, metadata }`. T9 stability under concurrent upserts. Section 21. |
| `tool-retrieve` | R4 | Agent-facing retrieval. `Retrieve(query, k?, filter?)` — embeds query → vector-store query → returns top-k hits as a numbered list with citations. Composes `embedder` + `vector-store` configured at boot via `tools.Retrieve.{ embedder, vector_store }`. T8 filter injection attempts. Section 21. |
| `target-pipeline` | F2 | Codegen for `target: "pipeline"`. Spec carries `agent`, `retrieve: { embedderModel, vectorBackend, defaultK }`, `indexing: { chunkStrategy, chunkSize, chunkOverlap, documents }`. v0 emits a CLI-mode bundle (HTTP serving deferred to a follow-up alongside `target-managed`'s gateway). Section 21. |
| `agent-handoff` | R10 | `createHandoffTool({from, targets})` — builds an in-band `Handoff(target, reason, context?)` tool used by `crew-orchestrator` roles. The tool's input schema is generated dynamically with the crew's role names as a Zod enum; self-handoffs are rejected at execute-time. Calling Handoff records a request on `bridge.crewMailbox` and returns a confirmation string; the model is expected to end its turn after seeing it. The orchestrator then dispatches to the target role, threading the parent transcript via `resume`. Section 22. |
| `a2a-protocol` | R5 | In-crew agent-to-agent peer messaging. `createSendMessageA2ATool({from, targets})` builds a `SendMessage(target, payload, kind?)` tool that calls `bridge.crewMailbox.sendA2A(toRole, payload)` synchronously and returns the peer's reply as the tool result. `buildEnvelope` produces the wire shape `{from, to, kind, payload, traceparent}`; the traceparent is stamped from `bus.currentTraceparent()` so every A2A message lands under the crew's single trace id. DISTINCT from `tool-message-channel`'s SendMessage (which posts to Slack). Section 22. |
| `crew-orchestrator` | R10 | Role-based dispatcher. `Crew()` builder: `addRole(name, def)`, `setEntry(name)`, `setRouting(routerFn)`, `setName(name)`, `compile()` → `RunnableCrew`. `run(input, opts) → AsyncIterable<CrewEvent>` over `role_start | handoff | a2a_message | role_end | crew_done`. Drives every role via `runChatLoop({ singleTurn: true, runContext, resume? })` — single shared `RunContext` across activations so all spans nest under one trace id. Auto-injects Handoff + SendMessage tools per role and grants them at the `flag` rule layer. Refusal-loop guard trips `HandoffRefusedError` after `refusalDepth` (default 2) consecutive handoffs; A2A recursion capped at `maxA2ADepth` (default 3); total activations capped at `maxActivations` (default 16). Section 22. |
| `target-crew` | F2 | Codegen for `target: "crew"`. Spec carries `model`, `entry`, `roles: { [name]: { instructions, model?, tools?, ... } }`, optional `routing: { kind: "match" \| "llm", match?: { [from]: [{contains, to}] } }`. Multi-file output: `daemon.ts` (reads stdin, runs the crew, prints one JSON `CrewEvent` per stdout line, exits non-zero on `HandoffRefusedError`), `orchestrator.ts` (composes the `Crew` builder), one `agent_<role>.ts` per role exporting a `RoleDefinition`. Section 22. |
| `citation-tracker` | R-orch | Section 23 RES — file-backed JSONL citation tracker. `createCitationTracker({runId, rootDir})` opens a per-research-run pair of append-only files (`fetches.jsonl` + `citations.jsonl`) under `<rootDir>/<runId>/`, plus a sha256-keyed content cache directory. `hasFetched(url)` lets the crawler skip re-fetches; `recordFetch({url, content})` is idempotent (first write wins); `recordCitation({url, snippet})` appends. `listCitationsOrdered()` returns citations in append-order — deterministic across re-runs, anchoring report-writer's "byte-identical citation block" guarantee. Boots by replaying the JSONL so kill-and-resume sees prior records. |
| `crawler` | R-orch | Section 23 RES — citation-tracker-backed source fetcher. `createCrawler({tracker, config})` returns `Crawler.fetch(url) → {content, fromCache, sha256, retrievedAt}`. Two transports: `https?://` (origin allow-list, redirect cap, body cap, optional per-domain rate limit) and `file://` (allow-list rooted on configured directories). Every call hits the tracker's `getFetchedContent` cache first; misses go through the transport, then `recordFetch`. Also exports `createSourceTool({crawler})` (model-facing `Source(uri)` tool) and `createCiteFactTool({tracker})` (model-facing `CiteFact(uri, snippet, supportingClaim?)` tool). |
| `planner` | R-orch | Section 23 RES — LLM-backed goal decomposer. `decompose(goal, opts)` calls the configured model with a fixed system prompt asking for `branchingFactor` sub-questions in a fenced JSON block; tolerant fence extractor + raw-`{...}` fallback; one auto-retry on parse failure. Returns `Plan { goal, subQuestions, branchingFactor, model, createdAt }` ready to checkpoint. Resolves the model via `@crewhaus/model-router` (so research can sit on Anthropic, OpenAI, etc.) with a test-only `_adapter` injection backdoor. |
| `report-writer` | R-orch | Section 23 RES — markdown + JSON report assembly. `writeReport({goal, branches, citations})` numbers citations by URL on first appearance (NOT by emit order within a branch), so a re-run that ends with the same citation set produces a byte-identical citation block. Emits a `# goal` H1 + `## sub-question` H2 per branch + a `## Citations` footer with `[N] <url>` lines and indented snippet bodies. The JSON shape mirrors the markdown for downstream consumers. |
| `target-research-bundle` | F2 | Section 23 RES — single-file `agent.ts` codegen for the research target. The emitted daemon parses CLI args (`--goal`, `--resume <runId>`, `--branching N`), opens a citation tracker under `.crewhaus/research/<runId>/`, calls `planner.decompose` (or loads a checkpoint), iterates sub-questions in order with one `runChatLoop({singleTurn: true})` per branch (auto-injected `Source(uri)` + `CiteFact(uri, snippet)` tools, allowed at the `flag` rule layer), persists `state.json` after each branch, respects `maxDurationMs`, and emits one JSON event per stdout line (`run_start | resume | plan_start | plan_done | sources_resolved | branch_start | branch_end | budget_exceeded | run_done`). PR feat/section-23-res. |
| `queue-protocol` | R14 | Section 23 BATCH — abstract queue interface used by `queue-consumer`. Methods: `pull({maxBatch, visibilityTimeoutMs})`, `ack(jobId)`, `nack(jobId, reason)`, `extendVisibility(jobId, ms)`, `stats()`. Reasons: `transient | permanent | timeout` — permanent moves the job to a DLQ, transient + timeout return it to pending. v0 ships an in-memory adapter (`createInMemoryQueue`) with monotonic JobIds, deterministic clock injection, and an `inspect()` test helper. SQS / Redis Streams / Postgres adapters land in follow-up PRs. |
| `queue-consumer` | R14 | Section 23 BATCH — long-running pull loop. `startConsumer({queue, handler, concurrency, visibilityTimeoutMs, idempotencyStore?})` returns `RunningConsumer` with `drain()` / `stop()` / `inFlight()`. Concurrency-bounded `Promise.race` slot gating; sidecar `extendVisibility` keeps long-running model calls alive past the lease; idempotency-key cache short-circuits retries; handler throws → `nack(transient | permanent)` based on attempt count vs `maxRetries`. Drain semantics: stops new pulls but lets in-flight handlers complete + ack, used by SIGTERM paths. |
| `idempotency-keys` | R7 | Section 23 BATCH — `idempotencyKey(jobId, attempt)` SHA-256 truncated to 24 hex chars; `createInMemoryIdempotencyStore<T>()` returns a TTL-keyed Map (`get` / `set` / `size`); `withIdempotency(handler, {store, ttlMs})` wraps a handler so re-invocations with the same key return the cached SUCCESS result while errors bypass the cache. T9 property test: 5 retries with the same key → handler called once, identical results returned. |
| `target-batch-worker` | F2 | Section 23 BATCH — single-file `agent.ts` codegen for the queue-worker target. Constructs the configured queue adapter (in-memory in v0; SQS / Redis Streams / Postgres throw `not implemented in v0` at boot), optionally seeds the in-memory queue with `queue.seedJobs`, boots `queue-consumer` with the spec's agent as the handler (one `runChatLoop({singleTurn: true})` per pulled job), wires SIGTERM/SIGINT drain handlers, and exits cleanly when the queue goes idle. Streams JSON events on stdout (`worker_start | job_start | job_end | drain_start | drain_end | queue_idle | shutdown_received | worker_stop`). PR feat/section-23-batch. |
| `voice-runtime` | R16 | Section 24 VOICE — provider-agnostic realtime audio adapter. `RealtimeAdapter` interface with `connect/sendAudio/commitInput/sendText/interrupt/on/disconnect`, normalising every adapter to PCM 16-bit mono at 24kHz. v0 ships `createOpenAIRealtimeAdapter` over the v1 WebSocket protocol (handles `session.created`, `response.audio_transcript.delta/done`, `response.audio.delta`, `response.function_call_arguments.done`, `response.cancelled`); `createVapiRealtimeAdapter` is a stub that throws `not implemented in v0`. Test injection via `_ws` factory. PCM ↔ base64 helpers exported. |
| `vad-engine` | R16 | Section 24 VOICE — energy + zero-crossing-rate heuristic VAD over PCM 16-bit frames. `createVadDetector({aggressiveness: 0..3})` returns `detect(frame): "speech" \| "silence" \| "transitioning"`. 30ms framing convention; `frames30ms(pcm, sampleRate)` helper splits buffers. No external ML deps. T1 + T9 noise-injection stability tests. |
| `barge-in-controller` | R16 | Section 24 VOICE — hysteresis-gated coordinator between `vad-engine` and a `RealtimeAdapter`. Watches inbound user-mic frames; when the user starts speaking AND the agent is currently playing audio (tracked via `audio_chunk` events), waits for `triggerFrames` consecutive speech-classified frames inside `windowMs`, then fires `adapter.interrupt()`. Default 4 frames in 200ms. Resets the speech-frame window on `transcript_final` / `interrupt` / `disconnect`. |
| `call-session` | R16 | Section 24 VOICE — call lifecycle state machine (`idle | dialing | connected | on-hold | transferred | terminated`) with pluggable `TelephonyAdapter` slot. `createInMemoryTelephonyAdapter` for tests + smoke; Twilio / LiveKit SIP / Vonage adapters land in follow-ups. Per-state listener hooks fire on transitions; listener errors are isolated. Illegal transitions throw `CallSessionError` without changing state. |
| `target-voice` | F2 | Section 24 VOICE — multi-file codegen for the realtime voice target. Emits `agent.ts` (config wrapper exporting `AGENT_CONFIG`), `voice-loop.ts` (per-call audio I/O loop wiring the RealtimeAdapter + barge-in-controller + vad-engine and waiting for `transcript_final`), and `daemon.ts` (entry with a `--smoke <pcm-path>` mode for headless tests). The smoke path emits one JSON `voice_event` per stdout line wrapping every adapter event. PR feat/section-24-voice. |
| `computer-use-driver` | R18 | Section 25 BROW — cross-platform mouse/keyboard/screenshot driver. `Driver` interface: `connect/goto/screenshot/click/type/key/scroll/getViewport/disconnect`. v0 ships a chromium backend over Playwright's bundled headless chromium (lazy-loaded so non-browser users don't need it in their tree). `host` and `remote` backends are stubs that throw at `connect()` per kickoff (host backend explicitly forbidden in smokes). |
| `tool-screen-capture` | R4 | Section 25 BROW — `Screenshot()` tool. Calls `driver.screenshot()`, encodes the PNG to base64, returns an Anthropic image content block. No filesystem touch (no path-traversal surface — the screenshot is in-memory). Composes with §14's `tool-image` content-block pattern. `readOnly: true`, `classifyOutput: false` (controlled output). |
| `tool-mouse-keyboard` | R4 | Section 25 BROW — `Click` / `Type` / `Key` / `Scroll` tools. ALL FOUR are `destructive: true` so §3 `permission-engine` refuses to grant `allow` in default mode without an explicit `alwaysAllow` rule. Driver errors are caught and surfaced as tool-result strings (so the model can recover). `createAllMouseKeyboardTools(opts)` returns the four tools as a struct for ergonomic registration. |
| `tool-vision-grounding` | R4 | Section 25 BROW — `FindElement(description)` tool. Takes a screenshot via the driver, sends image + description to a vision-capable Claude model, parses the model's fenced-JSON bbox response, returns `{bbox, centerX, centerY, confidence}`. Tolerant fence extractor + raw-`{...}` fallback; one auto-retry on parse failure. Grounding model configurable; defaults to the agent's primary model. |
| `target-browser-driver` | F2 | Section 25 BROW — single-file `agent.ts` codegen. The emitted daemon constructs the chromium driver, navigates to the spec's `startUrl` (when set), registers Screenshot + Click + Type + Key + Scroll + FindElement, reads a single user prompt from `--prompt` arg or stdin, runs `runChatLoop({singleTurn: true})`, and disconnects the driver in `finally`. Permissions block: spec yaml-source rules pass through verbatim — no flag-layer auto-grants for the destructive Click/Type/Key/Scroll, so the smoke's permission-floor probe works (no rules → default mode denies destructive). Streams `browser_start | navigated | browser_done` JSON events. PR feat/section-25-brow. |
| `trace-viewer` | F4 | Section 26 Studio — pure-logic Gantt-layout helper for `TraceEvent[]`. Pairs `*_start`/`*_end` events on `spanId` into spans with absolute t0/t1 ms; converts point events (hook_fired, permission_decision, error_recovered, handoff, a2a_message, crew_done) into single-instant spans. Computes BFS depth from parentSpanId chains. Deterministic — same corpus → same `Timeline { t0, t1, spans[] }`. Used by studio-ui's run viewer + embedded inside eval-report's per-sample drilldown. |
| `graph-visualizer` | F4 | Section 26 Studio — deterministic 2D layout for `IrGraphV0`. Layered BFS positioning (entry on the left, BFS columns to the right; unreachable nodes past the last reachable column). Returns `Layout { width, height, nodes, edges }` with resolved fromXY/toXY for SVG path emission. Also ships a tiny `renderSvg()` helper for first-paint + smoke artefacts. T1 stability invariant: same graph → byte-identical positions. |
| `wizard` | F4 | Section 26 Studio — 5-question guided spec creation. State machine: target → name → model → tools (only for cli/research/batch) → permissionMode. `nextQuestion(state)` returns the next prompt; `answerWizard(state, answer)` advances; `compileWizard(state)` synthesizes YAML by patching the matching scaffold-template with the user's answers, plus a `.env.example` listing every `$VAR_NAME` referenced. T3 covers all 10 target shapes end-to-end. |
| `scaffold-templates` | F4 | Section 26 Studio — 10 built-in spec templates, one per target shape (cli-coding-agent, slack-bot, research-agent, rag-bot, crew-research, graph-stateful, managed-multitenant, batch-worker, voice-realtime, browser-driver). Returns `{ id, target, title, description, yaml }` so studio + `crewhaus init` consume the same map. Every template's YAML is well-formed (T9 invariant — passes the spec parser without modification). |
| `plugin-sdk` | F5 | Section 26 Studio — typed surface for third-party studio plugins. `definePlugin({name, version, hooks?, panes?})` validates + freezes the definition. Hooks: `onSpecLoad`, `onTraceEvent`, `onEvalSampleRendered`. Panes contribute `{id, title, html}` UI tabs. `assertPluginPathsStaySandboxed(plugin, sandboxRoot)` rejects file:// URLs in pane HTML that escape the plugin's sandbox root (T8 path-sandbox guard; full content-sandbox isolation deferred). |
| `studio-server` | F4 | Section 26 Studio — Bun.serve daemon. v0 endpoints: /healthz, /api/templates, /api/specs (CRUD), /api/wizard/start|step|compile, /api/runs (POST → SSE), /api/graph-layout/:name, /api/plugins (lazy-loads `~/.crewhaus/plugins/<name>/index.ts` and applies the sandbox-path guard at load-time). v0 SSE emits canned `run_start | trace | event: done` events; full `runChatLoop` dispatch lands in a follow-up. Auth aligned with §20 gateway-server's HS256 JWT (open in dev mode). PR feat/section-26-studio. |
| `studio-ui` | F4 | Section 26 Studio — vanilla TS HTML+JS bundle (no Lit / Monaco / React in v0 — keep deps tiny). `renderStudioHtml({ title? })` returns a complete HTML doc with three nav tabs (Specs / Wizard / Plugins). `getStudioJs()` returns the inline JS bundle that drives `/api/specs` listing + textarea editor + `/api/wizard/start` walkthrough + `/api/plugins` panel. Lit + Monaco land in a follow-up. |

**Total**: 116 of ~190 modules. **All roadmap sections (1–26) shipped.**

### In progress (🚧)

*none*

---

## Dependency & risk conventions

The per-layer tables in PART A and PART B include a `Depends on` column for every module. The convention:

- The cell lists catalog modules that must be built (or have a stable contract) before the row can land. Cross-layer references are bare module names; foundational entries use `—`.
- "Blocks" relationships are the inverse and are derivable — see PART G.5 (Critical path & risk register) for a curated view of the highest-leverage inverse edges.
- For implemented (✅) and in-progress (🚧) rows, the column captures the dependency that *was* satisfied; the row no longer carries deferred risk.

Unbuilt modules also carry a one-character risk marker placed before the module name:

- 🔴 — **high-risk**: critical-path bottleneck (blocks ≥3 downstream modules or a target shape), security-sensitive design surface, or substantive design unknowns relative to the references.
- 🟡 — **medium-risk**: well-understood pattern but several non-trivial dependencies, novel-to-this-stack, or moderate blast radius if the contract changes.
- *(no marker on unbuilt rows)* — low-risk: isolated, proven pattern with few dependents.

Implemented (✅) and in-progress (🚧) modules have already absorbed their risk and do not carry the marker. The marker is a guide for sequencing, not a blocker — a 🟡 module can still ship in any phase as long as its `Depends on` row is satisfied.

---

## Critical path snapshot

All 11 target shapes ship; the v1.0 product surface is complete. The remaining roadmap (build-roadmap Sections 27–31) is about hardening what shipped: production-quality cost / rate-limit / cache / secrets controls, safe-rollout deployment surface, deeper evaluation tooling, completion of v0-stub backend adapters (queue / vector / telephony / browser host backend), and the Studio UI v1 polish (Lit + Monaco + run replay + plugin sandbox content isolation).

| # | Module | Layer | Section | Why critical | Direct downstream blocked |
|---|---|---|---|---|---|
| 1 | 🔴 `cost-tracker` | R15 | §27 | Per-run / per-tenant token + dollar cost accumulation; first observability layer that an operator pays attention to in production. | `budget-enforcer` (MGD), `eval-report` cost columns, deploy-gate cost regression check |
| 2 | 🔴 `rate-limiter` | R-infra | §27 | Token-bucket + leaky-bucket gating per tenant, per provider, per tool. | `gateway-server` per-tenant enforcement, `circuit-breaker` composes on top, multi-provider `model-router` failover |
| 3 | 🔴 `circuit-breaker` | R-infra | §27 | Half-open state for unhealthy providers; integrates with `model-router` to skip a provider for N seconds after repeated failures. | Multi-provider failover, MGD reliability SLO, `recovery-engine` exhaustion path |
| 4 | 🔴 `deployment-controller` | F3 | §28 | Roll spec changes from staging to production with version pinning. | `canary-controller`, `migration-runner`, MGD upgrade flow |
| 5 | 🔴 `canary-controller` | F3 | §28 | Gradual percent-of-traffic rollout gated by `eval-runner` regression checks. | Safe rollouts, MGD blue/green, deploy-as-eval-gate pattern (PART F #10) |
| 6 | 🔴 `prompt-optimizer` | F-eval | §29 | DSPy-style automated prompt tuning using §16 eval stack as the fitness function. | EVAL depth, regression-resilient agents, `target-eval-bundle` quality bar |
| 7 | 🔴 `target-eval-bundle` | F2 | §29 | First-class EVAL target shape (today eval is invoked via `crewhaus eval` subcommand only). | EVAL-as-deploy-gate, hosted eval services, multi-tenant eval matrices |
| 8 | 🟡 `secrets-manager` | R-infra | §27 | OAuth / API key / AWS cred rotation + audit. | Long-running daemons (CHN/MGD), key rotation without daemon restart, regulated-tenant compliance |
| 9 | 🟡 `prompt-cache-manager` | R6 | §27 | Explicit cache rotation across long runs; today the cache is per-`runChatLoop` only. | Long-running CHN/MGD daemons, RES multi-hour runs, cost reduction at scale |
| 10 | 🟡 `regression-runner` | F-eval | §29 | Diff-based pass/fail flip detection on every CI run, gated on §16 eval stack. | CI deploy-gate, prompt-optimizer regression check, `eval-report diff` automation |

(Sections 16–26 closed: §16 EVAL stack, §17 multi-provider model router, §18 sandbox + prompt-injection detector, §19 GRPH, §20 MGD, §21 RAG, §22 CRW, §23 RES + BATCH, §24 VOICE, §25 BROW, §26 Studio. All 11 target shapes ship; six top-of-mind hardening + depth modules in this snapshot are the highest-leverage next steps.)

Cross-cutting hardening note: as of Section 18, `tool-code-execution` runs in `sandbox` with network=none / read-only-root defaults. `tool-bash` (R4) still operates at host trust level by design — it's the operator-controlled escape hatch. Any production EVAL or MGD deployment running untrusted samples should prefer `Python` / `JavaScript` / `Shell` (sandboxed) over `Bash` (host).

---

## Target harness shapes (11)

The catalog must support producing all of these shapes from a single spec:

| Code | Shape | Reference style | Distinguishing needs |
|---|---|---|---|
| **CLI** | CLI coding agent | Claude Code, gstack | TUI, file/bash tools, hooks, skills, slash commands, MCP, plan mode |
| **CHN** | Channel-based assistant | OpenClaw | Telegram/Slack/Discord/WhatsApp/iMessage adapters, cron, heartbeats, gateway, daemon |
| **CRW** | Multi-agent crew | CrewAI, AutoGen, ADK | Roles, handoffs, structured event flows, A2A |
| **RAG** | RAG pipeline | Haystack, LlamaIndex | Components/pipelines, retrievers, rankers, doc loaders, chunkers, KGs |
| **EVAL** | Evaluation harness | HELM, Ragas, lm-evaluation-harness | Datasets, graders, replay, benchmarks, trajectory grading |
| **MGD** | Enterprise/managed runtime | MAF, ADK, Anthropic Managed | Graph workflows, OTel, A2A/MCP, audit, tenancy, durable execution |
| **GRPH** | Stateful graph runtime | LangGraph | Checkpointing, time-travel, durable execution, HITL |
| **RES** | Autonomous research agent | Deep Research, Manus | Long-horizon execution (hours), citations, branch exploration, report synthesis |
| **VOICE** | Voice-first / realtime | OpenAI Realtime, Vapi | VAD, barge-in, streaming audio, call lifecycle, telephony |
| **BROW** | Browser / computer-use | Operator | Screen capture, mouse/keyboard, vision-grounded actions, DOM/AX tree |
| **BATCH** | Batch / queue worker | none mainstream | Queue consumer, idempotency, retry, rate limit, no live UI |

---

## Test layer templates

Every module entry references one or more of these standard test layers (defined in `docs/AI-Harness-Systems.md` §test layer matrix):

| Code | Layer | What it proves |
|---|---|---|
| **T1** | Unit | Pure logic correct (schema validation, branching, parsing) |
| **T2** | Contract | Adapter honors model/tool/protocol schemas (MCP, A2A, function-call JSON) |
| **T3** | Integration | Module wires correctly with neighbors (orchestrator + tools + permission) |
| **T4** | Replay | Deterministic replay of recorded traces does not regress behavior |
| **T5** | Golden | Fixed dataset → expected metrics within thresholds (eval-graded) |
| **T6** | Trace-grading | Trajectory quality — tool choices, handoffs, approvals, safety |
| **T7** | Load/soak | Concurrency, long-run, resume-after-failure, storage pressure |
| **T8** | Security | Prompt injection, tool-escape, policy bypass, PII exfil, sandbox abuse |
| **T9** | Property | Randomized property-based testing (fuzz inputs, invariants) |

---

# PART A — Factory-Level Modules (the meta-harness itself)

These live in `crewhaus-factory` and produce/operate generated harnesses. They are **not** embedded in the generated artifact.

### Layer F1 — Spec & IR

| Module | Responsibility | Refs (`reference-repos/`) | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`spec-schema`** | Define user-facing harness spec DSL (YAML/TS): agents, tools, channels, workflow, eval, deploy. Versioned, JSON Schema-backed. Currently a `z.discriminatedUnion("target", [cli, workflow, channel])`: CLI carries `agent` + optional `tools[]` + optional `mcp_servers` + optional `permissions`; workflow carries top-level `model` + `steps[]` (each with `name`, `instructions`, optional `model` override, optional `tools[]`) + optional `mcp_servers` + optional `permissions`; channel adds `channels.{slack}` (with secret-ref fields) + `routing.sessionKey` ∈ `thread \| user \| channel`. `permissions.mode: bypass` is rejected at parse time on every shape. | `agent-framework/python/.../declarative/`, `adk-python/cli/`, `openclaw.yaml` schema, `crewAI/.../agent` configs, `claude-code/.../settings.ts` | All | DSL design tradeoffs (YAML vs TS); how MAF declarative format vs ADK YAML differ; LangGraph imperative vs declarative split | T1, T9 | — |
| ✅ **`spec-parser`** | Parse, lint, resolve includes/macros/overlays → AST. | `langgraph/_validate.py`, `haystack` pipeline YAML loader, `crewAI/.../flow_serializer.py` | All | Include/import semantics; cyclic detection; overlay merging | T1, T9 | `spec-schema` |
| ✅ **`spec-validator`** | Type-check, resolve refs, verify tool/agent/model existence, profile constraints. | `agent-framework/.../_validation.py`, `langgraph/_validate.py`, `haystack/.../Pipeline.validate` | All | Cross-module ref resolution; profile constraint propagation | T1, T2, T9 | `spec-schema`, `ir-model` |
| ✅ **`ir-model`** | Canonical typed IR: agents, nodes, edges, events, tools, policies, memory, evals, channels, schedule. Runtime-agnostic. `IrV0` (cli) carries name, agent, tools, mcp_servers, permissions; `IrWorkflowV0` carries name + `steps[]` (each step's `model` resolved at lower-time as `step.model ?? workflow.model`) + mcp_servers + permissions; `IrChannelV0` adds `channels.{slack}` with `IrSecretRef` (`literal` or `env`) + `routing.sessionKey`. Exported as `IrNode = IrV0 \| IrWorkflowV0 \| IrChannelV0` discriminated union. T1 covers every shape, the discriminated-union narrowing in a switch, and the secret-ref + permission + MCP variants. | `AI-Harness-Systems.md` §IR schema; `agent-framework/.../_workflow_builder.py`; LangGraph graph IR; ADK declarative IR | All | Single IR vs multiple? Event encoding (envelope vs typed message); type system depth | T1 | — |
| 🟡 **`ir-passes`** | Optimization passes: dead-tool elimination, profile pruning, edge fusion, prompt-cache prefix sorting, redundancy collapse. | `claude-code/.../services/compact/grouping.ts` (sort pattern); `dspy/.../teleprompt/` pass style | All | Pass ordering; idempotence; pluggable pass registry | T1, T4 | `ir-model` |
| 🟡 **`spec-registry`** | Persist spec templates, versions, migration metadata, ownership, environment overlays, tenant isolation. | `openclaw/config/`, `claude-code/.../migrations/`, `adk-python/cli/` | All | Storage backend (FS / SQL / object); multi-tenant isolation | T1, T3 | `ir-model`, `migration-engine` |
| 🟡 **`migration-engine`** | Versioned schema migrations across IR versions (template upgrade paths). | `claude-code/.../migrations/` (`CURRENT_MIGRATION_VERSION = 11`); LangGraph checkpoint version migrations | All | Forward + backward migration paths; deprecation policy | T1, T4 | `ir-model` |

### Layer F2 — Compiler & Codegen

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`compiler-core`** | Orchestrate frontend (parse → validate → IR) and dispatch to target backends; emit deployment bundle. `lower()` and `emit()` switch on `target` (`cli` → target-cli, `workflow` → target-workflow); workflow lowering resolves per-step model overrides against the workflow default. | LangChain LCEL → LangGraph compile path; MAF `_workflow_builder.py`; ADK declarative-config compilation | All | Pipeline composition; error reporting; partial compilation | T1, T3 | `spec-parser`, `spec-validator`, `ir-model` |
| ✅ **`target-graph`** | Section 19 — single-file `agent.ts` codegen. Builds a `createGraph({ checkpointStore })` with one node per IR-declared node (each LLM-backed via `runChatLoop({ singleTurn: true })`), wires edges + entry, and parses CLI args for `--resume <grun> <decision>` / `--branch-from <grun> <ckpt>`. HITL nodes call `ctx.requestApproval(prompt)` after the LLM turn. PR feat/section-19-grph-target. | `langgraph/.../pregel/`, `agent-framework/.../_workflows/_workflow.py` | **GRPH**, MGD, CRW | Pregel vs imperative; checkpoint scheme | T1, T4 | `compiler-core`, `codegen-templates`, `graph-engine`, `checkpoint-store` |
| ✅ **`target-workflow`** | Codegen for sequential workflow target. v0 emits an `agent.ts` that runs each step via `runChatLoop({ singleTurn: true })` with per-step instructions, model (override or workflow default), tools, and the prior step's terminal assistant text threaded forward as a synthetic user message. Async event semantics, pub/sub channels, parallel/conditional steps still pending. | `crewAI/.../flow/`, `llama_index/.../workflow`, `adk-python/.../flows/` | **CRW**, MGD, RES | Async event semantics; pub/sub vs typed channels | T1, T3 | `compiler-core`, `codegen-templates`, `runtime-orchestrator` |
| ✅ **`target-pipeline`** | Section 21 — emits a single `agent.ts` that boots embedder + vector-store + Retrieve registration, runs a chunk → embed → store indexing pipeline over the spec's seed documents, then drops into `runChatLoop` with the spec's permissions. PR feat/section-21-rag-target. | `haystack/.../core/`, `haystack/components/` | **RAG** | Component decorator; DAG scheduling | T1, T3 | `compiler-core`, `codegen-templates`, `pipeline-engine` |
| ✅ **`target-managed`** | Section 20 — emits `agent.ts` (`runOneTurn({ tenantId, sessionId, input })`) + `daemon.ts` that boots `createGatewayServer` from `process.env.PORT` (default 3000) and `process.env.CREWHAUS_GATEWAY_JWT_SECRET` (refused if shorter than 16 chars). Per-tenant budget overrides emitted from the spec; graceful SIGTERM/SIGINT shutdown. PR feat/section-20-mgd-target. | Anthropic Managed Agents docs; OpenAI App Server JSON-RPC; AWS AgentCore; ADK platform | **MGD** | Manifest format diversity; deploy artifact shape | T1, T3 | `compiler-core`, `bundle-packager`, `audit-log`, `tenancy` |
| ✅ **`target-cli-bundle`** | Codegen for CLI coding agent: entry, REPL, tools, hooks, settings.json, MCP config. Emits grouped tool imports + `defaultCatalog.register()` calls + `tools: defaultCatalog.list()` for any spec with non-empty `tools`; unknown names fail at compile time via `TargetEmitError`. `mcp_servers` non-empty → `McpHost` boot block + try/finally `disconnectAll`. `permissions.rules` non-empty → layered rules object referencing `BUILTIN_DEFAULT_RULES`. Hooks/skills/slash-commands extension surface emitted unconditionally for parity with the interpreter path. T1 covers shebang/escaping/every wiring branch and combined-everything ordering. | `claude-code/main.tsx`, `claude-code/cli/`, `claude-code/entrypoints/` | **CLI** | Bundling (Bun/Node); side-effect imports | T1, T3, T7 | `compiler-core`, `codegen-templates`, `runtime-orchestrator` |
| ✅ **`target-channel-bot`** | Multi-file codegen for the channel target — first shape that emits more than one file. Slice ships `daemon.ts` + channel-generic `gateway.ts` + `session-router.ts` + `agent.ts`. Boots `Bun.serve`, registers tools (including opt-in `sendMessage`), instantiates configured adapters, dedups by adapter-supplied idempotency keys, derives `sess_<16hex>` ids from `sha256(routingKey)`, calls `runChatLoop({ singleTurn: true, resume })` per inbound. SIGINT/SIGTERM clean shutdown. Section 12. | `openclaw/gateway/`, `openclaw/daemon/`, `openclaw/channels/` | **CHN** | Service shape; multi-channel orchestration | T1, T3 | `compiler-core`, `codegen-templates`, `channel-adapter-base`, `session-store`, `event-log` |
| ✅ **`target-crew`** | Section 22 — multi-file codegen for the CRW target. Emits `daemon.ts` (reads stdin, runs the crew, prints one JSON `CrewEvent` per stdout line, exits non-zero on `HandoffRefusedError`), `orchestrator.ts` (composes the `Crew` builder from per-role imports, sets entry, wires routing if configured), and one `agent_<role>.ts` per role exporting a `RoleDefinition`. Permissions block lands on the daemon's `RunOptions`; `BUILTIN_DEFAULT_RULES` is imported only when the spec defines explicit rules. PR feat/section-22-crw-crew. | `crewAI/.../crew.py`; `agent-framework/.../_workflows/_workflow.py` | **CRW** | Multi-role codegen; routing table emission | T1 | `compiler-core`, `codegen-templates`, `crew-orchestrator`, `agent-handoff`, `a2a-protocol` |
| 🟡 **`target-eval-bundle`** | Codegen for eval harness: dataset loader, runner, graders, report writer. | `lm-evaluation-harness/lm_eval/`, `helm/benchmark/`, `ragas/.../evaluate.py` | **EVAL** | Task spec format; reproducibility | T1, T5 | `compiler-core`, `eval-service`, `dataset-registry`, `grader-registry` |
| 🔴 **`target-research-bundle`** | Codegen for autonomous research agent: planner, crawler, citation tracker, report writer. | `gstack-main/.../autoplan/`; CrewAI research flows; ADK research samples | **RES** | Long-horizon resumability; checkpoint-per-stage | T1, T4, T7 | `compiler-core`, `research-planner`, `research-checkpointer`, `report-synthesizer` |
| 🔴 **`target-voice`** | Codegen for voice/realtime service: realtime API client, telephony adapter, call lifecycle. | `openai-agents/.../realtime/`, `openai-agents/.../voice/`, `openclaw/realtime-voice/`, `claude-code/voice/` | **VOICE** | Realtime API model differences; PCM/Opus framing | T1, T2, T7 | `compiler-core`, `voice-runtime`, `audio-stream`, `call-session` |
| 🔴 **`target-browser-driver`** | Codegen for computer-use/browser-only agent: screen-grab loop, action driver, vision grounding. | `mcp__computer-use__*`, `mcp__Claude_in_Chrome__*`, OpenAI Operator-style refs | **BROW** | Action latency budget; screenshot frequency | T1, T3 | `compiler-core`, `computer-use-driver`, `tool-screen-capture`, `tool-vision-grounding` |
| 🟡 **`target-batch-worker`** | Codegen for queue/batch worker: queue consumer, dedup, retry, rate limit, observability. | None mainstream — design from primitives. Loose ref: `dramatiq/`/`celery/`/`temporal` patterns; `claude-code/.../tools/MonitorTool` (background loop pattern) | **BATCH** | Queue protocol abstraction; idempotency keys | T1, T3, T7 | `compiler-core`, `batch-runner`, `queue-consumer`, `idempotency-store` |
| 🟡 **`bundle-packager`** | Package compiled artifacts to deployable form (Docker/OCI/npm/pypi/manifest.json). | OpenAI Codex container packaging; `agent-framework/.../foundry_hosting/`; `adk-python/.../platform/` | All | OCI vs language-native packaging; layered caching | T1, T3 | `compiler-core`, target-* backends |
| ✅ **`codegen-templates`** | Templates per target (e.g. Bun + Ink CLI, FastAPI service, Rust microservice). | `gstack-main/.../templates/`; `claude-code/.../components/` Ink templates | All | Template engine choice (Handlebars/Jinja/string); diff-friendly emit | T1 | — |

### Layer F3 — Deployment & Operations

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🔴 **`deployment-controller`** | Deploy bundles to local / self-hosted / managed / hybrid. | LangSmith Deployment; ADK Cloud Run / Agent Engine; AgentCore; CrewAI AMP; Foundry | All | Common deploy abstraction across radically different platforms | T1, T3 | `compiler-core`, `bundle-packager`, `secrets-manager` |
| 🟡 **`deployment-profiles`** | Profile defs (local-dev, k8s, lambda, agent-engine, foundry, agentcore, hybrid-vpc). | `AI-Harness-Systems.md` §implementation blueprint | All | Config schema per profile; secret resolution per profile | T1 | `deployment-controller`, `secrets-manager`, `tenancy` |
| 🟡 **`canary-controller`** | Shadow traffic / subset routing / rollback on eval or latency regressions. | `AI-Harness-Systems.md` §canary; production patterns | MGD, CHN, GRPH | Routing strategy (header / cookie / hash); rollback triggers | T3, T5, T7 | `deployment-controller`, `eval-service`, `gateway-server` |
| 🟡 **`migration-runner`** | Run schema/state migrations across deployed runtime versions. | `claude-code/.../migrations/`; LangGraph checkpoint migrations | GRPH, MGD | Online vs offline migrations; rollback safety | T1, T4 | `migration-engine`, `checkpoint-store` |
| 🟡 **`upgrade-controller`** | In-place runtime upgrades, gradual rollout, version pinning. | `claude-code/.../utils/autoUpdater.ts` | CLI, CHN, MGD | Update channels; pinning semantics | T1, T3 | `deployment-controller`, `update-channel` |

### Layer F4 — Studio & Authoring UX

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🟡 **`studio-ui`** | Visual editor: graph layout, tool wiring, channel binding, eval suite editor. | `agent-framework/.../devui/`; OpenAI Agent Builder; LangSmith Studio; CrewAI AMP UI | All (authoring) | Graph layout algos; collaborative edit OT/CRDT | T1, T3 | `studio-server`, `graph-visualizer`, `trace-viewer` |
| 🟡 **`studio-server`** | Backend (project mgmt, multi-user collab, preview runs). | LangSmith control plane; OpenAI Agent Builder backend | All (authoring) | Auth, project model, collab session | T1, T3 | `spec-registry`, `eval-service`, `gateway-server` |
| 🟡 **`trace-viewer`** | Visualize traces/runs/replays; trajectory grading UI. | OpenAI Trace UI; LangSmith; `agent-framework/.../devui/`; Haystack tracing UIs | All | Trace data model; flame/swim-lane rendering | T1, T3 | `trace-event-bus`, `trace-recorder`, `otel-exporter` |
| 🟡 **`graph-visualizer`** | Render IR or live workflow graph (mermaid/d3/xyflow). | LangGraph viz; MAF `_viz.py`; `openai-agents/.../visualization.py` | GRPH, CRW, RAG, MGD | Layout for typed graphs; live updates | T1, T3 | `ir-model`, `graph-engine` |
| ✅ **`spec-cli`** | Command-line: `crewhaus compile / init / run / doctor` shipped (PR #2 + PR #10); `deploy / eval / watch` pending. `run` parses spec, lowers in-memory, and dispatches to `runChatLoop` with lazily-resolved tools; `doctor` reports auth, Bun version, and project-spec presence. | `claude-code/cli/`; `adk-python/cli/`; `openclaw/cli/`; gstack CLI | All | Subcommand discovery; help generation; shell completions | T1, T3 | `compiler-core`, `runtime-orchestrator` |
| 🟡 **`wizard`** | First-run profile selection, channel/auth setup. | `openclaw/wizard/`; gstack `setup`; `claude-code/.../screens/` | CLI, CHN | State machine; backtracking; resumability | T1, T3 | `spec-cli`, `scaffold-templates`, `secrets-manager` |
| **`scaffold-templates`** | Built-in spec templates per target shape. | gstack slash-commands as ref; `openclaw` profiles | All | Template parameterization; conventions | T1 | `spec-schema` |

### Layer F5 — Plugin SDK & Extension

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🔴 **`plugin-sdk`** | Public typed surface for third-party tools, channels, models, graders, target backends. | `openclaw/plugin-sdk/`; `claude-code/.../plugins/`; LangChain integration; Haystack component decorator | All | Stable contract surface; semver | T1, T2 | `tool-catalog`, `channel-adapter-base`, `model-adapter`, `grader-registry` |
| 🟡 **`plugin-registry`** | Discovery, version pinning, signature verification, capability declaration. | `openclaw/plugins/`; `agent-framework` integrations | All | Signature trust; capability gating | T1, T2, T8 | `plugin-sdk`, `secrets-manager` |
| 🔴 **`plugin-loader`** | Runtime activation, sandboxed import, capability gating. | `openclaw/.../plugins/activation-planner.ts`; `claude-code/.../services/plugins/` | All | Lazy loading; sandboxing JS/Py imports | T1, T3, T8 | `plugin-sdk`, `plugin-registry`, `sandbox` |
| 🟡 **`module-marketplace`** | Optional remote registry for sharing modules (tools/skills/channels/graders). | npm/PyPI; HuggingFace Hub; OpenAI tool registry | All | Federation; trust; metadata schema | T1, T2 | `plugin-registry`, `secrets-manager` |

---

# PART B — Composable Runtime Modules

These ship as **selectable building blocks** the factory wires into a generated harness based on the spec.

### Layer R1 — Runtime Core (the agent loop)

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`runtime-orchestrator`** | The main agent/run loop: state-machine-driven inner loop (NeedModel/NeedTools/NeedCompaction/NeedRecovery/Done) with pre-turn compaction (snip → autocompact at 0.85 of context). `runChatLoop` runs as a stdin REPL by default; `singleTurn: true` + `seedMessages` runs one turn from a pre-built history and returns the terminal assistant text (used by target-workflow). Reactive 413-recovery + max-tokens recovery still pending. | `claude-code/query.ts` (1730-line async-gen loop); `langgraph/.../pregel/_loop.py`; `agent-framework/.../_runner.py`; `openai-agents/.../runner.py` | All | Async-gen vs reactive vs callback; state-machine vs free-form | T1, T3, T4 | `model-adapter`, `turn-state-machine`, `recovery-engine`, `abort-controller`, `run-context`, `tool-executor` |
| 🟡 **`query-engine`** | SDK/headless wrapper over orchestrator (`submitMessage()` async-gen). | `claude-code/QueryEngine.ts` | All | SDK message normalization | T1, T3 | `runtime-orchestrator` |
| ✅ **`turn-state-machine`** | Transitions: need_model / need_tools / need_compaction / need_recovery / done. Pure FSM; exhaustive transition tests. | `claude-code/query.ts` `State`/`Continue` pattern | All | Transition diagram; invariant checks | T1, T9 | — |
| ✅ **`recovery-engine`** | Pure decision function: `recover(error, state) → RecoveryAction`. Taxonomy: `prompt_too_long → compact`, `max_output_tokens → continue`, `overloaded/5xx → retry` (exp backoff capped at 30 s + jitter), `invalid_request → tombstone`, `user-aborted/unknown → fail`. Per-turn budgets (max 5 retries, 1 compact, 3 continues, 1 tombstone). Integrated into `runtime-core` via try/catch around the model stream call. | `claude-code/query.ts` recovery branches; `agent-framework/.../_runner.py` retry | CLI, CHN, CRW, GRPH, MGD, RES | Recovery taxonomy; idempotence under retry | T1, T4 | `error-types` |
| 🟡 **`stream-runtime`** | Generator composition: provider stream → orchestrator → consumer. | `claude-code/.../services/tools/StreamingToolExecutor.ts`; `openai-agents/.../stream_events.py`; `langgraph/.../streaming` | All | Backpressure; multi-consumer fanout | T1, T3, T7 | `runtime-orchestrator`, `streaming-tool-executor` |
| ✅ **`abort-controller`** | Parent/child `AbortTree` with WeakRef-backed cascade (parent aborts cancel children; siblings/children stay independent). Listener limit raised to 50 per signal. Wired into `runtime-core`: each turn gets a child of the run signal; each tool exec gets a grandchild; first SIGINT aborts the active turn, second exits the process. | `claude-code/.../utils/abortController.ts` | All | AbortSignal threading; cleanup ordering | T1, T3 | — |
| 🟡 **`scheduler`** | Concurrency primitives: bounded parallelism, queue, priority lanes (UI vs background vs cron). | `openclaw/.../cron/isolated-agent` lane mgmt | All | Lane isolation; starvation prevention | T1, T7 | `run-context`, `abort-controller` |
| 🔴 **`durability-mode`** | Configurable durability: `exit` / `async` / `sync` checkpoint write. | `langgraph/.../checkpoint` durability modes | GRPH, MGD, CHN, RES | Async write semantics; loss-window analysis | T1, T4, T7 | `checkpoint-store`, `event-log`, `runtime-orchestrator` |
| ✅ **`run-context`** | Per-run context object threaded through orchestrator/tools/policy. `runId`, `sessionId`, `turnNumber`, `abortSignal`, `logger` — logger child-bound by default. Tool-executor threading is a follow-up. | `claude-code/.../Tool.ts` `ToolUseContext`; `openai-agents/.../run_context.py` | All | What goes in context; immutability vs mutation | T1, T3 | `logging`, `abort-controller` |

### Layer R2 — Model Layer

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`adapter-anthropic`** | Owns the shared `ProviderAdapter` interface + `ProviderRequest` / `StreamEvent` shapes (Anthropic-isomorphic for transcript wire-compat). Anthropic SDK implementation; preserves OAuth/Pro-Max routing + cache markers + thinking. `consumeStream` / `collectFinalMessage` helpers used by every consumer. Section 17. | `openai-agents/.../models/anthropic.py`; `agent-framework/.../anthropic/` | All | Stream-event shape; cache marker design | T1, T2 | — |
| ✅ **`adapter-openai`** | OpenAI Chat Completions implementation; canonical → OpenAI messages/tools, SSE stream → canonical `StreamEvent`s. `caching = "automatic"`, no thinking/web-search. Reused by the `local/<model>@<url>` router path against any OpenAI-compatible local server. Section 17. | `openai-agents/.../models/openai.py`; `agent-framework/.../openai/` | All | Tool-call shape divergence; finish-reason mapping | T1, T2 | `adapter-anthropic` |
| ✅ **`adapter-gemini`** | Google Gemini implementation via `@google/genai`. Maps `tool_use` ↔ `functionCall`, system → `systemInstruction`. `caching = "explicit"`, `thinking = true`. Section 17. | `agent-framework/.../gemini/`; `dspy/.../google/` | All | functionCall arg shape; finish-reason enum | T1, T2 | `adapter-anthropic` |
| ✅ **`adapter-bedrock`** | AWS Bedrock implementation (`@aws-sdk/client-bedrock-runtime`). Per-family marshalling (Anthropic / Llama / Mistral) under `src/families/`; family-specific `features`. Standard AWS credential chain. Section 17. | AWS SDK v3 docs | All | Family-specific bodies; EventStream chunk decoding | T1, T2 | `adapter-anthropic` |
| ✅ **`model-router`** | Parses `agent.model` and lazy-loads the matching adapter via `await import(...)`. Anthropic-only specs never load AWS / Gemini / OpenAI SDKs. Caches per `(providerId, baseUrl, family)`. Strict prefix grammar (`claude-*` / `openai/*` / `gemini/*` / `bedrock/*` / `local/<model>@<url>`); malformed input throws `ConfigError`. Cost/quality/latency policy + alias resolution still ahead. Section 17. | `openclaw/.../agents/anthropic-vertex-stream.ts`; `crewAI/.../llm.py` | All | Lazy-loading invariant; future policy DSL | T1, T7 | `adapter-anthropic` |
| ✅ **`token-budget`** | `estimateTokens()` (char/4 heuristic) + `TokenBudget` class with `add()` and `isApproachingLimit(threshold = 0.85)`. Handles tool_use/tool_result content shapes; image/document blocks ignored. Auto-continue thresholds + diminishing-returns logic deferred. | `claude-code/.../query/tokenBudget.ts` (+500k auto-continue); `claude-code/.../utils/tokenEstimation.ts` | All | Tokenizer parity across providers | T1, T9 | — |
| 🟡 **`prompt-cache-manager`** | Prompt-cache stability: contiguous tool prefixes, cache-friendly system prompts, cache breakpoints. | `claude-code/tools.ts` (built-ins prefix); Anthropic prompt-caching docs | All | Provider cache semantics; breakpoint placement | T1, T4 | `model-adapter`, `system-prompt-builder` |
| 🟡 **`response-format-coercion`** | Force structured/JSON output; schema validation; retry-on-malformed. | `dspy/.../predict/`; `openai-agents/.../agent_output.py`; `openclaw/.../agents/anthropic-payload-policy.ts` | All | Provider-native vs prompted JSON; coercion ladders | T1, T2, T9 | `model-adapter`, `recovery-engine` |
| 🟡 **`reasoning-controller`** | Thinking modes / extended thinking, effort levels, reasoning visibility. | `claude-code/.../state/AppStateStore.ts` (`thinkingConfig`,`effortValue`); `openclaw/auto-reply/` | CLI, CHN, CRW, GRPH, RES | When to expose chain-of-thought; cost vs quality | T1, T5 | `model-adapter` |
| 🟡 **`speculation-engine`** | Speculative decoding / pre-fetch of likely-next-tool. | `claude-code/.../state/AppStateStore.ts` `speculation` | CLI, RES | Speculation hit-rate; rollback cost | T1, T7 | `model-adapter`, `runtime-orchestrator` |
| 🟡 **`cost-tracker`** | Estimate $/run, attribute to tenant/agent/tool. | `claude-code/.../cost-tracker.ts`; `ragas/.../cost.py`; `AI-Harness-Systems.md` §cost | All | Provider price tables; granular attribution | T1, T7 | `model-adapter`, `trace-event-bus` |
| 🟡 **`auth-profiles`** | Multi-provider creds (API keys, OAuth, IAM roles); profile switching; rotation. | `openclaw/.../agents/auth-profiles/`; `claude-code/.../utils/auth.ts`; `claude-code/.../services/oauth/` | All | Secure storage; OAuth flow framework | T1, T2, T8 | `secrets-manager` |
| 🟡 **`embedding-adapter`** | Embedding model adapter (separate from chat models). | `haystack/.../embedders/`; `llama_index/.../embeddings/`; `dspy/.../embeddings/` | RAG, RES, CHN, MGD | Pooling; batch sizing; quantization | T1, T2 | `model-adapter` |

### Layer R3 — Tool Layer (core)

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`tool-catalog`** | Registry of tools w/ metadata (concurrency-safe, read-only, destructive, defer, profile). | `claude-code/Tool.ts` + `tools.ts`; `openclaw/.../agents/tool-catalog.ts`; `openai-agents/.../tool.py` | CLI, CHN, CRW, MGD, GRPH, RES, BROW, BATCH | Metadata schema; profile membership | T1 | — |
| ✅ **`tool-builder`** | Factory `buildTool()` w/ fail-closed defaults. | `claude-code/.../Tool.ts` `buildTool` factory | All | Default safety posture | T1, T9 | `tool-catalog` |
| ✅ **`tool-orchestrator`** | Partition tool calls into concurrent/serial batches. | `claude-code/.../services/tools/toolOrchestration.ts` (`partitionToolCalls`); `agent-framework/.../_executor.py` | CLI, CHN, CRW, MGD, GRPH, RES | Concurrency-safety classification | T1, T3, T7 | `tool-catalog` |
| ✅ **`tool-executor`** | Execute single tool: validate → permission → invoke → format result. | `claude-code/.../services/tools/toolExecution.ts`; `openai-agents/.../agent.py` tool exec | CLI, CHN, CRW, MGD, GRPH, RES, BROW | Error normalization; timeout handling | T1, T3 | `tool-catalog`, `tool-validate`, `permission-engine` |
| ✅ **`streaming-tool-executor`** | Execute tools while model still streaming. | `claude-code/.../services/tools/StreamingToolExecutor.ts` | CLI, CHN, RES | Partial-args parsing; sibling abort | T1, T3, T7 | `tool-executor`, `abort-controller` |
| ✅ **`tool-result-store`** | Persist large tool outputs to disk; preview to model. | `claude-code/.../utils/toolResultStorage.ts` | CLI, CHN, RES | Preview shape; addressing scheme | T1, T3 | `infra-utils` |
| ✅ **`tool-loop-detection`** | Detect repeated tool calls; prevent runaway. | `openclaw/.../agents/tool-loop-detection.ts` | CLI, CHN, CRW, GRPH, RES | Hash window; false-positive rate | T1, T9 | — |
| 🟡 **`tool-search`** | Deferred tool loading by keyword (saves tokens). | `claude-code/.../tools/ToolSearchTool/`; `Tool.ts` `shouldDefer` | CLI, CHN, RES | Index strategy; recall quality | T1, T5 | `tool-catalog`, `embedding-adapter` |
| 🟡 **`tool-display`** | Render tool calls/results in TUI/UI (React/Ink). | `openclaw/.../agents/tool-display.ts`; `claude-code/Tool.ts` `renderToolUseMessage` | CLI, CHN | Render perf; long-output truncation | T1, T3 | `tool-catalog`, `tui-runtime` |
| ✅ **`tool-permission-matcher`** | Pattern matchers for permission rules (`Bash(git *)`). | `claude-code/.../utils/permissions/preparePermissionMatcher` | All | Glob vs regex; pattern composition | T1, T9 | — |
| ✅ **`tool-validate`** | Pre-permission input validation per tool (Zod/Pydantic). | `claude-code/.../Tool.ts` `validateInput`; `openai-agents/.../tool.py` schema | All | Schema-validation error mapping | T1, T2 | `tool-catalog`, `error-types` |

### Layer R4 — Built-in Tool Implementations

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`tool-fs`** | `read`/`write`/`edit`/`apply_patch`/glob/grep over workspace; FS policy enforcement. Slice ships `Read`/`Write`/`Edit`/`Glob`/`Grep` sandboxed to `process.cwd()`; atomic writes via temp + rename; `ToolPermissionError` on traversal. `apply_patch` and policy hooks pending. PR #8. | `claude-code/.../tools/FileReadTool` etc.; `openclaw/.../agents/bash-tools.*`; `openclaw/.../agents/tool-fs-policy.ts`; `openai-agents/.../sandbox/files.py` | CLI, CHN, CRW, RES, BATCH | Atomic edit; path traversal defense | T1, T3, T8 | `tool-builder`, `tool-catalog` |
| ✅ **`tool-bash`** | Shell exec, background process mgmt, kill-tree. Slice ships foreground exec via `Bun.spawn` with default 30s timeout (max 10min). Background process / kill-tree pending. PR #8. | `claude-code/.../tools/BashTool`; `openclaw/.../process/exec.ts`,`kill-tree.ts` | CLI, CHN, BATCH, RES | Sandboxing; timeout; output buffering | T1, T3, T8 | `tool-builder`, `abort-controller` |
| 🟡 **`tool-process`** | Long-running background processes; monitoring; log capture. | `openclaw/.../agents/bash-tools.process.ts`; `claude-code/.../utils/Shell.ts` | CLI, CHN, BATCH | PID tracking; reap policy | T1, T3, T7 | `tool-bash`, `scheduler` |
| ✅ **`tool-code-execution`** | Section 18 — `Python` (`python:3.13-slim`), `JavaScript` (`node:22-alpine`), `Shell` (`alpine:3.19`). All built via `buildTool()` with `requiresSandbox: true` + `destructive: true`; runtime forwards `onStreamChunk` so stdout/stderr publish as `tool_stream_chunk` trace events. Config registered via `registerCodeExecutionConfig({ sandbox?, backend?, allowedImages?, mountWhitelist?, mounts?, images? })`; `target-cli` BUILTIN_TOOL_MAP wires the three tools to a single shared `registerCodeExecutionConfig` init. Warm pool deferred to v1; cold-start ≈700ms on python:3.13-slim. PR feat/section-18-safety-floor. | `claude-code/.../tools/REPLTool`; OpenAI Code Interpreter; Foundry CI; `adk-python/.../code_executors/` | CLI, MGD, EVAL, RES, BATCH | Container vs micro-VM; warm pool | T1, T3, T7, T8 | `tool-builder`, `sandbox` |
| ✅ **`tool-web-search`** | Brave/Bing/Google/X/Tavily providers. Slice ships `WebSearch(query, allowed_domains?, blocked_domains?)` inside `@crewhaus/tool-web` with provider dispatch via env (`CREWHAUS_SEARCH_PROVIDER` + `CREWHAUS_SEARCH_API_KEY`); Brave + Tavily today, normalized to `{title, url, snippet}[]`. Anthropic server-side `web_search` path deferred to Section 17 (needs `model.features.web_search`). Section 14. | `openclaw/web-search/`; `claude-code/.../tools/WebSearchTool`; `openai-agents/.../extensions/` | CLI, CHN, CRW, RAG, RES | Provider fallback; result normalization | T1, T2 | `tool-builder`, `secrets-manager` |
| ✅ **`tool-web-fetch`** | URL fetch, content extraction, link understanding. Slice ships `WebFetch(url, prompt?)` inside `@crewhaus/tool-web`: cheerio + turndown HTML→markdown, 5 MB body cap, ≤5 redirects, 30 s timeout, optional `tools.WebFetch.allowed_domains`. The optional `prompt` is prepended to the body for the parent agent's next assistant turn — recursive model calls inside a tool would fight the runtime loop. Section 14. | `openclaw/.../web-fetch/`; `openclaw/.../link-understanding/`; `claude-code/.../tools/WebFetchTool` | CLI, CHN, CRW, RAG, RES | Robots.txt; sniff; redirect cycles | T1, T2, T8 | `tool-builder` |
| 🔴 **`tool-browser`** | Stateful browser automation (Playwright/Chromium). | `claude-code/.../tools/WebBrowserTool`; `openclaw/.../canvas-host/`; OpenAI Agents browser | CLI, CHN, CRW, RES, BROW | Session pooling; isolation | T1, T3, T7, T8 | `tool-builder`, `sandbox`, `computer-use-driver` |
| ✅ **`tool-mcp`** | Wrapper presenting external MCP tools as native tools. Slice ships `registerMcpServer(host, serverName, catalog, opts?)` building one `RegisteredTool` per remote tool with namespaced name `<serverName>__<toolName>`; per-tool flag overrides via `opts.perTool`. JSON Schema forwarded verbatim via `RegisteredTool.jsonSchema` (runtime-core honours it over `zodToJsonSchema`). Section 9. | `claude-code/.../services/mcp/`; `crewAI/.../tools/mcp_native_tool.py`; `openclaw/mcp/` | CLI, CHN, MGD, RES, BROW | Stdio vs SSE; auth handoff | T1, T2, T3 | `mcp-host`, `tool-builder`, `tool-catalog` |
| 🟡 **`tool-image-generation`** | Image gen (DALL-E/Flux). | `openclaw/.../image-generation/`; `openclaw/.../agents/tool-images.ts` | CHN, CRW, RES | Provider abstraction; safety filters | T1, T2 | `tool-builder`, `model-adapter` |
| ✅ **`tool-image`** | Read image inputs from disk and present them to the model as Anthropic content blocks. Slice ships `ReadImage(path)` (`@crewhaus/tool-image`): path resolves under `process.cwd()`, magic-byte sniff (PNG/JPEG/GIF/WebP) rejects extension spoofing, 5 MB per-image cap. Returns a base64 image content block via the widened `RegisteredTool.execute` return type (Section 14 cross-cut: `Promise<string \| ToolResultContent>`). 20-images-per-turn cap deferred — needs `RunContext.turnMetrics`. Section 14. | `claude-code/.../tools/ReadTool` image branch | CLI, CHN, CRW | Magic-byte sniff; base64 size; per-turn limit | T1, T8 | `tool-builder` |
| ✅ **`tool-fetch`** | Generic HTTP escape hatch for API integrations. Slice ships `Fetch(url, method?, body?, headers?)` (`@crewhaus/tool-fetch`). Fail-closed `tools.Fetch.allowed_origins` (empty = deny all); SSRF default-deny on loopback / link-local / RFC1918 / mDNS / AWS metadata even when allow-listed; DNS lookup catches rebinding. ≤5 redirects re-checked per hop, 5 MB body cap, 30 s timeout. `Cookie` / `Set-Cookie` / `Authorization` stripped from responses. Section 14. | `openclaw/.../agents/http-tool.ts` | CLI, CHN, CRW, MGD | Allow-list canonicalisation; SSRF; credential strip | T1, T8 | `tool-builder` |
| 🔴 **`tool-tts-stt`** | TTS + speech-to-text tools. | `openclaw/tts/`; `openai-agents/.../voice/`; `openclaw/.../realtime-voice/`,`realtime-transcription/` | CHN, VOICE | Codec choice; streaming | T1, T2, T7 | `tool-builder`, `voice-runtime`, `audio-stream` |
| ✅ **`tool-todo`** | Todo-list tool for plan tracking. Module-level per-process list; `TodoWrite` overwrites the list and renders a markdown checklist with status/priority. PR #8. | `claude-code/.../tools/TodoWriteTool` | CLI, CRW, RES | Diff-friendly output | T1 | `tool-builder` |
| **`tool-skill`** | Read SKILL.md → context. | `claude-code/.../tools/SkillTool`; `openclaw/.../agents/skills/serialize.ts` | CLI, CHN, RES | Frontmatter schema; conflict resolution | T1, T3 | `tool-builder`, `skills-registry` |
| 🟡 **`tool-ask-user`** | Interactive question prompting. | `claude-code/.../tools/AskUserQuestionTool` | CLI, CHN, CRW, VOICE | Multi-select UX; preview rendering | T1, T3 | `tool-builder`, `hitl-engine` |
| 🟡 **`tool-cron`** | Cron-job CRUD tool. | `claude-code/.../tools/ScheduleCronTool`; `openclaw/cron/` | CLI, CHN | Cron-expression parsing | T1, T3 | `tool-builder`, `scheduler-cron` |
| ✅ **`tool-task`** | `Task(description, prompt, subagent_type?)` — spawns a sub-agent in an isolated context and returns the child's final assistant text. Resolves `subagent_type` in priority order: spec-inline `sub_agents` map → `<cwd>/.crewhaus/sub-agents/<name>.md` (SKILL.md-style frontmatter) → built-in `general-purpose`. Filters the parent catalog to the child's allowed tool names; resolves child permissions via `sub-agent-permission-inheritance` (bypass does NOT propagate unless `def.inherit_bypass === true`). `concurrencySafe: true` so multiple sub-agents fan out via the orchestrator's `Promise.all` batch. Section 13. | `claude-code/.../tools/Task*Tool/`; `openclaw/tasks/` | CLI, CHN, CRW, RES | Sub-agent definition resolution; child catalog filtering | T1, T3 | `tool-builder`, `sub-agent-spawner`, `sub-agent-permission-inheritance` |
| 🟡 **`tool-team`** | Team/teammate management. | `claude-code/.../tools/TeamCreateTool`,`TeamDeleteTool` | CLI, CRW | Team invite flow | T1, T3 | `tool-builder`, `role-system` |
| 🟡 **`tool-agent`** | Subagent spawn (`AgentTool`). | `claude-code/.../tools/AgentTool/`; `adk-python/.../agents/`; `openai-agents/.../_agent.py` | CLI, CRW, MGD, RES | Context filtering; tool restriction | T1, T3, T6 | `tool-builder`, `sub-agent-spawner` |
| ✅ **`tool-message-channel`** | `SendMessage(channel, text)` — opt-in built-in tool. `destructive: true` so the permission engine fails closed (asks in default mode, denies in plan mode); requires explicit `alwaysAllow SendMessage` rule to invoke. Routing key `<adapterId>:<workspaceId>:<channelId>[:<threadTs>]`; dispatches to the adapter registered at boot. Section 12. | `openclaw/.../agents/channel-tools.ts`; `claude-code/.../tools/SendMessageTool` | CHN | Cross-channel addressing | T1, T2, T8 | `tool-builder`, `channel-adapter-base`, `permission-engine` |
| 🟡 **`tool-memory`** | Memory get/search tool. | `crewAI/.../memory/`; `openai-agents/.../memory/`; `agent-framework/.../mem0/`; `openclaw/.../agents/memory/` | CHN, CRW, GRPH, RES | Recall ranking | T1, T5 | `tool-builder`, `memory-service` |
| **`tool-plan-mode`** | Plan-mode entry/exit (read-only planning). | `claude-code/.../tools/EnterPlanModeTool`,`ExitPlanModeV2Tool` | CLI, RES | Plan file persistence | T1, T3 | `tool-builder`, `autoplan-engine` |
| **`tool-worktree`** | Git worktree management. | `claude-code/.../tools/EnterWorktreeTool`,`ExitWorktreeTool` | CLI | Cleanup safety | T1, T3 | `tool-builder`, `tool-bash` |
| 🟡 **`tool-lsp`** | Language-server queries (defs, refs, diagnostics). | `claude-code/.../tools/LSPTool/`; `claude-code/.../services/lsp/` | CLI | LSP capability discovery | T1, T2, T3 | `tool-builder`, `lsp-host` |
| 🟡 **`tool-monitor`** | Stream lines from long-running process as notifications. | `claude-code/.../tools/MonitorTool` | CLI, CHN, BATCH | Backpressure | T1, T3, T7 | `tool-builder`, `tool-process` |
| **`tool-sleep`** | Schedule a wakeup. | `claude-code/.../tools/SleepTool` | CLI, CHN, RES | Drift handling | T1 | `tool-builder`, `scheduler` |
| **`tool-notebook-edit`** | Jupyter notebook edits. | `claude-code/.../tools/NotebookEditTool` | CLI, RAG | nbformat compatibility | T1, T3 | `tool-builder`, `tool-fs` |
| 🟡 **`tool-notification`** | Push notification (slack/email/webhook to user). | `claude-code/.../tools/PushNotification`; `openclaw/.../agents/announce-idempotency.ts` | CHN, RES, BATCH | Dedup; quiet hours | T1, T3 | `tool-builder`, `notifications-service`, `idempotency-store` |
| 🟡 **`tool-remote-trigger`** | Trigger external URL/webhook with payload. | `claude-code/.../tools/RemoteTriggerTool` | CHN, CRW, BATCH | Auth; idempotency keys | T1, T2, T8 | `tool-builder`, `tool-fetch`, `idempotency-store` |
| 🟡 **`tool-canvas`** | UI-canvas-as-tool (rich-content surface). | `openclaw/canvas-host/` | CHN | Canvas widget vocabulary | T1, T3 | `tool-builder`, `canvas-host` |
| 🟡 **`tool-citations`** | Manage citation/source-attribution metadata in tool calls. | `agent-framework/.../citations/`; `openclaw/.../citations/` (if present); `claude-code/.../citations/` | RAG, RES, CHN | Span vs doc-level; cross-source merging | T1, T5 | `tool-builder`, `tool-result-store` |
| 🔴 **`tool-screen-capture`** | Take screenshots / screen video. | `mcp__computer-use__screenshot`; `claude-code/.../tools/screenshot` | BROW, CLI | Frame rate; diff detection | T1, T3 | `tool-builder`, `computer-use-driver` |
| 🔴 **`tool-mouse-keyboard`** | Pixel-level mouse/keyboard input. | `mcp__computer-use__left_click` etc.; CC keyboard tools | BROW | Coordinate normalization; key chord encoding | T1, T3 | `tool-builder`, `computer-use-driver` |
| 🔴 **`tool-vision-grounding`** | Find on-screen elements by description (vision model + heuristics). | `mcp__Claude_in_Chrome__find`; OpenAI Operator-style | BROW, CLI | Element-locator robustness | T1, T5, T6 | `tool-builder`, `tool-screen-capture`, `model-adapter` |
| 🟡 **`tool-dom-inspector`** | DOM/AX-tree-based element queries (when in browser). | `mcp__Claude_in_Chrome__read_page`,`get_page_text` | BROW | DOM snapshot diffing | T1, T2, T3 | `tool-builder`, `browser-extension-bridge` |

### Layer R5 — MCP & Protocol Hosts

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`mcp-host`** | MCP client manager: stdio + SSE transports, reconnect, server registry. Slice ships `McpHost.addServer()` / `getClient()` / `disconnectAll()` and `McpClient` over `@modelcontextprotocol/sdk` with exp-backoff reconnect (1–30 s, ±10% jitter, no max attempts), proactive `onclose` detection, queue-capped (16) `connectedDeferred` for in-flight calls, and reduced text/image/audio/resource result blocks. T2 contract test gated behind `CREWHAUS_RUN_MCP_CONTRACT=1`. WebSocket / OAuth pending. Section 9. | `claude-code/.../services/mcp/`; `openclaw/mcp/`; `agent-framework/.../_mcp.py`; `openai-agents/.../mcp/`; `adk-python/.../tools/` MCP support | All except pure RAG/EVAL | Transport handshake; reconnection backoff | T1, T2, T7, T8 | `error-types`, `infra-utils` |
| 🟡 **`mcp-server-host`** | Expose harness's tools as an MCP server. | `openclaw/.../canvas-host/server.ts`; LangSmith MCP exposure | CHN, MGD | Surface filtering; auth | T1, T2, T8 | `tool-catalog`, `mcp-host`, `gateway-server` |
| ✅ **`a2a-protocol`** | In-crew agent-to-agent peer messaging. `createSendMessageA2ATool({from, targets})` builds a `SendMessage(target, payload, kind?)` tool that routes through the orchestrator's mailbox; `buildEnvelope({from, to, kind, payload, traceparent})` produces the wire shape `{from, to, kind, payload, traceparent}`. The traceparent is stamped from `bus.currentTraceparent()` so every A2A message lands under the crew's single trace id. Per-call recursion depth-limited (default 3). DISTINCT from §12's channel-bot `SendMessage`. Section 22. | `agent-framework/.../a2a/`; `crewAI/.../a2a/`; `adk-python/.../a2a/` | CRW, (future MGD) | A2A message envelope; traceparent propagation | T1, T2, T9 | `agent-context-isolation`, `tool-builder`, `tool-catalog`, `trace-event-bus` |
| 🟡 **`acp-protocol`** | Agent Control Plane protocol (session policy, approvals, spawning). | `openclaw/acp/` | CHN, MGD | Control vs data plane split | T1, T2 | `permission-engine`, `hooks-engine`, `gateway-server` |
| 🟡 **`ag-ui-protocol`** | Agent-UI protocol streaming UI events (Codex App Server-style JSON-RPC). | `agent-framework/.../ag_ui/`; OpenAI Codex App Server JSON-RPC; `openclaw/.../canvas-host/a2ui*` | CLI, CHN, MGD | Bidirectional event vocabulary | T1, T2, T3 | `trace-event-bus`, `gateway-server` |
| 🟡 **`webhook-host`** | Inbound webhook receiver (for tools, channels, integrations). | `openclaw/.../gateway/server-webhooks.ts`; CC custom hooks | CHN, BATCH, MGD | Verification; replay defense | T1, T2, T8 | `secrets-manager`, `gateway-server`, `idempotency-store` |

### Layer R6 — Context & Memory

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🟡 **`context-engine`** | Pluggable context-management interface (bootstrap/maintain/ingest/assemble/compact). | `openclaw/context-engine/`; `claude-code/context.ts` | CLI, CHN, CRW, GRPH, RES | Interface stability; named impls | T1, T3 | `ir-model`, `runtime-orchestrator`, `token-budget` |
| ✅ **`compaction-snip`** | Pure middle-message removal with `[Context compacted: N messages removed]` marker. Boundary defense walks `headEnd`/`tailStart` until tool_use/tool_result pairs are intact. | `claude-code/.../services/compact/snipCompact.ts` | CLI, CHN, RES | Snip thresholds | T1, T9 | `token-budget` |
| 🟡 **`compaction-microcompact`** | Cached per-tool-use-id compaction. | `claude-code/.../services/compact/microCompact.ts`,`apiMicrocompact.ts` | CLI, CHN, RES | Cache key design | T1, T4 | `token-budget`, `model-adapter`, `tool-result-store` |
| 🟡 **`compaction-context-collapse`** | Read-time projection over full history. | `claude-code/.../services/contextCollapse/` | CLI, CHN, GRPH, RES | Projection schema | T1, T4 | `token-budget`, `model-adapter` |
| ✅ **`compaction-autocompact`** | Calls the model to summarize the conversation; returns `[user-marker, assistant-summary]` pair. Circuit breaker, session-memory promotion, cache-safe params deferred. | `claude-code/.../services/compact/autoCompact.ts` | CLI, CHN, CRW, GRPH, MGD, RES | Summary fidelity vs cost | T1, T5 | `model-adapter`, `token-budget` |
| 🟡 **`compaction-reactive`** | Emergency compaction on 413 / prompt-too-long. | `claude-code/query.ts` reactive-compact branch | CLI, CHN, CRW, GRPH, MGD, RES | Recovery loop bound | T1, T4 | `recovery-engine`, `compaction-autocompact` |
| 🟡 **`compaction-tool-result-budget`** | Per-message aggregate-tool-result budget; persist overflow. | `claude-code/.../utils/toolResultStorage.ts` | CLI, CHN, RES | Budget allocation strategy | T1, T9 | `tool-result-store`, `token-budget` |
| 🟡 **`compaction-session-memory`** | Promote insights to session memory at compaction. | `claude-code/.../services/compact/sessionMemoryCompact.ts` | CLI, CHN | Promotion criteria | T1, T5 | `compaction-autocompact`, `memory-service` |
| 🟡 **`bootstrap-files`** | Workspace-file injection (CLAUDE.md/AGENTS.md/TOOLS.md/USER.md/SOUL.md/HEARTBEAT.md), budget, caching. | `openclaw/.../agents/bootstrap-files.ts`,`bootstrap-cache.ts`,`bootstrap-budget.ts`; `claude-code/context.ts` | CLI, CHN, RES | Walk semantics; budget allocation | T1, T3, T9 | `context-engine`, `token-budget`, `tool-fs` |
| 🟡 **`system-prompt-builder`** | Composable sections (identity/tooling/safety/skills/messaging/runtime/extras), token-budget-aware. | `openclaw/.../agents/system-prompt.ts` (anti-pattern: 720-line god fn); `claude-code/context.ts` | All | Section composition; per-section budget | T1, T9 | `context-engine`, `token-budget` |
| 🔴 **`memory-service`** | Long-term memory (key-value, vector, episodic, declarative). | `crewAI/.../memory/`; `openai-agents/.../memory/`; `agent-framework/.../mem0/`; `adk-python/.../memory/`; `openclaw/.../agents/memory/`; `claude-code/memdir/` | CHN, CRW, GRPH, RES | Recall vs precision; namespace model | T1, T3, T5 | `vector-store`, `embedding-adapter`, `session-store` |
| 🟡 **`memory-extraction`** | Auto-extract memories from conversations. | `claude-code/.../services/extractMemories/` | CHN, CLI, RES | Extraction quality; PII | T1, T5, T8 | `memory-service`, `model-adapter`, `pii-redactor` |
| 🟡 **`personalization-store`** | User preference + identity store. | `openclaw/secrets/`; `openclaw/.../agents/auth-profiles/` | CHN | Schema; consent | T1, T8 | `secrets-manager`, `session-store` |
| ✅ **`vector-store`** | Section 21 — `createVectorStore({ backend })`. v0 ships `in-memory` (flat L2 over Map, exact-match metadata filter, T8 SQL-injection-shape filter guard). `lance` / `qdrant` / `pinecone` / `weaviate` constructors throw a clear "not implemented in v0" error. PR feat/section-21-rag-target. | `haystack/.../document_stores/`; `llama_index/.../vector_stores/` | RAG, CHN, CRW, RES | Backend abstraction; metadata filtering | T1, T2, T7 | `embedding-adapter`, `secrets-manager` |

### Layer R7 — State, Sessions, Persistence

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`session-store`** | Short-term session state, identity, lifecycle. v0 file-backed JSON at `.crewhaus/sessions/<id>.json`; id format `sess_<16 hex>`; atomic writes via tmp + rename; `list()` evicts files whose **mtime** is older than `ttlDays` (default 30) days, unlinking the `.json` AND the sibling `.jsonl` event log. Path-traversal guard on every read path. Section 10. | `openclaw/sessions/`; `claude-code/.../utils/sessionStorage.ts`; `agent-framework/.../_sessions.py`; `adk-python/.../sessions/`; `openai-agents/.../memory/session.py` | All | TTL; eviction; identity model | T1, T3, T7 | `infra-utils`, `error-types` |
| ✅ **`event-log`** | Append-only event/transcript history (JSONL). v0 ships `openEventLog(sessionId, opts)` → `{ append, read, close }`; events `{ ts, version: 1, kind, payload }` with kind ∈ `user_message | assistant_message | tool_use | tool_result | error | compaction`. Append uses `appendFileSync` mode `0o600`; read streams via `node:readline` with optional `since`/`until` filtering; malformed lines throw `RuntimeError` carrying the line number. Replay (`replayMessageHistory` in runtime-core) walks `user_message`+`assistant_message` events into a `MessageParam[]`. Section 10. | `claude-code/.../utils/sessionStorage.ts`; `AI-Harness-Systems.md` §append-only event history; OpenClaw transcripts | All | Schema versioning; GC | T1, T4, T7 | `session-store`, `infra-utils` |
| ✅ **`checkpoint-store`** | Section 19 — `createCheckpointStore({ rootDir?, adapter? })`. Default adapter is file-backed under `.crewhaus/graphs/<grun_…>/<ckpt_…>.json` plus a `_meta.json` head pointer. `save({ graphRunId, nodeName, state, parentCheckpointId? })`, `load(grun, ckpt?)` (head when omitted), `list(grun, { limit?, since? })` in mtime order, `branch(parentRun, ckpt)` mints a fresh `grun_…` whose head copies the source checkpoint and whose `_meta.json` records `branchedFrom`. Path-traversal guard rejects ids that don't match `grun_<16hex>` / `ckpt_<16hex>`. Pluggable `CheckpointStoreAdapter` interface for SQLite/Postgres/S3. PR feat/section-19-grph-target. | `langgraph/.../checkpoint/`; `agent-framework/.../_checkpoint.py`,`_checkpoint_encoding.py`; ADK sessions | GRPH, MGD, CHN, RES | Encoding; partial save | T1, T4, T7 | `event-log`, `checkpoint-encoder`, `state-store` |
| ✅ **`state-store`** | In-process state container (zustand-style); referential equality, listeners. v0 ships `createStore<T>(initial)` → `{ get, set, subscribe, select }`; root listeners fire on every actual change, `select(selector)` listeners fire only when `Object.is(selector(next), selector(prev)) === false`. ~50 lines, no dependencies. Per-`runChatLoop` instance; consumed by hooks/skills in Section 11+. Section 10. | `claude-code/.../state/store.ts` (40 lines) | All | Observers; immutability | T1, T9 | — |
| 🟡 **`app-state-store`** | Monolithic typed `AppState` for running harness. | `claude-code/.../state/AppStateStore.ts` | CLI, CHN | State tree shape | T1, T3 | `state-store` |
| **`global-state-singleton`** | Process-global registry (`Symbol.for(...)`). | `openclaw/global-state.ts`; `claude-code/.../bootstrap/state.ts` | CLI, CHN | Test isolation | T1, T3 | — |
| ✅ **`branch-history`** | Section 19 — `branchAt(store, parentRun, ckpt)` (thin wrapper over `store.branch`) and `diff(store, runA, runB)` returning `NodeDiff[]` with `same | node-mismatch | state-mismatch | only-a | only-b` kinds. State equality compares a SHA-256 truncation of `JSON.stringify(state)` so non-trivial state types diff cleanly. PR feat/section-19-grph-target. | `langgraph` time-travel; `agent-framework/.../_checkpoint.py`; `claude-code/buddy/` | GRPH, MGD, RES | Branch identity; merge semantics | T1, T4 | `checkpoint-store`, `event-log` |
| 🟡 **`artifact-store`** | Persist files, screenshots, patches, generated docs, reports. | OpenAI artifact APIs; `adk-python/.../artifacts/`; `openclaw/blob-store.ts` | All except RAG-only | Content addressing; GC | T1, T3 | `infra-utils`, `secrets-manager` |
| 🟡 **`session-router`** | Resolve session keys (`agent:<id>:main`/`:subagent:<uuid>`/`:cron:<jobId>`). | `openclaw/routing/`; `openclaw/.../sessions/session-id.ts` | CHN, CLI | Key grammar; collision | T1, T9 | `session-store`, `channel-binding` |
| 🟡 **`session-binding`** | Bind session ↔ channel ↔ thread ↔ user. | `openclaw/bindings/` | CHN | Multi-tenant | T1, T3 | `session-store`, `channel-registry` |
| 🟡 **`replay-store`** | Recorded runs for replay tests + regression. | `openai-agents/tracing/`; `dspy/cache.py` | EVAL, all CI | Determinism guarantees | T1, T4 | `event-log`, `trace-recorder` |

### Layer R8 — Permission, Policy, Safety

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`permission-engine`** | Modes (`default`/`plan`/`auto`/`bypass`); 5-source `RuleSet` walked in priority order `flag → settings → yaml → hooks → builtin`; rule types `alwaysAllow`/`alwaysDeny`/`alwaysAsk`. Built on `tool-permission-matcher` patterns. SECURITY: `mode: bypass` is rejected at parse time from yaml/settings sources (only the `--permission-mode` flag may select it). Integrated into `runtime-core` so each tool call evaluates before exec; in REPL mode `ask` prompts via stdin. | `claude-code/.../utils/permissions/` (14 files); `AI-Harness-Systems.md` §Policy engine | CLI, CHN, CRW, MGD, BATCH | Layered evaluation; precedence | T1, T3, T8 | `tool-permission-matcher`, `error-types` |
| 🟡 **`approval-classifier`** | ML auto-approve for safe ops (yolo classifier). | `claude-code/.../utils/permissions/yoloClassifier.ts`; `openclaw/.../acp/approval-classifier.ts` | CLI, CHN | Calibration; abstain rate | T1, T5, T8 | `permission-engine`, `model-adapter` |
| **`denial-tracking`** | Count consecutive denials; fall back to ask. | `claude-code/.../utils/permissions/denialTracking.ts`; `claude-code/.../utils/autoModeDenials.ts` | CLI, CHN | Threshold tuning | T1, T9 | `permission-engine` |
| 🟡 **`guardrails`** | Input/output guardrails (PII, jailbreak, secret leak, NSFW). | `openai-agents/.../guardrail.py`,`tool_guardrails.py`; `AI-Harness-Systems.md` §guardrails | CHN, CRW, MGD, RES, VOICE | Cost/latency; false positive | T1, T3, T8 | `model-adapter`, `permission-engine`, `pii-redactor` |
| ✅ **`policy-engine`** | Section 20 — `evaluatePolicy(call, { mode?, rules?, tenantPolicy? })` over five sideEffect classes (none/filesystem/network/external/messaging). Tenant rules win over global rules; `permissive | audit | strict` mode demotes/upgrades rule actions. `auditPolicyDecision(log, call, result)` writes the structured row. Composes with `permission-engine` (permission first, policy second). PR feat/section-20-mgd-target. | `AI-Harness-Systems.md` §PolicyEngine; `openclaw/.../security/exec-policy.ts`; `openclaw/.../agents/auth-mode-policy.ts` | CLI, CHN, MGD, BATCH | Policy DSL; compile vs runtime | T1, T3, T8 | `permission-engine`, `audit-log`, `tool-permission-matcher` |
| 🟡 **`secrets-manager`** | Read/decrypt secrets (env / keychain / vault / cloud KMS). | `openclaw/secrets/`; `claude-code/.../utils/auth.ts`; `agent-framework/.../_settings.py` | All | Backend abstraction; rotation | T1, T2, T8 | `infra-utils`, `error-types` |
| ✅ **`sandbox`** | Section 18 — `createSandbox({ backend?, allowedImages?, mountWhitelist?, defaultTimeoutMs? })`. Backends: `docker` (default), `podman`, `noop` (test-only, opt-in via `CREWHAUS_SANDBOX=noop`). Defaults: `--network none`, `--memory 512m`, `--cpus 1.0`, `--read-only`, `--tmpfs /tmp:rw,size=64m,mode=1777,exec`, `--security-opt no-new-privileges`, 60 s timeout. Image allowlist (`CREWHAUS_SANDBOX_ALLOWED_IMAGES` extends defaults: `python:3.13-slim`, `node:22-alpine`, `alpine:3.19`); CLI-flag-injection guard rejects images starting with `-` or containing whitespace; mount whitelist rejects paths outside the configured root + `..` traversal + newline tampering; env keys validated against `[A-Za-z_][A-Za-z0-9_]*`. Streaming via `onStdoutChunk`/`onStderrChunk` callbacks. Permission floor in `permission-engine.evaluateWithReason()` denies any `requiresSandbox: true` tool unless an `alwaysAllow` rule matches AND `sandboxAvailable: true`. PR feat/section-18-safety-floor. | `openai-agents/.../sandbox/`; `openclaw/.../security/exec-policy.ts`; OpenAI Codex sandboxes | CLI, CHN, MGD, EVAL, BATCH | VM startup latency; warm pool | T1, T3, T7, T8 | `infra-utils`, `policy-engine` |
| **`safety-prompt-injector`** | Inject safety/system-policy text. | `claude-code/context.ts`; `AI-Harness-Systems.md` §safety | All | Section ordering | T1, T8 | `system-prompt-builder` |
| 🟡 **`hitl-engine`** | Human-in-the-loop interrupt/resume; approval gates. | `langgraph` HITL; `haystack/.../human_in_the_loop/`; `agent-framework/.../_request_info_mixin.py`; `crewAI/.../flow/human_feedback.py` | GRPH, MGD, CRW, RES | Resumption tokens; UI hooks | T1, T3, T4 | `permission-engine`, `runtime-orchestrator`, `checkpoint-store` |
| 🟡 **`pii-redactor`** | Detect/redact PII in inputs, outputs, logs. | `openai-agents/guardrails`; common DLP libs | CHN, MGD, VOICE | Recall vs precision | T1, T5, T8 | `model-adapter` |
| ✅ **`prompt-injection-detector`** | Section 18 — `classifyText(text, opts?)` returns `{ classification: "clean"\|"suspicious"\|"malicious", score, hits }`. Layer 1: 50+ regex rules over OWASP LLM Top-10 + hand-crafted vectors (ignore-previous, system-prompt leak, role-marker injection, destructive shell, exfil, javascript: URIs, unicode tag spoof, RTL override, etc). Layer 2: structural heuristics (BOM, role-marker cluster, trailing imperative, suspicious base64-with-decode-neighbour, URL+credential pair). Layer 3: optional LLM classifier gated by `CREWHAUS_PI_CLASSIFIER_MODEL` and a runtime-supplied callback. Severity-weighted probabilistic-OR aggregate score; thresholds 0.40 / 0.80. `runtime-core.applyInjectionClassification()` is the post-tool hook: malicious → `buildRedactionNotice(hits)` replaces the previewContent + `permission_decision { outcome: "redacted", rules }` event; suspicious → kept + `permission_decision { outcome: "warned" }` event + once-per-session warning. Tools opt out via `classifyOutput: false` (used by the in-process `Task` wrapper only). PR feat/section-18-safety-floor. | `gstack-main/.../prompt-injection-defense/`; `AI-Harness-Systems.md` §security | All | Injection taxonomy; mitigation cost | T1, T8 | `tool-result-store`, `model-adapter` |

### Layer R9 — Hooks, Skills, Slash Commands

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`hooks-engine`** | Lifecycle hooks fired at every meaningful runtime moment. v0 ships `loadHooks` (user-then-project layered `.crewhaus/settings.json`) + `runHooks(event, payload, hooks)` + `aggregateDecisions`. Events: `session-start | stop | pre-tool | post-tool | pre-model | post-model | pre-compact | post-compact | pre-slash`. Spawn via `Bun.spawn(["sh", "-c", cmd])` with **restricted env** (PATH=`/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin`, HOME, USER, LANG, TERM, TMPDIR, LC_*; strips `ANTHROPIC_AUTH_TOKEN`/`AWS_*`/`GH_TOKEN`/`OPENAI_API_KEY`). Each hook gets a 5 s default timeout (SIGKILL on miss, drain-grace pattern from `tool-bash` for orphan grandchildren). Decision: `{ decision: "allow"\|"deny"\|"block", reason?, mutate? }` — first deny/block wins; mutate honoured only for `pre-slash` (the `expanded` field). Section 11. | `openclaw/hooks/`; `claude-code/hooks/`; `agent-framework/.../_middleware.py`; `crewAI/.../hooks/` | CLI, CHN, CRW, GRPH, MGD, RES, BATCH | Hook lifecycle; user vs system hooks | T1, T3, T8 | `infra-utils`, `error-types` |
| **`hook-loader`** | Discover hooks from settings.json, plugins, project files. | `claude-code/hooks/` | CLI, CHN | Discovery order | T1, T3 | `hooks-engine`, `config-loader` |
| ✅ **`skills-registry`** | Lazy-loaded SKILL.md catalog. v0 ships `discoverSkills({ cwd, homeDir, pluginDirs })` (frontmatter only, walks `~/.crewhaus/skills/*/SKILL.md` then project then plugin dirs, project overrides user by `name`), `parseSkillFile` (`yaml` lib, validates required `name`+`description`, optional `triggers`/`tools`), `loadSkillBody(ref)` (full body on demand), `formatSkillsForPrompt` (renders `Available skills:` system block), and `createSkillTool(skills)` returning a `RegisteredTool` named `Skill` with `inputSchema z.object({ name: z.string() })` whose `execute` calls `loadSkillBody`. Lazy-load contract: discovery touches frontmatter only; body lands only when `Skill({ name })` is invoked. T3 verifies the contract via a body-content spy. Section 11. | `openclaw/.../agents/skills/`; `claude-code/skills/` | CLI, CHN, RES | Lazy-loading; conflict | T1, T3 | `tool-builder`, `infra-utils` |
| **`skills-serializer`** | Render skills list into system prompt. | `openclaw/.../agents/skills/serialize.ts`; `claude-code/.../skills/loadSkillsDir.ts` | CLI, CHN, RES | Token budget for skill list | T1, T9 | `skills-registry`, `system-prompt-builder` |
| ✅ **`slash-commands`** | Markdown-templated slash commands. v0 ships `loadCommands({ cwd })` (reads `<cwd>/.crewhaus/commands/*.md`, optional `description`/`argument-hint`/`argumentHint` frontmatter), `parseCommandFile`, and `expand(input, commands)` that matches `^\/(\S+)\s*([\s\S]*)$` and substitutes via `body.split("$ARGUMENTS").join(args)` — non-recursive, regex-special-safe. Returns `{ handled: false, expanded: input }` for non-slash or unknown commands. Pre-slash hook fires from `runtime-core` before substitution; `mutate.expanded` overrides. T9 property test runs 200 random body+args pairs against the substitution invariant. Section 11. | `openclaw/commands/`; `claude-code/commands.ts`,`commands/`; gstack slash-command catalog | CLI, CHN | Arg parsing; help | T1, T9 | `infra-utils` |
| 🟡 **`output-styles`** | Output-style/persona overlays. | `claude-code/outputStyles/` | CLI, CHN, VOICE | Style composition | T1, T5 | `system-prompt-builder`, `hooks-engine` |
| 🟡 **`autoreply-policy`** | Should agent respond? (mention-gating, batch debounce, heartbeat suppression). | `openclaw/auto-reply/`; `openclaw/.../cron/heartbeat-policy.ts` | CHN | Decision tree | T1, T9 | `channel-adapter-base`, `scheduler-cron` |

### Layer R10 — Multi-Agent / Coordination

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🟡 **`subagent-runtime`** | Spawn subagents w/ filtered tool sets and forked context. (Largely realised by Section 13's `agent-context-isolation` + `sub-agent-spawner`; this row tracks the broader runtime contract that absorbs them.) | `claude-code/.../tools/AgentTool/`; `claude-code/.../utils/agentContext.ts`; `openclaw/.../agents/pi-embedded-runner/`; `crewAI/.../agent`; `openai-agents/.../_agent.py`; `adk-python/.../agents/` | CLI, CRW, MGD, RES | Context filter rules; isolation | T1, T3, T6 | `agent-context-isolation`, `sub-agent-spawner` |
| ✅ **`agent-context-isolation`** | `createIsolatedContext(parent, opts)` — fresh `RunContext` (new runId / sessionId), child `EventLog` (own JSONL file under the same `sessionRootDir`), isolated `state-store`, child `AbortTree` rooted on parent's signal so SIGINT cascades down but child completion doesn't propagate up. Owns the shared types (`SubAgentDefinition`, `ParentRunHandle`, `RuntimeBridge`, `SpawnSubAgentFn`, `SubAgentResult`) so `runtime-core` and `tool-task` consume them without a cycle. Section 13. | `claude-code/.../utils/agentContext.ts`; `openai-agents/handoffs.py` | CLI, CHN, CRW, RES | RunContext lifetime; abort tree | T1 | `run-context`, `state-store`, `event-log`, `abort-controller` |
| ✅ **`sub-agent-spawner`** | `spawnSubAgent(parent, opts)` — wires `createIsolatedContext` + child `runChatLoop({ singleTurn: true, runContext: child.runContext, … })` + `replayMessageHistory` of the child's event log to assemble `SubAgentResult { finalMessage, transcript, toolCalls, usage }`. Re-emits boundary events `sub_agent_start` / `sub_agent_end` on the PARENT's event log so the boundary is visible from the parent transcript. T7 covers 10-way parallel fan-out from one parent. Section 13. | `claude-code/services/agents/spawnSubAgent.ts` | CLI, CHN, CRW, RES | Parent re-emission; usage roll-up | T1, T3, T7 | `agent-context-isolation`, `runtime-orchestrator` |
| ✅ **`sub-agent-permission-inheritance`** | `resolveChildPermissions(parent, def)` — modes `inherit | scoped | { allow, deny }`. Bypass does NOT propagate: when `parent.mode === "bypass"` and `def.inherit_bypass !== true`, child mode falls back to `default`. Scoped mode keeps only parent rules whose toolGlob matches at least one name in `def.tools`. Replace mode rebuilds yaml-source rules and preserves `BUILTIN_DEFAULT_RULES` as the safety floor. Section 13. | `claude-code/utils/permissions/childInheritance.ts` | CLI, CHN, CRW, RES | Bypass non-propagation | T8, T9 | `permission-engine`, `tool-permission-matcher` |
| 🟡 **`coordinator`** | Centralized coordinator (manager → workers). | `claude-code/.../coordinator/`; `crewAI/.../process.py`; `autogen/.../magentic-one`; `agent-framework/.../orchestrations/` | CRW, CLI, RES | Task assignment; aggregation | T1, T3, T6 | `sub-agent-spawner`, `runtime-orchestrator`, `task-engine` |
| 🟡 **`swarm-runtime`** | Decentralized peer-to-peer team (tmux/iTerm/in-process/network). | `claude-code/.../utils/swarm/`; `openclaw/.../tasks/detached-task-runtime.ts` | CRW, CLI | Discovery; consensus | T1, T3 | `sub-agent-spawner`, `abort-controller`, `node-host` |
| 🟡 **`handoff-engine`** | Structured agent-to-agent handoff w/ filters. (Section 22 ships the v0 of this surface as `agent-handoff` + the `crew-orchestrator` mailbox; richer filter DSLs land later.) | `openai-agents/.../handoffs/`,`handoff_filters.py`,`handoff_prompt.py`; `crewAI/.../process.py`; `agent-framework/.../orchestrations/` | CRW, MGD, GRPH | Handoff envelope; filter DSL | T1, T3, T6 | `agent-handoff`, `crew-orchestrator` |
| ✅ **`agent-handoff`** | Section 22 — `createHandoffTool({from, targets})` builds an in-band `Handoff(target, reason, context?)` tool. Receiver inherits the parent's session + transcript via the orchestrator's `resume` plumbing (NOT a sub-agent fork). Self-handoff and unknown-target rejected at execute-time; refusal-loop guard depth-counts handoffs and trips `HandoffRefusedError` in the orchestrator. T1 + T8 cover the refusal-loop guard. | `openai-agents/handoffs.py`; `crewAI/.../task_handoff.py` | CRW | Refusal-loop guard; in-band session inheritance | T1, T8 | `agent-context-isolation`, `tool-builder`, `tool-catalog` |
| ✅ **`crew-orchestrator`** | Section 22 — role-based dispatcher. `Crew()` builder: `addRole`, `setEntry`, `setRouting`, `setName`, `compile` → `RunnableCrew`. `run(input, opts) → AsyncIterable<CrewEvent>` over `role_start | handoff | a2a_message | role_end | crew_done`. Drives every role via `runChatLoop({ singleTurn: true })` sharing one `RunContext`/`TraceEventBus` so all spans carry the same `traceId`. Auto-injects Handoff + SendMessage tools per role and grants them at the `flag` rule layer. Refusal-loop guard trips at `refusalDepth` (default 2); A2A recursion capped at `maxA2ADepth` (default 3); total activations capped at `maxActivations` (default 16). T1 builder methods + T3 3-role end-to-end + T8 refusal-loop + T9 traceparent property. | `crewAI/.../crew.py`; `agent-framework/.../_workflows/_workflow.py`; AutoGen Magentic-One | CRW | Role dispatch; refusal-loop guard | T1, T3, T8, T9 | `agent-handoff`, `a2a-protocol`, `runtime-core`, `agent-context-isolation`, `event-log`, `permission-engine` |
| 🟡 **`role-system`** | Role definitions (CrewAI-style: backstory/goal/agent-type), team composition. (Section 22 ships the role-as-`{model, instructions, tools}` shape used by `crew-orchestrator`; richer schemas land later.) | `crewAI/.../agent`; `crewAI/.../crew.py`; `crewAI/crews/` | CRW | Role schema; conflict | T1, T6 | `ir-model`, `system-prompt-builder`, `crew-orchestrator` |
| 🟡 **`task-engine`** | Task lifecycle (create/run/track/result), dependency wiring. | `openclaw/tasks/`; `claude-code/Task.ts`,`tasks.ts`; `crewAI/.../task.py`,`tasks/` | CRW, CLI, MGD, BATCH, RES | DAG semantics | T1, T3, T7 | `tool-task`, `state-store`, `durable-execution` |
| 🟡 **`pairing-engine`** | Pair/teammate model (operator + agent, multi-host). | `claude-code/buddy/`; `openclaw/pairing/` | CLI, CHN | Cross-process state sync | T1, T3 | `state-store`, `channel-adapter-base`, `node-host` |

### Layer R11 — Workflow / Graph / Pipeline Engines

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| ✅ **`graph-engine`** | Section 19 — `createGraph({ checkpointStore? })` builder: `addNode(name, fn)`, `addEdge(from, to, condition?)`, `addParallel(names)`, `setEntry(name)`, `setInputAdapter`. `compile()` returns `RunnableGraph.run(input, opts)` yielding `node_start | node_end | edge_taken | checkpoint | hitl_pause | run_done`. HITL via `ctx.requestApproval(prompt)` (throws `HitlPauseSignal`, engine catches and persists checkpoint); `resume(grun, ckpt, decision)` reattaches by feeding the decision back through `NodeContext.approval`. Parallel groups run via `Promise.all` and merge with last-writer-wins (mutating accumulator avoids quadratic spread). Each node receives the parent `RunContext` so existing trace events publish naturally. PR feat/section-19-grph-target. | `langgraph/.../pregel/`; `agent-framework/.../_workflow.py`,`_workflow_builder.py`,`_executor.py` | GRPH, MGD | Pregel-style supersteps; deterministic stepping | T1, T3, T4 | `state-store`, `run-context`, `recovery-engine`, `abort-controller`, `checkpoint-store` |
| 🔴 **`workflow-engine`** | Async-event workflow runtime; pub/sub event bus, step decorators. | `crewAI/.../flow/`; `llama_index/.../workflow`; `adk-python/.../flows/` | CRW, MGD, RES, BATCH | Event semantics; backpressure | T1, T3, T7 | `state-store`, `run-context`, `event-bus`, `sub-agent-spawner` |
| ✅ **`pipeline-engine`** | Section 21 — `createPipeline` builder with `addComponent(name, fn)`, `connect(from, to, mapping?)`, `setInput`, `setOutput`, `compile`. Pure async-fn components, Kahn-topological scheduler that runs sibling layers via `Promise.all`, optional EdgeMapping renames, ComponentEvent stream. Cycles throw at compile time. PR feat/section-21-rag-target. | `haystack/.../core/`; `haystack/components/`; `llama_index/.../query_engine/` | RAG | DAG validation; type-flow | T1, T3 | `ir-model`, `model-adapter`, `run-context` |
| ✅ **`durable-execution`** | Section 19 — `idempotencyKey(grun, nodeName, attempt?)` (SHA-256 truncated to 24 hex), `IdempotencyStore` interface + `InMemoryIdempotencyStore`, `withIdempotency(inner, { store?, attempt? })` wrapper that caches each completed call so crash-replay is exactly-once, and `resumeFrom(store, grun)` returning `{ checkpointId, nextNode }` for `graph.run({ resumeFrom })`. The graph engine's checkpoint-after-each-node contract supplies the durable substrate; this package layers idempotency keys on top. PR feat/section-19-grph-target. | `agent-framework/.../durabletask/`; `langgraph` durability modes | GRPH, MGD, BATCH, RES | Activity replay; idempotence | T1, T4, T7 | `checkpoint-store`, `run-context`, `idempotency-store` |
| 🟡 **`workflow-checkpointer`** | Workflow-specific checkpointing. | `agent-framework/.../_checkpoint.py` | GRPH, MGD, RES | Encoding format | T1, T4 | `checkpoint-store`, `workflow-engine`, `checkpoint-encoder` |
| 🟡 **`event-bus`** | Internal event publishing for workflow/agents. | `crewAI/.../events/`; `llama_index/.../instrumentation/` | CRW, MGD, GRPH | Topic taxonomy; subscriber model | T1, T3, T7 | `trace-event-bus`, `run-context` |
| 🟡 **`checkpoint-encoder`** | Versioned encoding/decoding of checkpoint payloads. | `agent-framework/.../_checkpoint_encoding.py`; LangGraph serializers | GRPH, MGD, RES | Schema evolution; size | T1, T4 | `error-types`, `migration-engine` |

### Layer R12 — RAG / Retrieval / Knowledge

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🟡 **`document-store`** | Document storage abstraction (in-mem/FAISS/qdrant/pinecone/weaviate). | `haystack/.../document_stores/`; `llama_index/.../indices/` | RAG, CHN, CRW, RES | Backend selection; metadata filter | T1, T2, T7 | `infra-utils`, `secrets-manager` |
| 🟡 **`retriever`** | Retrieve docs by similarity / BM25 / hybrid / structured-query. | `haystack/.../retrievers/`; `llama_index/.../retrievers/`; `dspy/.../retrievers/`; `crewAI/.../rag/` | RAG, CHN, CRW, RES | Hybrid scoring; recency | T1, T5 | `document-store`, `embedding-adapter` |
| 🟡 **`ranker`** | Rerank retrieved docs (cross-encoder/LLM/fusion). | `haystack/.../rankers/` | RAG, RES | Cost vs uplift | T1, T5 | `retriever`, `model-adapter` |
| 🟡 **`reader-extractor`** | Extract spans/answer from retrieved docs. | `haystack/.../readers`; `llama_index/.../extractors/` | RAG | Extractive vs abstractive | T1, T5 | `retriever`, `model-adapter` |
| 🟡 **`query-router`** | Conditional pipeline branching (query type / language / topic). | `haystack/.../routers/`; `llama_index/.../selectors/` | RAG, MGD | Classifier choice | T1, T3 | `pipeline-engine`, `model-adapter` |
| 🟡 **`generator`** | LLM call inside pipeline (with retrieved context). | `haystack/.../generators/`; `llama_index/.../llms/` | RAG | Wraps `model-adapter` | T1, T2 | `model-adapter`, `retriever` |
| **`document-loader`** | Read PDF/DOCX/HTML/Notion/Drive into Documents. | `haystack/.../readers`; `llama_index/.../readers/`; `openclaw/markdown/` | RAG, CHN, RES | Format coverage | T1, T2 | `infra-utils` |
| **`chunker`** | Split docs into chunks. | `haystack/.../preprocessors`; `llama_index/.../node_parser/` | RAG | Chunk size/overlap study | T1, T5 | `infra-utils` |
| 🟡 **`knowledge-graph`** | Optional KG layer over docs. | `crewAI/.../knowledge/`; `llama_index/.../graph_stores/` | RAG, CRW, RES | Triple extraction quality | T1, T5 | `document-store`, `model-adapter` |
| 🟡 **`ingestion-pipeline`** | Crawl → load → chunk → embed → store. | `llama_index/.../ingestion/`; `haystack` pipelines | RAG, RES | Incremental update; dedup | T1, T3, T7 | `document-loader`, `chunker`, `embedding-adapter`, `document-store` |
| 🟡 **`citation-tracker`** | Track sources, quotes, evidence per claim. | `agent-framework/.../citations/`; OpenClaw citation refs | RAG, RES | Span-level vs doc-level | T1, T5, T6 | `tool-result-store`, `model-adapter` |

### Layer R13 — Channels & Messaging

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| **`channel-registry`** | Register channel adapters by id; normalize ids. | `openclaw/.../channels/registry.ts`,`ids.ts` | CHN | Plugin loader integration | T1 | `ir-model`, `channel-adapter-base` |
| ✅ **`channel-adapter-base`** | Concrete `ChannelAdapter` interface lives in `@crewhaus/channel-adapter-slack` and is shared by future adapters: `{ id, verify, parseInbound, sendReply, setTyping }`. `parseInbound` returns a tagged union (`event` / `challenge` / `skip`); adapters surface their own `idempotencyKey` so the gateway dedups generically. Section 12. | `openclaw/.../channels/types.ts` | CHN | Capability model (text/media/buttons/threads) | T1, T2 | — |
| 🟡 **`channel-telegram`** | Telegram adapter. | `openclaw/.../channels/plugins/telegram*` | CHN | Long polling vs webhook | T1, T2, T3 | `channel-adapter-base`, `secrets-manager` |
| ✅ **`channel-slack`** | Slack adapter — no `@slack/*` SDK dep, just plain HTTP + Node `crypto`. HMAC-SHA256 verify of `v0:${ts}:${body}` with constant-time `timingSafeEqual` and ±5 min timestamp tolerance. Parses `event_callback` envelopes for `app_mention` + `message`, skips bot self-mentions, surfaces `event_id` as dedup key. `sendReply` POSTs `chat.postMessage` with `apiBaseUrl` overridable via constructor / `SLACK_API_BASE_URL` env (test injection). T8 covers the full tampering matrix (body, sig, secret, ts skew, missing headers, length mismatch). Section 12. | `openclaw/.../channels/plugins/slack*` | CHN | Socket mode; Block Kit | T1, T2, T3 | `channel-adapter-base`, `secrets-manager` |
| 🟡 **`channel-discord`** | Discord adapter. | `openclaw/.../channels/plugins/discord*` | CHN | Slash command interop | T1, T2, T3 | `channel-adapter-base`, `secrets-manager` |
| 🟡 **`channel-whatsapp`** | WhatsApp adapter (cloud API + linked devices). | `openclaw/.../channels/plugins/whatsapp*` | CHN | Cloud API vs Business | T1, T2, T3 | `channel-adapter-base`, `secrets-manager` |
| 🟡 **`channel-signal`** | Signal adapter. | `openclaw/.../channels/plugins/signal*` | CHN | Signal-CLI integration | T1, T2, T3 | `channel-adapter-base`, `sandbox` |
| 🟡 **`channel-bluebubbles`** | iMessage via BlueBubbles. | `openclaw/.../channels/plugins/bluebubbles*` | CHN | Mac dependency | T1, T2, T3 | `channel-adapter-base` |
| 🟡 **`channel-imessage-native`** | Native macOS iMessage. | `openclaw/.../channels/plugins/imessage*` | CHN | AppleScript bridge | T1, T2, T3 | `channel-adapter-base` |
| 🟡 **`channel-email`** | Email (IMAP/SMTP). | TBD ref | CHN, BATCH | Threading; reply-to | T1, T2 | `channel-adapter-base`, `scheduler-cron` |
| 🟡 **`channel-sms`** | SMS via Twilio/Vonage. | TBD ref | CHN | Length splits | T1, T2 | `channel-adapter-base`, `secrets-manager` |
| 🟡 **`channel-web`** | Web chat widget. | `openclaw/.../channels/web/`; `openclaw/.../canvas-host/` | CHN | Streaming over WS | T1, T2, T3 | `channel-adapter-base`, `gateway-server` |
| **`channel-transport`** | Stall watchdog, retries, batching. | `openclaw/.../channels/transport/` | CHN | Watchdog timeouts | T1, T7 | `infra-utils`, `retry-policy` |
| **`channel-binding`** | Session ↔ channel-conversation mapping. | `openclaw/bindings/` | CHN | Identity collisions | T1, T3 | `session-store`, `channel-registry` |
| 🟡 **`channel-router`** | Outbound message routing (which channel for which session). | `openclaw/routing/` | CHN | Fallback/preferences | T1, T3 | `channel-registry`, `session-binding` |
| **`channel-features`** | Cross-channel features (typing, edit-in-place, mention-gate, allowlist, model-override, debounce, threads). | `openclaw/.../channels/plugins/` features | CHN | Capability-based feature gating | T1, T3 | `channel-adapter-base` |
| **`directory-adapters`** | Contact/group resolution per channel. | `openclaw/.../channels/plugins/directory-adapters.ts` | CHN | Caching | T1, T3 | `channel-adapter-base` |
| 🟡 **`media-payload`** | Per-channel media limits, format conversion, payload framing. | `openclaw/.../channels/plugins/media-limits.ts`,`media-payload.ts` | CHN | Transcoding | T1, T2 | `channel-adapter-base`, `media-service` |
| 🟡 **`pairing-flow`** | Device pairing for mobile channels. | `openclaw/pairing/`; `openclaw/.../channels/plugins/pairing.ts` | CHN | UX for QR pairing | T1, T3 | `channel-adapter-base`, `secrets-manager` |
| **`message-actions`** | Reactions, edits, deletes, inline buttons. | `openclaw/.../channels/plugins/message-actions.ts` | CHN | Per-channel feature matrix | T1, T2 | `channel-adapter-base` |

### Layer R14 — Scheduling & Background

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🟡 **`scheduler-cron`** | Cron service: at/every/cron schedules, persistence, run/wake/enqueue. | `openclaw/.../cron/service.ts`,`service/ops.ts`,`types.ts` | CHN, CLI, RES | Drift; missed-fire policy | T1, T7 | `state-store`, `event-log` |
| 🟡 **`heartbeat-engine`** | Periodic system event for "anything to do?"; HEARTBEAT_OK suppression. | `openclaw/.../cron/heartbeat-policy.ts` | CHN | Suppression logic | T1, T3 | `scheduler-cron`, `channel-adapter-base` |
| 🟡 **`isolated-agent-runner`** | Run a one-shot agent for cron/heartbeat in own session. | `openclaw/.../cron/isolated-agent/` | CHN | Session reaping | T1, T3 | `agent-context-isolation`, `session-store` |
| **`session-reaper`** | Cleanup expired/orphaned sessions. | `openclaw/.../cron/isolated-agent/` reaper | CHN, MGD | Eviction policy | T1, T7 | `session-store`, `scheduler-cron` |
| **`background-housekeeping`** | Cache eviction, log rotation, FS GC. | `claude-code/.../utils/backgroundHousekeeping.ts` | All | Cadence; pause-resume | T1, T7 | `scheduler-cron`, `infra-utils` |
| 🟡 **`task-scheduler`** | One-off scheduled tasks. | `openclaw/tasks/`; `mcp__scheduled-tasks` | CHN, CLI, RES | Persistence | T1, T3 | `scheduler-cron`, `state-store` |
| 🟡 **`queue-consumer`** | Pull from queue (SQS/Redis/RabbitMQ/NATS/etc.). | None directly — reference `temporal`/`celery`/`dramatiq` patterns | BATCH | Backend abstraction; ack/nack | T1, T2, T3, T7 | `secrets-manager`, `retry-policy`, `infra-utils` |
| **`idempotency-store`** | Track processed message ids; exactly-once-ish semantics. | Common patterns; `temporal` workflow ids | BATCH, CHN | TTL; collision | T1, T7 | `infra-utils` |
| **`rate-limiter`** | Token-bucket / leaky-bucket outbound limiting per provider/tool. | Common patterns | All except EVAL | Per-tenant fairness | T1, T7 | `infra-utils` |
| **`retry-policy`** | Backoff strategies (exp/jitter/circuit-breaker). | `agent-framework/.../_retry.py`; common patterns | All | Failure taxonomy | T1, T7 | `infra-utils`, `error-types` |
| 🟡 **`dead-letter-queue`** | Quarantine failed items. | Common patterns | BATCH, CHN | Replay UX | T1, T3, T7 | `queue-consumer`, `event-log` |
| **`batch-progress-tracker`** | Track batch progress for long-running jobs. | None — design | BATCH, RES | Reporting cadence | T1, T3 | `trace-event-bus` |

### Layer R15 — Telemetry, Tracing, Eval

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🟡 **`telemetry-engine`** | Structured logging, metrics, OpenTelemetry spans. | `agent-framework/.../observability.py`,`_telemetry.py`; `openai-agents/.../tracing/`; `openclaw/logging/`; `claude-code/.../services/internalLogging.ts`; `haystack/.../tracing/` | All | Span vocabulary; sampling | T1, T7 | `trace-event-bus`, `otel-exporter`, `metrics-collector`, `logging` |
| ✅ **`trace-event-bus`** | In-process pub/sub bus carrying a `TraceEvent` discriminated union (15 kinds: `turn_start/end`, `model_request/response/stream_token`, `tool_call_start/end`, `mcp_call_start/end`, `hook_fired`, `compaction_fired`, `permission_decision`, `error_recovered`, `sub_agent_start/end`); 5000-event ring buffer with `recent({since?,kinds?})`; W3C `traceparent` propagation; `ephemeral` flag for stream tokens; subscriber failure isolation. PR feat/section-15-observability. | `agent-framework/.../observability.py`; `openai-agents/.../tracing/` | All | Subscriber overhead at 1k-event/s | T1, T7, T9 | `infra-utils` |
| ✅ **`otel-exporter`** | OTLP/HTTP exporter (no SDK dep). Maps `TraceEvent` to OTel spans using `gen_ai/*` semantic conventions (`gen_ai.system`, `gen_ai.request.model`, `gen_ai.usage.input_tokens`/`output_tokens`, `gen_ai.response.finish_reason`, `gen_ai.operation.name`); `model_stream_token` → span events on the model span (never per-token spans); 5s batch flush + sync flush on shutdown; honors `OTEL_EXPORTER_OTLP_ENDPOINT`/`HEADERS`/`OTEL_SERVICE_NAME`. PR feat/section-15-observability. | `agent-framework/.../observability.py`; OTel GenAI conventions | All | Backend matrix | T1, T2 | `trace-event-bus`, `infra-utils` |
| 🟡 **`trace-recorder`** | Persist spans/traces locally (replay, grading). | `openai-agents/tracing/`; `agent-framework/.../foundry/` traces | All | Storage size; rotation | T1, T7 | `trace-event-bus`, `event-log` |
| ✅ **`metrics-collector`** | Dependency-free counters/histograms with Prometheus default buckets. Counters: `crewhaus_turns_total`, `crewhaus_tool_calls_total{tool}`, `crewhaus_tokens_total{direction}`, `crewhaus_errors_total{kind}`. Histograms: `crewhaus_turn_duration_seconds`, `crewhaus_tool_duration_seconds{tool}`, `crewhaus_model_ttft_seconds`. Three sinks (Prometheus textfile / buffered stdout JSON / pull-based HTTP `/metrics`). Selected via `CREWHAUS_METRICS=stdout\|textfile\|textfile:/path\|http:PORT`. Stdout sink buffers + emits one JSON dump on flush so it doesn't interleave with the model's assistant text on stdout. PR feat/section-15-observability. | `agent-framework/.../observability.py` | All | Dimension cardinality | T1, T3, T7 | `trace-event-bus`, `infra-utils` |
| ✅ **`structured-event-printer`** | Pretty (`CREWHAUS_TRACE=pretty`) or JSON Lines (`CREWHAUS_TRACE=json`) printer subscribed to the trace bus. Color-codes by event kind (gated on TTY + `NO_COLOR`); collapses `model_stream_token` deltas into a single rolling line via `\r` rewrites on a TTY (or one summary line on `model_response` when stderr is piped). PR feat/section-15-observability. | None — design | All | Format stability | T1 | `trace-event-bus` |
| 🟡 **`replay-engine`** | Deterministic replay of recorded runs (fixtures + traces). | `dspy/cache.py`; `openai-agents/.../tracing/` replay | EVAL, CLI, CHN, all CI | Determinism boundaries | T1, T4 | `trace-recorder`, `model-adapter`, `event-log` |
| 🔴 **`eval-service`** | Run eval suites: dataset → run → grade → report. | `dspy/.../evaluate/`; `helm/benchmark/`; `lm-evaluation-harness/lm_eval/.../evaluator.py`; `ragas/.../evaluation.py`; `haystack/.../evaluation/`; `crewAI/.../experimental/` evals; `adk-python/.../evaluation/` | EVAL, MGD, GRPH (CI), RES | Suite composition; threshold gating | T1, T3, T5 | `trace-event-bus`, `dataset-registry`, `grader-registry`, `runtime-orchestrator` |
| ✅ **`eval-dataset`** | Section 16 dataset loaders: JSONL, CSV, YAML, HTTP. Lazy `AsyncIterable<Sample>` so 100k-sample datasets stream rather than load fully; `Sample = {id, input, expected_output?, expected_tools?, metadata?}`. JSONL/CSV stream off disk via `Bun.file().stream()` line-buffering; YAML and HTTP buffer (YAML cannot stream; HTTP buffers before re-dispatching by `Content-Type`). Hand-rolled RFC 4180 CSV parser (no `csv-parse` dep). PR feat/section-16-eval-stack. | `helm/benchmark/scenarios/`; `lm-evaluation-harness/lm_eval/tasks/` | EVAL | Streaming/buffer trade-off documented | T1 | `errors`, `yaml`, `zod` |
| ✅ **`eval-grader`** | Section 16 deterministic graders + composers. Built-ins: `exact_match`, `contains`, `regex`, `json_path` (hand-rolled subset: `$`, `.key`, `["key"]`, `[N]`, `[*]`, `..key`), `schema` (Zod), `tool_call_sequence` (`exact` / `subseq` / `set` modes). Composers: `all` (AND, score=min), `any` (OR, score=max), `weighted` (Σ score·w / Σw, threshold gate). YAML-driven config via `parseGradersConfig` discriminated-union schema. PR feat/section-16-eval-stack. | `helm/.../metrics/`; `dspy/.../evaluate/metrics.py`; OpenAI graders | EVAL | Composition; rationale strings | T1, T9 (property tests on composers) | `eval-dataset`, `event-log`, `trace-event-bus`, `zod` |
| ✅ **`eval-judge`** | Section 16 LLM-as-judge grader. Default model `claude-sonnet-4-5`. Per-call random sentinel-token wrapping (`<<<UNTRUSTED_<hex>>>>` / `<<<END_<hex>>>>`) for prompt-injection defense; system prompt classifies sentinel-wrapped content as DATA. Structured output enforced via Anthropic `tool_choice: {type: "tool", name: "submit_score"}` — judge cannot deviate. OAuth-aware via `runtime-core.createAnthropicClient` (prepends "You are Claude Code" prefix when token is OAuth so the API doesn't reject it). PR feat/section-16-eval-stack. | DSPy LLM-judge; OpenAI evals; `ragas/metrics/` | EVAL | Prompt-injection corpus (T8) | T1, T8 (13-payload corpus) | `eval-dataset`, `eval-grader`, `runtime-core`, `zod-to-json-schema` |
| ✅ **`eval-runner`** | Section 16 runner: per-sample fresh `RunContext` (fresh `TraceEventBus` + `sessionId`); concurrency configurable (default 4) via in-package `Semaphore` (~25 lines, no `p-limit` dep); `Promise.allSettled` so one bad sample doesn't kill the rest; `process.on("SIGINT")` flushes partial `results.json`. Persists per-sample artifacts to `.crewhaus/evals/<runId>/<sampleId>/{transcript.jsonl, events.jsonl, grades.json, meta.json}`; run-level `run.json` (config snapshot incl. `Bun.hash` spec hash) + `results.json` (aggregates). Forces `permissionMode: "auto"` so `alwaysAsk` rules auto-deny. MCP servers shared across samples (read-mostly assumption; `isolateMcpPerSample?` reserved). `--seed` recorded in `meta.json` for replay tolerance comparison; not honored by Anthropic. `AgentInvoker` injection point lets tests substitute a deterministic stub for `runChatLoop`. `wireRunOnce(ir, {cwd})` factors the agent stack (tools/hooks/skills/slash-commands/sub-agents/MCP/permission rules) into one helper — single source of truth for new code. PR feat/section-16-eval-stack. | `dspy/.../evaluate/`; `helm/benchmark/`; `lm-evaluation-harness` runner | EVAL | Per-sample isolation; concurrency SLO | T3 (5-sample fixture), T7 (200-sample concurrency-8 SLO <60s) | `eval-dataset`, `eval-grader`, `eval-judge`, `runtime-core`, `run-context`, `trace-event-bus`, `event-log`, `spec`, `compiler`, `ir`, `permission-engine`, `tool-catalog`, `mcp-host` (transitively all built-in tools) |
| ✅ **`eval-report`** | Section 16 report renderer. Self-contained HTML (~9KB, inlined CSS+vanilla JS for sortable table + `<details>` drill-down per sample showing transcript + trace timeline + grader rationales) + JSON output. Aggregates: pass rate, mean score, p50/p95 turn count, p50/p95 latency, total token usage. Diff mode `diffReports(prev, next)` highlights pass→fail regressions, fail→pass recoveries, score-shifts > ε=0.1; rejects mismatched dataset shapes (no silent index alignment — keysets must match). Run-id resolution: `^run_<hex>$` → `.crewhaus/evals/<id>/`, else treated as filesystem path. PR feat/section-16-eval-stack. | LangSmith run viewer; OpenAI eval reports; LaunchDarkly experiments diff | EVAL | HTML XSS escaping; diff stability | T1 (incl. snapshot + XSS escape) | `eval-runner` (types only), `errors` |
| 🟡 **`dataset-registry`** | Dataset abstractions, loaders, splits, golden sets (broader scope than `eval-dataset`'s file-level loaders — registry-level metadata, versioning, splits). | `helm/benchmark/scenarios/`; `lm-evaluation-harness/lm_eval/tasks/`; `dspy/.../datasets/`; `ragas/.../dataset.py`,`testset/` | EVAL | Split semantics | T1, T2 | `eval-dataset`, `infra-utils` |
| 🟡 **`grader-registry`** | Pluggable graders (exact-match/llm-judge/ragas-faithfulness/custom) with discoverable plugin surface beyond Section 16's built-in set. | `helm/.../metrics/`; `lm-evaluation-harness/lm_eval/api/metrics.py`; `dspy/.../evaluate/metrics.py`; `ragas/metrics/`; OpenAI graders | EVAL | Composition; calibration | T1, T5 | `eval-grader`, `eval-judge`, `eval-service`, `model-adapter` |
| 🟡 **`benchmark-runner`** | Multi-task benchmark orchestration. | `helm/benchmark/`; lm-evaluation-harness runner | EVAL | Reproducibility | T1, T7 | `eval-service`, `dataset-registry` |
| 🟡 **`trajectory-grading`** | Grade tool choices / handoffs / approvals / safety on full trajectory. | `AI-Harness-Systems.md` §trace grading; `openclaw/trajectory/`; OpenAI trace grading | EVAL, MGD, RES | Step-level vs run-level | T1, T6 | `trace-recorder`, `model-adapter`, `eval-service` |
| 🟡 **`canary-router`** | Live A/B traffic split for runtime comparison. | `AI-Harness-Systems.md` §canary | MGD | Routing strategies | T1, T3 | `eval-service`, `deployment-controller` |
| 🔴 **`prompt-optimizer`** | DSPy-style automatic prompt optimization (MIPRO, BootstrapFewShot). | `dspy/.../teleprompt/`,`dspy/.../predict/`,`dspy/.../optimization/`; `adk-python/.../optimization/` | EVAL, CLI (advanced) | Optimizer choice; budget | T1, T5 | `eval-service`, `model-adapter`, `system-prompt-builder` |
| 🟡 **`cost-attribution`** | Attribute cost across run/agent/tool/tenant. | `AI-Harness-Systems.md` §cost | All | Dimension model | T1, T7 | `trace-event-bus`, `cost-tracker` |

### Layer R16 — UI / TUI / Voice / Media

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🟡 **`tui-runtime`** | Terminal UI (React/Ink): REPL, message rendering, input, modals. | `claude-code/main.tsx`,`screens/`,`components/`,`ink/`; `openclaw/tui/`,`terminal/` | CLI, CHN | Render perf; modal stack | T1, T3 | `state-store`, `runtime-orchestrator` |
| **`tui-keybindings`** | Customizable keybindings (chord support). | `claude-code/.../keybindings/`; `~/.claude/keybindings.json` | CLI | Chord parser | T1, T9 | `infra-utils` |
| 🟡 **`repl-launcher`** | Launch interactive REPL or headless print. | `claude-code/.../replLauncher.tsx`,`main.tsx`; `openai-agents/.../repl.py` | CLI | Mode detection | T1, T3 | `runtime-orchestrator`, `tui-runtime` |
| 🟡 **`web-ui`** | Web client for the harness (DevUI / Studio runtime view). | `agent-framework/.../devui/`; OpenAI Agent Builder; LangSmith Studio | CHN, MGD, studio | Streaming over WS | T1, T3 | `gateway-server`, `trace-event-bus` |
| 🔴 **`gateway-server`** | HTTP+WebSocket+JSON-RPC server (App Server). | `openclaw/gateway/`; OpenAI Codex App Server JSON-RPC; `claude-code/.../server/`; `agent-framework/.../foundry_hosting/` | CHN, MGD, BATCH | Auth; rate limit | T1, T2, T7, T8 | `secrets-manager`, `runtime-orchestrator`, `permission-engine` |
| 🟡 **`gateway-protocol`** | Wire protocol (REST routes, WS messages, JSON-RPC methods). | `openclaw/.../gateway/protocol/`; OpenAI App Server protocol | CHN, MGD | Versioning | T1, T2 | `gateway-server` |
| 🔴 **`voice-runtime`** | Realtime voice loop (audio in/out, VAD, speech sessions). | `claude-code/voice/`; `openai-agents/.../voice/`,`realtime/`; `openclaw/realtime-voice/` | VOICE | Provider abstraction | T1, T2, T7 | `model-adapter`, `audio-stream`, `vad-engine` |
| 🔴 **`vad-engine`** | Voice activity detection. | OpenAI Realtime VAD; `webrtcvad`; Silero VAD | VOICE | Latency vs accuracy | T1, T5, T7 | `audio-stream` |
| 🔴 **`barge-in-controller`** | Handle user interruption mid-speech. | `openai-agents/.../realtime/`; common voice agent patterns | VOICE | Cancel + roll-back | T1, T3 | `voice-runtime`, `abort-controller` |
| 🔴 **`audio-stream`** | Audio I/O streaming (PCM/Opus); buffering. | OpenAI Realtime; common patterns | VOICE | Codec; framing | T1, T2, T7 | `infra-utils` |
| 🔴 **`call-session`** | Call lifecycle: ring/answer/hold/transfer/hangup. | Twilio Voice; Vonage; Agora | VOICE | State machine | T1, T3 | `voice-runtime`, `channel-adapter-base` |
| 🔴 **`telephony-adapter`** | Telephony provider integration (Twilio/Vonage/Plivo/SIP). | TBD ref | VOICE | SIP vs HTTP webhook | T1, T2, T3 | `call-session`, `secrets-manager`, `webhook-host` |
| 🟡 **`dtmf-handler`** | DTMF (keypress) handling. | Twilio Voice docs | VOICE | Detection threshold | T1, T2 | `call-session` |
| 🟡 **`prosody-controller`** | Voice/speed/pause/SSML directives. | `openclaw/.../tts/directives.ts` | VOICE, CHN | SSML support | T1, T2 | `voice-runtime` |
| 🟡 **`media-service`** | Image/audio/video handling, thumbnails, format conversion. | `openclaw/media/`,`media-understanding/`; `openclaw/.../agents/tool-images.ts` | CHN, CRW, VOICE | Codec matrix | T1, T2, T7 | `infra-utils` |
| 🟡 **`canvas-host`** | Canvas/A2UI rich-content surface server. | `openclaw/canvas-host/`; `agent-framework/.../ag_ui/` | CHN | Widget vocabulary | T1, T2, T3 | `gateway-server`, `channel-adapter-base` |
| 🟡 **`notifications-service`** | Cross-app notifications (system tray, OS, push). | `claude-code/.../services/notifier.ts`,`hooks/notifs/` | CLI, CHN, RES, BATCH | OS abstraction | T1, T3 | `channel-adapter-base`, `scheduler` |

### Layer R17 — Infrastructure & Cross-Cutting

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| **`config-loader`** | YAML/JSON/TS config loading, schema, env-var expansion, overlays. | `openclaw/config/`; `claude-code/.../services/settingsSync/`; `agent-framework/.../_settings.py` | All | Layered merge | T1, T9 | `infra-utils`, `error-types` |
| 🟡 **`settings-sync`** | Multi-source settings merge (project/user/enterprise/policy). | `claude-code/.../services/settingsSync/`,`remoteManagedSettings/`,`policyLimits/` | CLI, CHN, MGD | Precedence rules | T1, T3 | `config-loader` |
| **`i18n`** | Internationalization, locale, translations. | `openclaw/i18n/`; `crewAI/translations/` | CHN, VOICE | Locale negotiation | T1, T9 | `infra-utils` |
| ✅ **`logging`** | Structured logger interface. | `openclaw/logging/`; `claude-code/.../services/internalLogging.ts`; `haystack/logging.py` | All | Field schema | T1 | — |
| ✅ **`error-types`** | Typed error hierarchy, classification. | `openclaw/.../infra/errors/`; `agent-framework/.../exceptions.py`; `haystack/errors.py` | All | Recovery hint mapping | T1 | — |
| ✅ **`infra-utils`** | Common: paths, env, fs, json, retry, time, ids. | `openclaw/.../infra/`,`shared/`,`utils/`; `claude-code/.../utils/`; `agent-framework/.../utils/` | All | API stability | T1 | — |
| **`feature-flags`** | Build-time + runtime flags (dead-code elimination). | `claude-code/main.tsx` `bun:bundle feature()` pattern | All | Build-time elim | T1 | `config-loader` |
| 🟡 **`runtime-migrations`** | Versioned runtime data migrations. | `claude-code/.../migrations/` | All | Forward/back migrations | T1, T4 | `migration-engine` |
| 🟡 **`daemon-process`** | Long-running process manager, IPC, supervisor. | `openclaw/daemon/` | CHN, BATCH | Crash recovery | T1, T7 | `state-store`, `scheduler-cron` |
| 🟡 **`node-host`** | Multi-host execution (run agent on phone/server/laptop). | `openclaw/node-host/`,`pairing/` | CHN | Cross-host RPC | T1, T3 | `daemon-process`, `secrets-manager` |
| 🟡 **`proxy-capture`** | HTTP proxy for capturing/replaying tool traffic. | `openclaw/.../proxy-capture/`; `openclaw/.../security/proxy-server.ts` | EVAL, CLI | Cert installation | T1, T8 | `trace-event-bus`, `infra-utils` |
| 🟡 **`bootstrap-runtime`** | First-run trust prompts, telemetry consent, settings init. | `claude-code/main.tsx` `init()`; `openclaw/bootstrap/` | CLI, CHN, VOICE | Consent flows | T1, T3 | `config-loader`, `secrets-manager` |
| **`startup-profiler`** | Startup-time profiling, deferred prefetches, eager loads. | `claude-code/main.tsx` `profileCheckpoint()`,`startDeferredPrefetches()` | CLI, CHN | Critical path | T1, T7 | `trace-event-bus` |
| 🟡 **`update-channel`** | Auto-update / version channel selection. | `claude-code/.../utils/autoUpdater.ts`; `claude-code/.../utils/binaryCheck.ts` | CLI | Channel semantics | T1, T3 | `secrets-manager`, `infra-utils` |
| 🔴 **`tenancy`** | Multi-tenant isolation (tenant-id, quotas, billing). | `AI-Harness-Systems.md` §`tenant_id`; AgentCore tenant model | MGD | Quota model | T1, T3, T8 | `secrets-manager`, `audit-log`, `gateway-server` |
| 🔴 **`audit-log`** | Tamper-evident audit trail (separate from event log). | AWS AgentCore observability; `openclaw/.../security/audit*.ts` | MGD | Signing; rotation | T1, T8 | `event-log`, `secrets-manager` |

### Layer R18 — Specialized / Advanced

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🔴 **`computer-use-driver`** | Mouse/keyboard/screenshot drivers. | `mcp__computer-use__*`; OpenAI Operator | BROW, CLI | Cross-OS abstraction | T1, T2, T3 | `infra-utils` |
| 🔴 **`browser-extension-bridge`** | Bridge to browser extension MCP. | `mcp__Claude_in_Chrome__*` | BROW, CLI | Extension lifecycle | T1, T2, T3 | `mcp-host`, `gateway-server` |
| 🟡 **`lsp-host`** | LSP client for code-tools. | `claude-code/.../services/lsp/` | CLI | Server pooling | T1, T2, T3 | `infra-utils`, `scheduler` |
| 🟡 **`sandbox-fs-overlay`** | Copy-on-write workspace for safe edits + rollback. | `openai-agents/.../sandbox/files.py`; `openclaw/.../scripts/` worktree | CLI, EVAL | Diff materialization | T1, T3 | `tool-fs`, `infra-utils` |
| 🟡 **`structured-output-tools`** | Tools that emit typed structured output (graders, classifiers). | `dspy/.../predict/`; `claude-code/.../tools/SyntheticOutputTool` | EVAL, MGD | Schema reuse | T1, T2 | `tool-builder`, `model-adapter`, `response-format-coercion` |
| 🟡 **`autoplan-engine`** | Plan-then-act: dedicated planning model produces plan consumed by executor. | `gstack-main/.../autoplan/`; `claude-code/.../tools/EnterPlanModeTool` | CLI, CRW, RES | Plan format | T1, T6 | `model-adapter`, `system-prompt-builder` |
| 🟡 **`auto-classifier`** | Content classifier (`yoloClassifier`). | `claude-code/.../utils/permissions/yoloClassifier.ts` | CLI, CHN | Calibration | T1, T5 | `model-adapter` |
| **`fingerprint`** | Run/agent fingerprinting (deterministic build IDs, content-hashes). | `crewAI/.../security/fingerprint.py`; `claude-code/.../utils/attribution.ts` | EVAL, MGD | Hash granularity | T1, T4 | `infra-utils` |
| **`activity-manager`** | Track user/idle/active state for UI hints, away-summaries. | `claude-code/.../utils/activityManager.ts`,`awaySummary.ts` | CLI, CHN | Idle thresholds | T1, T3 | `state-store` |
| **`speculation-cache`** | Cached predictions for speculative execution. | `claude-code/.../state/AppStateStore.ts` `speculation` | CLI | Hit rate | T1, T7 | `state-store` |
| 🟡 **`commitments-engine`** | Track agent commitments/promises across sessions. | `openclaw/commitments/` | CHN | Promise schema | T1, T3 | `memory-service`, `session-store` |

### Layer R19 — Research-Agent Specific

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🔴 **`research-planner`** | Decompose research goal into ordered sub-questions; revise plan as evidence accrues. | `gstack-main/autoplan/`; `claude-code/.../tools/EnterPlanModeTool`; ADK research flows | RES | Plan revision policy | T1, T5, T6 | `autoplan-engine`, `model-adapter` |
| 🔴 **`crawler`** | Autonomous multi-page web crawl beyond simple fetch (frontier, dedup, polite). | `llama_index/.../readers/` web; common crawler patterns | RES, RAG | Politeness; cycle detection | T1, T3, T7 | `tool-fetch`, `scheduler-cron`, `idempotency-store` |
| 🔴 **`branch-explorer`** | Explore multiple research branches in parallel; rank/prune. | `langgraph` branching; subagent fan-out | RES | Branch budget; ranking | T1, T3, T6 | `sub-agent-spawner`, `run-context`, `branch-history` |
| 🟡 **`evidence-store`** | Accumulate evidence with metadata (source, timestamp, claim, support level). | `agent-framework/.../citations/`; KG-style stores | RES, RAG | Dedup; conflict resolution | T1, T5 | `document-store`, `citation-tracker` |
| 🔴 **`report-synthesizer`** | Compose long-form structured report from evidence (markdown/PDF/docx). | `anthropic-skills:docx`; `anthropic-skills:pdf`; `dspy/.../predict/` long-form | RES | Outline generation; section budgeting | T1, T5, T6 | `model-adapter`, `evidence-store` |
| 🟡 **`progress-streamer`** | Long-run progress reporting (since runs can be hours). | None directly — design | RES, BATCH | Reporting cadence | T1, T3 | `trace-event-bus`, `notifications-service` |
| 🔴 **`research-checkpointer`** | Stage-level checkpoints for hours-long runs (resume after crash/preempt). | `agent-framework/.../_checkpoint.py` (ref); `langgraph` durability | RES | Stage granularity | T1, T4, T7 | `checkpoint-store`, `durable-execution` |
| 🟡 **`source-quality-scorer`** | Score source credibility (domain authority, recency, citations). | TBD; common search QA patterns | RES, RAG | Score model | T1, T5 | `model-adapter`, `citation-tracker` |

### Layer R20 — Batch-Worker Specific

| Module | Responsibility | Refs | Targets | Research focus | Tests | Depends on |
|---|---|---|---|---|---|---|
| 🔴 **`batch-runner`** | Drive worker loop: pull → process → ack → repeat. | `temporal`/`celery`/`dramatiq` patterns | BATCH | Concurrency; back-off | T1, T3, T7 | `queue-consumer`, `retry-policy`, `runtime-orchestrator` |
| 🟡 **`batch-job-spec`** | Per-item job spec (input schema, output schema, timeout, retries). | None directly | BATCH | Schema design | T1, T2 | `ir-model` |
| 🟡 **`fan-out-fan-in`** | Split a large job into many tasks; aggregate results. | `agent-framework/.../orchestrations/`; `temporal` workflows | BATCH, CRW | Aggregation guarantees | T1, T3, T7 | `batch-runner`, `sub-agent-spawner` |
| 🟡 **`output-sink`** | Write results to destination (DB / S3 / file / webhook). | TBD | BATCH | Sink abstraction | T1, T2, T3 | `infra-utils`, `secrets-manager` |
| 🟡 **`batch-eval-loop`** | Tie batch worker to eval-service for online metric tracking. | `eval-service` | BATCH, EVAL | Metric streaming | T1, T5 | `batch-runner`, `eval-service`, `trace-event-bus` |

---

# PART C — Module Group Aggregations (coarse composition)

For ergonomic spec authoring, expose grouped bundles so users can compose at a coarser level. Each group is a stable selector that pulls in its constituent modules with sensible defaults.

| Group | Includes | When to enable |
|---|---|---|
| **Tool Layer (full)** | tool-catalog · tool-builder · tool-orchestrator · tool-executor · streaming-tool-executor · tool-result-store · tool-loop-detection · tool-search · tool-display · tool-permission-matcher · tool-validate | Any agent that calls tools |
| **MCP Bundle** | mcp-host · tool-mcp · mcp-server-host (optional) | Agents needing external/portable tools |
| **Compaction Stack** | compaction-snip · microcompact · context-collapse · autocompact · reactive · tool-result-budget · session-memory | Long-running chat/agent harnesses |
| **Permission Stack** | permission-engine · approval-classifier · denial-tracking · policy-engine · safety-prompt-injector · guardrails · prompt-injection-detector · pii-redactor | Production-grade safety |
| **Observability Stack** | telemetry-engine · otel-exporter · trace-recorder · metrics-collector · replay-engine · cost-tracker · cost-attribution · audit-log | Any production deployment |
| **Eval Stack** | eval-service · dataset-registry · grader-registry · benchmark-runner · trajectory-grading · prompt-optimizer · replay-engine | EVAL target or CI for any target |
| **Channel Stack** | channel-registry · channel-adapter-base · channel-{telegram,slack,discord,whatsapp,signal,bluebubbles,imessage-native,email,sms,web} · channel-transport · channel-binding · channel-router · channel-features · directory-adapters · media-payload · pairing-flow · message-actions | Channel-based assistants |
| **Scheduling Stack** | scheduler-cron · heartbeat-engine · isolated-agent-runner · session-reaper · task-scheduler · background-housekeeping | Always-on assistants |
| **Multi-Agent Stack** | subagent-runtime · coordinator · swarm-runtime · handoff-engine · role-system · task-engine · pairing-engine | Crews / supervised agents |
| **State & Persistence Stack** | session-store · event-log · checkpoint-store · state-store · app-state-store · branch-history · artifact-store · session-router · session-binding · replay-store | All harnesses (subset minimum) |
| **RAG Stack** | document-store · embedding-adapter · retriever · ranker · reader-extractor · query-router · generator · document-loader · chunker · knowledge-graph · ingestion-pipeline · citation-tracker · vector-store | Retrieval-heavy harnesses |
| **CLI Front-end Stack** | tui-runtime · tui-keybindings · repl-launcher · spec-cli · slash-commands · output-styles · skills-{registry,serializer} · hooks-engine | CLI coding agent |
| **Channel Front-end Stack** | gateway-server · gateway-protocol · channel stack · daemon-process · node-host · web-ui · canvas-host · voice-runtime (optional) · notifications-service · pairing-flow | Channel assistant |
| **Memory Stack** | memory-service · memory-extraction · personalization-store · bootstrap-files · system-prompt-builder · vector-store | Stateful long-horizon agents |
| **Voice Stack** | voice-runtime · vad-engine · barge-in-controller · audio-stream · call-session · telephony-adapter · dtmf-handler · prosody-controller · tool-tts-stt | Voice/telephony harnesses |
| **Computer-Use Stack** | computer-use-driver · browser-extension-bridge · tool-screen-capture · tool-mouse-keyboard · tool-vision-grounding · tool-dom-inspector · tool-browser | Browser/desktop driving harnesses |
| **Research Stack** | research-planner · crawler · branch-explorer · evidence-store · report-synthesizer · progress-streamer · research-checkpointer · source-quality-scorer · citation-tracker · autoplan-engine | Long-horizon research agents |
| **Batch Stack** | batch-runner · batch-job-spec · queue-consumer · idempotency-store · rate-limiter · retry-policy · dead-letter-queue · batch-progress-tracker · fan-out-fan-in · output-sink · batch-eval-loop | Queue/batch workers |

---

# PART D — Mapping: Module → Target Shape Requirements

Condensed which-shapes-need-what so the compiler can pick a base set per target.

| Shape | Always-on layers | Frequently-on | Rarely needed |
|---|---|---|---|
| **CLI** | Runtime Core, Tool Layer, Tool Impls (fs/bash/web), Permission, Compaction, State, Telemetry, MCP, Hooks, Skills, Slash, TUI, Bootstrap | Subagent, Coordinator, Swarm, LSP, Worktree, Plan-mode, Replay, Voice (light) | Channels, RAG, Eval (CI only), Pipeline engine |
| **CHN** | Runtime Core, Tool Layer (light), Permission, Compaction, State, Telemetry, MCP, Hooks, Channels, Scheduling, Gateway, Memory, Auto-reply | Subagent, Voice, Media, Skills, Slash, Browser, Web, ACP | RAG (unless KB), Eval, Pipeline, LSP |
| **CRW** | Runtime Core, Tool Layer, Multi-Agent (roles/handoff/coordinator/tasks), Workflow Engine, State, Telemetry, Permission, Hooks, Memory | Channels (out), MCP, A2A, Subagent, Knowledge | TUI, Voice, Pairing, Cron, Media |
| **RAG** | Pipeline Engine, RAG Stack, Telemetry, Eval (light), Model Layer, State (light) | Tool Layer (limited), MCP, Workflow Engine, Memory | Channels, Permission (full), TUI, Voice, Cron, Multi-agent |
| **EVAL** | Eval Stack, Dataset Registry, Grader Registry, Benchmark Runner, Replay, Telemetry, Model Layer | Sandbox, Tool Layer, Pipeline Engine, Trajectory Grading, Prompt Optimizer | Channels, TUI, Cron, Multi-agent (unless evaluating crews), Hooks |
| **MGD** | Runtime Core, Tool Layer, Permission, Policy, Telemetry, Checkpoint, Audit, Tenancy, MCP, A2A, Gateway, OTel, Sandbox | Workflow Engine, Graph Engine, HITL, Eval, Multi-agent, Memory, Canary, Durable Execution | TUI, Channels (often), Voice, Cron (often) |
| **GRPH** | Graph Engine, Checkpoint Store, Branch History, Durable Execution, Runtime Core, State, Telemetry, HITL, Recovery | Tool Layer, Permission, Memory, Replay, Eval, Subagent | TUI, Channels, Voice, Cron, RAG (unless knowledge node), Pipeline engine |
| **RES** | Research Stack, Runtime Core, Tool Layer, Web tools, Compaction, Memory, Citation, Checkpoint, Telemetry | Subagent, Browser, Branch-Explorer, Skill, Eval (online), Workflow Engine | Channels, TUI, Voice, Pipeline engine |
| **VOICE** | Voice Stack, Runtime Core, Telemetry, Channels (telephony), Permission (light), State (call-shaped) | Memory, Skills, Tool Layer (limited) | TUI, RAG, Eval, Browser/Computer-use, Multi-agent |
| **BROW** | Computer-Use Stack, Tool Layer (limited), Runtime Core, Telemetry, Sandbox | MCP, Subagent, Memory, Eval (replay) | Channels, RAG, Voice, TUI, Cron |
| **BATCH** | Batch Stack, Runtime Core, Telemetry, Permission, Tool Layer (specific to job), State (job-shaped) | Sandbox, MCP, Eval (online), Memory, Multi-agent | TUI, Channels (unless notification), Voice, Browser |

---

# PART E — Cross-Cutting IR-Level Switches

These shape multiple modules and must surface as explicit spec-level options. The compiler reads these to select/configure runtime modules.

1. **Durability mode** (`exit | async | sync | none`) — drives `checkpoint-store`, `durability-mode`, `event-log` flush policy.
2. **Streaming mode** (`pull | push | none`) — drives `stream-runtime`, `gateway-protocol`, `tui-runtime`.
3. **Permission mode** (`default | plan | auto | bypass`) — drives `permission-engine`, `approval-classifier`, `hitl-engine`.
4. **Compaction policy** (`summary-plus-artifacts | snip-only | tool-result-only | full-stack`) — drives compaction-stack composition.
5. **Runtime topology** (`graph | workflow | pipeline | conversation | managed | batch | research`) — selects engine module + IR target.
6. **Multi-agent topology** (`single | central | decentralized | none`) — coordinator vs swarm.
7. **Deployment profile** (`local | self-hosted | managed | hybrid`) — drives `deployment-controller`, `secrets-manager`, `tenancy`.
8. **Channel set** (`none | one | multi`) — drives the channel stack inclusion.
9. **Eval class** (`offline | online | trace-graded | none`) — drives eval-stack composition.
10. **Tool profile** (`minimal | coding | messaging | rag | research | batch | full`) — drives `tool-catalog` filtering.
11. **Voice mode** (`none | tts-only | full-realtime | telephony`) — drives voice-stack inclusion.
12. **Vision mode** (`none | multimodal-input | screen-driver`) — drives computer-use-stack and media-service.
13. **Compute envelope** (`local-process | container | sandbox-vm | edge-device`) — drives `sandbox`, `node-host`, `bundle-packager`.

---

# PART F — Anti-Patterns to Bake In as Constraints

The factory should *prevent* these by design, drawn from the architecture studies:

1. **God-function system prompt** (OpenClaw's 720-line `system-prompt.ts`). `system-prompt-builder` must be section-composable with token budget per section.
2. **Tool description double-counting** (OpenClaw embeds tool descs in prose AND tool schemas). Pick one channel.
3. **Channel-specific guidance leaking into non-channel sessions**. Sections must be gated by active modules.
4. **Backward-compat proxy bloat** (OpenClaw context-engine 200-line sessionKey compat). `migration-engine` produces clean rewrites, not runtime adapters.
5. **Monolithic agent directories** (OpenClaw's 280-file `agents/`). Codegen splits outputs into the layer groups defined here.
6. **No token-budget awareness in prompt assembly**. `prompt-cache-manager` and `system-prompt-builder` enforce budgets.
7. **Heartbeat sending full prompt every tick** (OpenClaw default). `heartbeat-engine` defaults to lightweight bootstrap.
8. **Random temp-file paths breaking prompt cache**. Use content-hash paths (Claude Code pattern).
9. **Ad-hoc subagent context filtering**. `subagent-runtime` exposes a documented context filter API.
10. **Eval as a dashboard afterthought**. `eval-service` is first-class and gates deployment.

---

# PART G — Module-build dependency order (rough)

Phases for implementation sequencing. Earlier phases unblock later ones.

1. **Foundations** — `infra-utils`, `error-types`, `logging`, `config-loader`, `state-store`, `global-state-singleton`, `feature-flags`, `runtime-migrations`.
2. **IR & Compiler** — `spec-schema`, `spec-parser`, `spec-validator`, `ir-model`, `ir-passes`, `migration-engine`, `compiler-core`, `bundle-packager`.
3. **Model & Tool primitives** — `model-adapter`, `model-router`, `token-budget`, `prompt-cache-manager`, `auth-profiles`, `secrets-manager`, `tool-catalog`, `tool-builder`, `tool-validate`, `tool-permission-matcher`.
4. **Runtime core** — `runtime-orchestrator`, `query-engine`, `turn-state-machine`, `recovery-engine`, `stream-runtime`, `abort-controller`, `scheduler`, `run-context`.
5. **Tool execution** — `tool-orchestrator`, `tool-executor`, `streaming-tool-executor`, `tool-result-store`, `tool-loop-detection`, `tool-search`, `tool-display`.
6. **State & persistence** — `session-store`, `event-log`, `checkpoint-store`, `app-state-store`, `branch-history`, `artifact-store`.
7. **Permission/policy** — `permission-engine`, `policy-engine`, `safety-prompt-injector`, `guardrails`, `hitl-engine`, `approval-classifier`, `denial-tracking`, `pii-redactor`, `prompt-injection-detector`.
8. **Context & memory** — `context-engine`, compaction stack, `bootstrap-files`, `system-prompt-builder`, `memory-service`, `vector-store`, `embedding-adapter`.
9. **Telemetry & eval** — `telemetry-engine`, `otel-exporter`, `trace-recorder`, `metrics-collector`, `replay-engine`, `cost-tracker`, `eval-service`, `dataset-registry`, `grader-registry`, `benchmark-runner`, `trajectory-grading`, `prompt-optimizer`.
10. **MCP/protocol** — `mcp-host`, `mcp-server-host`, `webhook-host`, `a2a-protocol`, `acp-protocol`, `ag-ui-protocol`.
11. **Built-in tool implementations** — `tool-fs`, `tool-bash`, `tool-process`, `tool-code-execution`, `tool-web-*`, `tool-mcp`, `tool-todo`, `tool-skill`, `tool-ask-user`, `tool-cron`, `tool-task`, `tool-team`, `tool-agent`, `tool-message-channel`, `tool-memory`, `tool-plan-mode`, `tool-worktree`, `tool-lsp`, `tool-monitor`, `tool-sleep`, `tool-notebook-edit`, `tool-notification`, `tool-remote-trigger`, `tool-canvas`, `tool-citations`, `tool-screen-capture`, `tool-mouse-keyboard`, `tool-vision-grounding`, `tool-dom-inspector`.
12. **Multi-agent & engines** — `subagent-runtime`, `coordinator`, `swarm-runtime`, `handoff-engine`, `role-system`, `task-engine`, `graph-engine`, `workflow-engine`, `pipeline-engine`, `durable-execution`, `event-bus`, `checkpoint-encoder`.
13. **Hooks/skills/commands** — `hooks-engine`, `hook-loader`, `skills-registry`, `skills-serializer`, `slash-commands`, `output-styles`.
14. **RAG layer** — `document-store`, `retriever`, `ranker`, `reader-extractor`, `query-router`, `generator`, `document-loader`, `chunker`, `knowledge-graph`, `ingestion-pipeline`, `citation-tracker`.
15. **Channels** — `channel-registry`, `channel-adapter-base`, individual `channel-*`, `channel-transport`, `channel-binding`, `channel-router`, `channel-features`, `directory-adapters`, `media-payload`, `pairing-flow`, `message-actions`, `gateway-server`, `gateway-protocol`.
16. **Scheduling** — `scheduler-cron`, `heartbeat-engine`, `isolated-agent-runner`, `session-reaper`, `task-scheduler`, `background-housekeeping`, `queue-consumer`, `idempotency-store`, `rate-limiter`, `retry-policy`, `dead-letter-queue`, `batch-progress-tracker`.
17. **UI/TUI/Voice/Media** — `tui-runtime`, `tui-keybindings`, `repl-launcher`, `web-ui`, `voice-runtime`, `vad-engine`, `barge-in-controller`, `audio-stream`, `call-session`, `telephony-adapter`, `dtmf-handler`, `prosody-controller`, `media-service`, `canvas-host`, `notifications-service`.
18. **Deployment & studio** — `deployment-controller`, `deployment-profiles`, `canary-controller`, `migration-runner`, `upgrade-controller`, `studio-ui`, `studio-server`, `trace-viewer`, `graph-visualizer`, `spec-cli`, `wizard`, `scaffold-templates`.
19. **Plugin SDK** — `plugin-sdk`, `plugin-registry`, `plugin-loader`, `module-marketplace`.
20. **Specialized + per-shape final modules** — research stack, batch stack, computer-use stack, voice-specific extras.

---

# PART G.5 — Critical path & risk register

Cross-references the per-layer `Depends on` columns in PARTS A and B with the build phases in PART G. This section is the single place to look up "what is the marginal cost of unblocking shape X?" — every entry below is an unbuilt module whose absence stalls a target shape, a stack, or another high-leverage module.

## High-risk (🔴) modules with their direct blockers

These are the bottleneck modules. Each row lists the modules that must land first and the downstream work that unblocks once it does. PART G phase column maps the row to its existing rough sequencing slot — note the staging gap where high-risk modules cluster in late phases (11, 12, 15, 17) rather than being pulled forward.

| Module | Layer | Phase (PART G) | Must land first | Unblocks (direct downstream) |
|---|---|---|---|---|
| `model-router` | R2 | 3 | `model-adapter`, `secrets-manager`, `auth-profiles` | `adapter-{openai,gemini,bedrock}`, multi-provider compaction, Section 17 |
| `target-graph` | F2 | 2 (gated by R11) | `compiler-core`, `codegen-templates`, `graph-engine`, `checkpoint-store` | GRPH shape, MGD partial |
| `target-pipeline` | F2 | 2 (gated by R11) | `compiler-core`, `codegen-templates`, `pipeline-engine` | RAG shape end-to-end |
| `target-managed` | F2 | 2 (gated by F3) | `compiler-core`, `bundle-packager`, `audit-log`, `tenancy` | MGD shape |
| `target-research-bundle` | F2 | 2 (gated by R19) | `compiler-core`, `research-planner`, `research-checkpointer`, `report-synthesizer` | RES shape |
| `target-voice` | F2 | 2 (gated by R16) | `compiler-core`, `voice-runtime`, `audio-stream`, `call-session` | VOICE shape |
| `target-browser-driver` | F2 | 2 (gated by R18) | `compiler-core`, `computer-use-driver`, `tool-screen-capture`, `tool-vision-grounding` | BROW shape |
| `deployment-controller` | F3 | 18 | `compiler-core`, `bundle-packager`, `secrets-manager` | `deployment-profiles`, `canary-controller`, `upgrade-controller`, MGD |
| `plugin-sdk` | F5 | 19 | `tool-catalog`, `channel-adapter-base`, `model-adapter`, `grader-registry` | `plugin-registry`, `plugin-loader`, `module-marketplace` |
| `plugin-loader` | F5 | 19 | `plugin-sdk`, `plugin-registry`, `sandbox` | All third-party plugin runtime |
| `durability-mode` | R1 | 4 | `checkpoint-store`, `event-log`, `runtime-orchestrator` | GRPH/MGD durability semantics, `durable-execution` consumers |
| `tool-code-execution` | R4 | 11 | `tool-builder`, `sandbox` | EVAL CI auto-run, RES auto-execution, MGD code samples |
| `tool-browser` | R4 | 11 | `tool-builder`, `sandbox`, `computer-use-driver` | BROW + RES web automation |
| `tool-tts-stt` | R4 | 11 | `tool-builder`, `voice-runtime`, `audio-stream` | VOICE realistic dialog |
| `tool-screen-capture` | R4 | 11 | `tool-builder`, `computer-use-driver` | BROW shape |
| `tool-mouse-keyboard` | R4 | 11 | `tool-builder`, `computer-use-driver` | BROW shape |
| `tool-vision-grounding` | R4 | 11 | `tool-builder`, `tool-screen-capture`, `model-adapter` | BROW grounded actions, CLI visual debugging |
| `a2a-protocol` | R5 | 10 | `tool-catalog`, `model-adapter`, `webhook-host` | CRW cross-org, MGD federated agents |
| `memory-service` | R6 | 8 | `vector-store`, `embedding-adapter`, `session-store` | `memory-extraction`, `tool-memory`, `compaction-session-memory`, RES long-horizon |
| `checkpoint-store` | R7 | 6 | `event-log`, `checkpoint-encoder`, `state-store` | `branch-history`, `target-graph`, `durable-execution`, `research-checkpointer` |
| `branch-history` | R7 | 6 | `checkpoint-store`, `event-log` | GRPH time-travel, RES branch exploration |
| `policy-engine` | R8 | 7 | `permission-engine`, `audit-log`, `tool-permission-matcher` | `tenancy`, regulated MGD, `sandbox` compose |
| `sandbox` | R8 | 7 | `infra-utils`, `policy-engine` | `tool-code-execution`, `tool-browser`, `channel-signal`, `plugin-loader`, EVAL/MGD hardening |
| `prompt-injection-detector` | R8 | 7 | `tool-result-store`, `model-adapter` | `guardrails` depth, MGD audit, RES untrusted-source ingest |
| `graph-engine` | R11 | 12 | `state-store`, `run-context`, `recovery-engine`, `abort-controller`, `checkpoint-store` | `target-graph`, GRPH shape, `durable-execution` for graphs |
| `workflow-engine` | R11 | 12 | `state-store`, `run-context`, `event-bus`, `sub-agent-spawner` | CRW expansion, RES workflows |
| `pipeline-engine` | R11 | 12 | `ir-model`, `model-adapter`, `run-context` | `target-pipeline`, `query-router`, RAG composition |
| `durable-execution` | R11 | 12 | `checkpoint-store`, `run-context`, `idempotency-store` | GRPH/MGD/BATCH long runs, `research-checkpointer` |
| `eval-service` | R15 | 9 | `trace-event-bus`, `dataset-registry`, `grader-registry`, `runtime-orchestrator` | `eval-runner`, `eval-report`, `prompt-optimizer`, `target-eval-bundle`, Section 16 |
| `prompt-optimizer` | R15 | 9 | `eval-service`, `model-adapter`, `system-prompt-builder` | DSPy-style optimization across all shapes |
| `gateway-server` | R16 | 17 | `secrets-manager`, `runtime-orchestrator`, `permission-engine` | `gateway-protocol`, `web-ui`, `studio-server`, MGD/CHN remote, `mcp-server-host`, `acp-protocol`, `ag-ui-protocol`, `webhook-host` |
| `voice-runtime` | R16 | 17 | `model-adapter`, `audio-stream`, `vad-engine` | `target-voice`, `barge-in-controller`, `call-session`, `tool-tts-stt` |
| `vad-engine` / `barge-in-controller` / `audio-stream` / `call-session` / `telephony-adapter` | R16 | 17 | (chain — see per-row `Depends on`) | VOICE shape |
| `tenancy` | R17 | 17 | `secrets-manager`, `audit-log`, `gateway-server` | MGD multi-customer isolation |
| `audit-log` | R17 | 17 | `event-log`, `secrets-manager` | `tenancy`, `policy-engine` audit story, MGD compliance |
| `computer-use-driver` | R18 | 20 | `infra-utils` | All R4 BROW tools, `target-browser-driver` |
| `browser-extension-bridge` | R18 | 20 | `mcp-host`, `gateway-server` | DOM-aware BROW, `tool-dom-inspector` |
| `research-planner` / `crawler` / `branch-explorer` / `report-synthesizer` / `research-checkpointer` | R19 | 20 | (per-row `Depends on`) | `target-research-bundle`, RES shape end-to-end |
| `batch-runner` | R20 | 20 | `queue-consumer`, `retry-policy`, `runtime-orchestrator` | `target-batch-worker`, `fan-out-fan-in`, `batch-eval-loop`, BATCH shape |

## Inverse view — what blocks each unbuilt target shape?

| Target shape | Status | Critical missing modules |
|---|---|---|
| **CLI** | mostly built — Section 13 closed sub-agents, Section 14 closed web/image/fetch | Polish: `tool-search`, `tool-display`, `tui-runtime`, `tui-keybindings`, `repl-launcher`, `output-styles`, `tool-skill`, `tool-ask-user`, `tool-cron`, `tool-plan-mode`, `tool-worktree`, `tool-lsp`, `tool-monitor`, `tool-sleep`, `tool-notebook-edit` |
| **CHN** | built (Slack only) | Other adapters; `channel-router`, `channel-features`, `media-payload`, `pairing-flow`, `autoreply-policy`, `heartbeat-engine`, `notifications-service` |
| **CRW** | partial via `target-workflow` + sub-agents | `workflow-engine`, `coordinator`, `swarm-runtime`, `handoff-engine`, `role-system`, `task-engine`, `pairing-engine`, `a2a-protocol` |
| **RAG** | not started | `target-pipeline`, `pipeline-engine`, full R12 stack (`document-store`, `retriever`, `ranker`, `reader-extractor`, `query-router`, `generator`, `document-loader`, `chunker`, `knowledge-graph`, `ingestion-pipeline`, `citation-tracker`), `vector-store`, `embedding-adapter` |
| **EVAL** | not started — Section 16 is the next phase | `target-eval-bundle`, R15 eval modules (`eval-service`, `dataset-registry`, `grader-registry`, `benchmark-runner`, `trajectory-grading`, `prompt-optimizer`, `replay-engine`), `model-router` for cross-provider sweeps |
| **MGD** | not started | `target-managed`, `deployment-controller`, `tenancy`, `audit-log`, `policy-engine`, `sandbox`, `gateway-server`, `gateway-protocol`, `acp-protocol`, `ag-ui-protocol`, `canary-router`, `migration-runner`, `upgrade-controller`, `hitl-engine` |
| **GRPH** | not started | `target-graph`, `graph-engine`, `durable-execution`, `checkpoint-store`, `branch-history`, `workflow-checkpointer`, `checkpoint-encoder`, `event-bus`, `hitl-engine`, `durability-mode` |
| **RES** | not started | `target-research-bundle`, full R19 stack, `branch-history`, `crawler`, `evidence-store`, `report-synthesizer`, `research-checkpointer`, `branch-explorer`, `progress-streamer`, `source-quality-scorer`, `autoplan-engine` |
| **VOICE** | not started | `target-voice`, R16 voice stack (`voice-runtime`, `vad-engine`, `barge-in-controller`, `audio-stream`, `call-session`, `telephony-adapter`, `dtmf-handler`, `prosody-controller`), `tool-tts-stt`, `media-service` |
| **BROW** | not started | `target-browser-driver`, `computer-use-driver`, `browser-extension-bridge`, `tool-screen-capture`, `tool-mouse-keyboard`, `tool-vision-grounding`, `tool-dom-inspector`, `tool-browser`, `sandbox` |
| **BATCH** | not started | `target-batch-worker`, R20 stack (`batch-runner`, `batch-job-spec`, `fan-out-fan-in`, `output-sink`, `batch-eval-loop`), `queue-consumer`, `idempotency-store`, `dead-letter-queue`, `rate-limiter`, `retry-policy` |

## Sequencing implications (informational; PART G stays the canonical order)

- **Pull `model-router` and `eval-service` earlier than PART G phase 9.** Both are 🔴 on the critical path and both blocked only by foundations that already exist (`model-adapter`, `secrets-manager`, `trace-event-bus`, `runtime-orchestrator`). Landing them ahead of dependent target shapes removes the largest compounding risk.
- **`graph-engine` / `pipeline-engine` / `workflow-engine` are independent investments** — none depend on the others. If only one ships in the next quarter, choose by which target shape (GRPH / RAG / CRW-expansion) is most urgent rather than by phase order.
- **The voice and browser stacks are deep, narrow chains** — every module is 🔴 because each links to the next. Either commit to the full chain or defer the whole shape; partial progress doesn't unblock anything.
- **`sandbox` blocks four things** — `tool-code-execution`, `tool-browser`, `channel-signal`, `plugin-loader` — and is a single security-critical design. Treat as a standalone hardening phase.

---

# PART H — Verification

End-to-end tests for the catalog itself (independent of any single module):

1. **Catalog completeness check** — for each of the 11 target shapes, build a "canonical example spec" and verify the compiler picks a non-empty, type-checked module set covering all required layers from PART D.
2. **Shape coverage matrix** — automated test that confirms every required layer × shape cell has at least one module that satisfies it, by walking PART D against PART B.
3. **Reference reproduction** — produce minimal harnesses that mimic Claude Code (CLI), OpenClaw (CHN), Haystack RAG (RAG), and verify generated modules map onto the originals' module list (≥80% overlap of layer-coverage).
4. **Cross-cutting switch matrix** — for each PART E switch, assert at least one IR pass adjusts module selection accordingly.
5. **Anti-pattern lint** — codegen lint that fails when an output violates a PART F constraint (e.g. `system-prompt-builder` produces a single string > N tokens; `subagent-runtime` invocation lacks a context filter spec).
6. **Per-module test plan present** — every module entry has at least one applicable test layer from T1–T9 (this plan asserts that, and a CI lint maintains it as modules evolve).

---

# PART I - Module Briefs

One-page build briefs now live in [`docs/module-briefs/README.md`](module-briefs/README.md).
They follow the dependency order from PART G, expand shorthand entries such as
`tool-web-*`, `channel-*`, and the named stacks, and include catalog modules that
PART G implies by layer but does not name directly.

---

## Critical files for implementation reference

- `docs/AI-Harness-Systems.md` — IR schema, reference architecture, test layers, deployment profiles, OTel dimensions, cross-cutting tradeoffs.
- `docs/architecture studies/openclaw-architecture.md` — channel, cron, skills, context-engine, plugin patterns; tool catalog + profiles; system-prompt anti-patterns.
- `docs/architecture studies/cc-architecture.md` — Tool interface + buildTool; query loop; multi-layer compaction; permission system; streaming; subagents.
- `reference-repos/claude-code/src/Tool.ts` — Tool interface contract.
- `reference-repos/claude-code/src/query.ts` — async-generator agent loop reference.
- `reference-repos/claude-code/src/services/compact/` — multi-layer compaction reference.
- `reference-repos/claude-code/src/utils/permissions/` — layered permission system.
- `reference-repos/openclaw/src/context-engine/` — pluggable context-engine interface.
- `reference-repos/openclaw/src/cron/` — cron + heartbeat + isolated-agent.
- `reference-repos/openclaw/src/channels/plugins/` — channel adapter pattern.
- `reference-repos/agent-framework/python/packages/core/agent_framework/_workflows/_workflow_builder.py` — declarative workflow IR + builder.
- `reference-repos/langgraph/libs/langgraph/langgraph/pregel/` — Pregel-style stateful graph runtime.
- `reference-repos/haystack/haystack/core/` — pipeline + component decorator pattern.
- `reference-repos/dspy/dspy/teleprompt/` — prompt program optimization.
- `reference-repos/lm-evaluation-harness/lm_eval/evaluator.py` — benchmark runner.
- `reference-repos/ragas/src/ragas/evaluation.py` — RAG-specific evaluation.
- `reference-repos/openai-agents-python/src/agents/` — handoffs, guardrails, sessions, voice, realtime.

---

## Module count summary

- **Factory-level**: ~35 modules across F1–F5
- **Runtime composable**: ~155 modules across R1–R20
- **Total**: ~190 modules (exceeds the 120–150 target — pruning will happen as similar adjacent modules collapse during specing; this catalog errs comprehensive)
- **Grouped bundles**: 18 coarse aggregations in PART C for ergonomic spec authoring

Each module has a research focus pointing to specific files in `reference-repos/` and a test layer assignment from T1–T9. The next phase is to write a one-page module brief for each entry, in dependency order from PART G.
