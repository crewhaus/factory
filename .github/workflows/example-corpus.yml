name: example-corpus

# Section 40 — matrix CI gate that compiles every examples/<name>/crewhaus.yaml
# end-to-end. Catches drift the moment any spec stops parsing, lowering, or
# emitting a clean bundle. Asserts:
#   - `crewhaus compile <spec>` exits 0 within 60s
#   - the emitted directory contains at least one `.ts` file
#   - the emitted entry imports cleanly (Bun parses the bundle without
#     executing it — daemon-shape examples don't need --dry-run because
#     compile alone exercises the full lower → emit pipeline)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  enumerate:
    runs-on: ubuntu-latest
    outputs:
      examples: ${{ steps.list.outputs.examples }}
    steps:
      - uses: actions/checkout@v4
      - id: list
        name: Enumerate examples/*/crewhaus.yaml
        run: |
          set -euo pipefail
          examples=$(
            find examples -mindepth 2 -maxdepth 2 -name crewhaus.yaml \
              | sed 's|examples/||;s|/crewhaus.yaml||' \
              | sort \
              | jq -R -s -c 'split("\n") | map(select(length > 0))'
          )
          echo "examples=$examples" >> "$GITHUB_OUTPUT"
          echo "Found $(echo "$examples" | jq 'length') examples"

  compile:
    needs: enumerate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        example: ${{ fromJson(needs.enumerate.outputs.examples) }}
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ">=1.2.0"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Compile examples/${{ matrix.example }}/crewhaus.yaml
        timeout-minutes: 1
        run: |
          out="/tmp/${{ matrix.example }}"
          rm -rf "$out"
          mkdir -p "$out"
          # Catch a hung compile early via the 60s timeout(1) wrapper.
          timeout 60s bun apps/cli/src/index.ts compile \
            "examples/${{ matrix.example }}/crewhaus.yaml" \
            -o "$out"

      - name: Verify bundle has at least one .ts file
        run: |
          out="/tmp/${{ matrix.example }}"
          ts_count=$(find "$out" -maxdepth 1 -name "*.ts" | wc -l)
          if [ "$ts_count" -lt 1 ]; then
            echo "::error::expected at least one .ts file in $out"
            ls -la "$out"
            exit 1
          fi
          echo "$ts_count .ts file(s) emitted"

      - name: Verify bundle parses cleanly (typecheck only)
        run: |
          out="/tmp/${{ matrix.example }}"
          # `bun build --no-bundle` parses + transforms but does not
          # execute the entry — surfaces syntax + import errors without
          # running the agent (which would need ANTHROPIC_AUTH_TOKEN).
          first=$(find "$out" -maxdepth 1 -name "*.ts" | head -1)
          bun build --no-bundle "$first" --outdir /tmp/parse-check >/dev/null

  status:
    needs: compile
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Aggregate compile-matrix status
        run: |
          if [ "${{ needs.compile.result }}" != "success" ]; then
            echo "::error::example-corpus matrix failed"
            exit 1
          fi
          echo "All examples compiled cleanly."
