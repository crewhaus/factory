{
  "name": "@crewhaus/target-cf-worker-workflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Codegen backend for the workflow target emitted as a Cloudflare Worker. Sibling of target-cf-worker-cli; reads the same IrWorkflowV0 variant, emits a self-contained Worker bundle that runs the steps sequentially.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test src"
  },
  "dependencies": {
    "@crewhaus/errors": "workspace:*",
    "@crewhaus/infra-utils": "workspace:*",
    "@crewhaus/ir": "workspace:*"
  }
}
