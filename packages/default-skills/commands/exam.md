---
description: Grade yourself against the configured competency exam (EXAM mode)
---
Enter EXAM mode as defined by the `learning-loop` skill — load it with the
`Skill` tool if you have not already. Run the competency exam configured
for this harness, then report the pass rate and every failed item verbatim.
Log each failure as a knowledge gap with `log_knowledge_gap` so the next
study pass picks it up, and add questions for newly mastered topics so the
exam keeps pace with your expertise.
