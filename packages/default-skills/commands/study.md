---
description: Run one deliberate learning pass (STUDY mode of the learning loop)
argument-hint: "[topic]"
---
Enter STUDY mode as defined by the `learning-loop` skill — load it with the
`Skill` tool if you have not already. Topic, if the user chose one: $ARGUMENTS

If no topic was given, pick one by the standing priority: open knowledge
gaps first, then the next curriculum rung, then the frontier. Run one
bounded study pass — gather from allowlisted sources, commit only cited
knowledge, upsert rather than duplicate — and finish with the summary the
skill requires: what you learned, what you committed (slugs), and what you
want to learn next.
