---
description: Reconcile and re-verify the knowledge base (REFLECT mode of the learning loop)
---
Enter REFLECT mode as defined by the `learning-loop` skill — load it with
the `Skill` tool if you have not already. Run one bounded reflection pass:
stalest and lowest-confidence articles first, reconcile contradictions and
duplicates through related articles, update quality signals, and curate the
curriculum and the exam. Supersede, never delete. Finish with the
reflection note the skill requires: contradictions reconciled, articles
re-verified, curriculum and exam changes, and the top open gap.
