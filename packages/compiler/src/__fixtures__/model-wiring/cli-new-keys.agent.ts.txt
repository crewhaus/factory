#!/usr/bin/env bun
// Generated by crewhaus. DO NOT EDIT.
// Source spec: "wiring-cli-new-keys" (target: cli, ir version: 0)
import { formatRunFailure, toFailureReport } from "@crewhaus/errors";
import { runChatLoop } from "@crewhaus/runtime-core";
import { defaultCatalog } from "@crewhaus/tool-catalog";
import { read } from "@crewhaus/tool-fs";
import { wireMemory } from "@crewhaus/memory-service";
import { loadHooks } from "@crewhaus/hooks-engine";
import { createSkillTool } from "@crewhaus/skills-registry";

defaultCatalog.register(read);

const __cwd = process.cwd();
const __hooks = await loadHooks({ cwd: __cwd });

const __memWired = await wireMemory({"specName":"wiring-cli-new-keys","continuity":{"plan":true,"proof":"ladder","ledger":true,"handoff":true,"scope":"spec"}}, { catalog: defaultCatalog, cwd: __cwd });
const __skills = __memWired.options.skills ?? [];
const __slashCommands = __memWired.options.slashCommands ?? new Map();
if (__skills.length > 0) defaultCatalog.register(createSkillTool(__skills));

try {
  await runChatLoop({
    model: "claude-sonnet-4-6",
    instructions: "Model-wiring fixture carrying one NEW 0.6.0 key per level: a profile field\n(temperature) on a candidate, a candidate-level key (enabled: false) and\npool-level keys (rules). Reply concisely.\n",
    sessionName: "wiring-cli-new-keys",
    sessionTarget: "cli",
    modelPool: {"candidates":[{"model":"claude-haiku-4-5","tags":["cheap"],"profile":"fast","temperature":0.2},{"model":"claude-sonnet-4-6","tags":["mid"],"enabled":false},{"model":"claude-opus-4-8","tags":["strong"]}],"policy":"heuristic","rules":[{"id":"code-goes-strong","when":{"message_matches":"refactor|stack ?trace"},"use":"strong"}]},
    tools: defaultCatalog.list(),
    hooks: __hooks,
    skills: __skills,
    slashCommands: __slashCommands,
    ...__memWired.options,
  });
} catch (__err) {
  const __report = toFailureReport(__err);
  process.stderr.write(`${formatRunFailure(__report)}\n`);
  process.exit(__report.exitCode);
}
