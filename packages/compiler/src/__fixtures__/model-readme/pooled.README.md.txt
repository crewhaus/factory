<!-- crewhaus:generated-readme -->

# pooled-readme-pin

Compiled CrewHaus bundle for the `cli` target shape.

Generated by CrewHaus — do not edit by hand; recompile from the spec instead.

## Harness

| | |
| --- | --- |
| Name | `pooled-readme-pin` |
| Target | `cli` |
| Models | `claude-sonnet-4-6`, `claude-haiku-4-5`, `claude-opus-4-8` |

## Tools

| Tool | Used by | Scope | Notes |
| --- | --- | --- | --- |
| `read` | agent | built-in | — |

## Environment variables

No spec-declared environment variables.

The model provider's API key must also be present (e.g. `ANTHROPIC_API_KEY` for Anthropic `claude-*` models; model(s) in use: `claude-sonnet-4-6`, `claude-haiku-4-5`, `claude-opus-4-8`).

## Run

```sh
bun install   # first run only — installs the bundle's dependencies
bun agent.ts
```

## Runtime data

Launch the bundle from inside this directory — it reads and writes workspace state under `.crewhaus/` relative to the working directory:

- `.crewhaus/sessions/` — session transcripts (one file per session)
- `.crewhaus/feedback/` — response ratings collected by the feedback tooling
