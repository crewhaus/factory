// GENERATED by scripts/gen-dockerfile-bodies.ts — do not edit by hand.
// Re-run that script after changing any docker/<target>/Dockerfile;
// docker-images' tests assert these bodies match the files byte-for-byte.
/**
 * Build-time-baked bodies of the per-shape `docker/<target>/Dockerfile`
 * files.
 *
 * Reading them package-relative broke `crewhaus build-image` on every
 * PUBLISHED channel — `dockerfile missing for target cli (expected at
 * /$bunfs/docker/cli/Dockerfile)` from the compiled binary, and the same miss
 * against an npm tarball whose `files` array omitted `docker/`. Both are now
 * fixed: the bodies live here, and `docker/` ships in `files` so the real
 * files are still present for a build context.
 *
 * These are literals rather than `with { type: "text" }` imports because Bun's
 * `--compile` does not embed EXTENSIONLESS files: a bare `Dockerfile` inlines
 * fine under the interpreter and resolves to `undefined` in the standalone
 * binary. `digests.json` is deliberately absent — `loadDigests` already
 * returns an empty file when it is missing, and it is only read or written
 * from a source checkout during the release workflow.
 */
export const DOCKERFILE_BODIES: Readonly<Record<string, string>> = {
  batch:
    '# Section 32 — docker-images: batch target shape (queue worker daemon).\n# Section 23 BATCH worker pulls from queue-protocol, runs one runChatLoop per job.\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus && chown crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=batch\nEXPOSE 0\n# Batch worker is a long-running consumer; healthcheck just verifies the process is alive.\nHEALTHCHECK --interval=30s --timeout=5s --start-period=5s --retries=3 \\\n  CMD pgrep -f crewhaus.js > /dev/null || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  browser:
    '# Section 32 — docker-images: browser target shape (computer-use agent).\n# Adds chromium + Playwright runtime so the BROW driver can spawn real browsers.\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\n# chromium + the libs Playwright\'s bundled chromium needs at runtime\nRUN apk add --no-cache \\\n      chromium \\\n      nss \\\n      freetype \\\n      harfbuzz \\\n      ca-certificates \\\n      ttf-freefont \\\n      dbus \\\n      curl && \\\n    addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus && chown crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=browser \\\n    PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 \\\n    PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium-browser \\\n    PORT=8080\nEXPOSE 8080\nHEALTHCHECK --interval=20s --timeout=10s --start-period=10s --retries=3 \\\n  CMD curl -fsS http://localhost:${PORT:-8080}/healthz || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  channel:
    '# Section 32 — docker-images: channel target shape (long-running daemon).\n# Section 12 daemon listens on $PORT (default 3000) for inbound webhooks.\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN apk add --no-cache curl ca-certificates && \\\n    addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus && chown crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=channel PORT=3000\nEXPOSE 3000\nHEALTHCHECK --interval=15s --timeout=3s --start-period=5s --retries=3 \\\n  CMD curl -fsS http://localhost:${PORT:-3000}/healthz || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  cli: '# Section 32 — docker-images: CLI target shape (interactive REPL).\n# Multi-stage: deps → build → runtime. Non-root user, read-only root, HEALTHCHECK.\n\n# ──────────────────────────────────────────────────────────────────────────────\n# Stage 1: deps (cached layer for node_modules)\n# ──────────────────────────────────────────────────────────────────────────────\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\n# ──────────────────────────────────────────────────────────────────────────────\n# Stage 2: build (typecheck + bundle the CLI entrypoint)\n# ──────────────────────────────────────────────────────────────────────────────\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\n# ──────────────────────────────────────────────────────────────────────────────\n# Stage 3: runtime (minimal — only what the CLI needs at boot)\n# ──────────────────────────────────────────────────────────────────────────────\nFROM oven/bun:1.2-alpine AS runtime\nRUN addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=cli\nEXPOSE 0\nHEALTHCHECK --interval=30s --timeout=5s --start-period=2s --retries=3 \\\n  CMD bun /app/crewhaus.js doctor --liveness || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["--help"]\n',
  crew: '# Section 32 — docker-images: crew target shape (multi-agent A2A daemon).\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN apk add --no-cache curl ca-certificates && \\\n    addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus && chown crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=crew PORT=3000\nEXPOSE 3000\nHEALTHCHECK --interval=15s --timeout=3s --start-period=5s --retries=3 \\\n  CMD curl -fsS http://localhost:${PORT:-3000}/healthz || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  eval: '# Section 32 — docker-images: eval target shape (deployable eval bundle).\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus/evals && chown -R crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=eval\nEXPOSE 0\nHEALTHCHECK --interval=30s --timeout=5s --start-period=2s --retries=3 \\\n  CMD bun /app/crewhaus.js doctor --liveness || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["eval", "/spec/crewhaus.yaml"]\n',
  graph:
    '# Section 32 — docker-images: graph target shape (durable graph runtime).\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus/graphs && chown -R crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=graph\nEXPOSE 0\nHEALTHCHECK --interval=30s --timeout=5s --start-period=2s --retries=3 \\\n  CMD bun /app/crewhaus.js doctor --liveness || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  managed:
    '# Section 32 — docker-images: managed target shape (multi-tenant gateway daemon).\n# Section 20 daemon listens on $PORT (default 8080) for tenant API traffic.\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN apk add --no-cache curl ca-certificates && \\\n    addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus/audit && chown -R crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=managed PORT=8080\nEXPOSE 8080\nHEALTHCHECK --interval=15s --timeout=3s --start-period=5s --retries=3 \\\n  CMD curl -fsS http://localhost:${PORT:-8080}/healthz || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  pipeline:
    '# Section 32 — docker-images: pipeline target shape (RAG ingest pipeline).\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus/vector && chown -R crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=pipeline\nEXPOSE 0\nHEALTHCHECK --interval=30s --timeout=5s --start-period=2s --retries=3 \\\n  CMD bun /app/crewhaus.js doctor --liveness || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  research:
    '# Section 32 — docker-images: research target shape (autonomous research agent).\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus/research && chown -R crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=research\nEXPOSE 0\nHEALTHCHECK --interval=30s --timeout=5s --start-period=2s --retries=3 \\\n  CMD bun /app/crewhaus.js doctor --liveness || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  voice:
    '# Section 32 — docker-images: voice target shape (realtime telephony agent).\n# Adds opus (PCM ↔ Opus codec for telephony codecs) on top of the base.\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN apk add --no-cache curl ca-certificates opus opus-tools && \\\n    addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001 && \\\n    mkdir -p /app/.crewhaus && chown crewhaus:crewhaus /app/.crewhaus\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=voice PORT=8080\nEXPOSE 8080\nHEALTHCHECK --interval=15s --timeout=5s --start-period=10s --retries=3 \\\n  CMD curl -fsS http://localhost:${PORT:-8080}/healthz || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
  workflow:
    '# Section 32 — docker-images: workflow target shape (sequential multi-step agent).\nFROM oven/bun:1.2-alpine AS deps\nWORKDIR /app\nCOPY bun.lock package.json ./\nCOPY packages packages\nCOPY apps apps\nRUN bun install --frozen-lockfile\n\nFROM deps AS build\nWORKDIR /app\nCOPY tsconfig.base.json tsconfig.json ./\nRUN bun run typecheck\nRUN bun build apps/cli/src/index.ts --target=bun --outfile=/app/dist/crewhaus.js\n\nFROM oven/bun:1.2-alpine AS runtime\nRUN addgroup -S crewhaus && adduser -S crewhaus -G crewhaus -u 10001\nWORKDIR /app\nCOPY --from=build --chown=crewhaus:crewhaus /app/dist/crewhaus.js /app/crewhaus.js\nCOPY --from=deps --chown=crewhaus:crewhaus /app/node_modules /app/node_modules\nUSER crewhaus\nENV NODE_ENV=production CREWHAUS_TARGET=workflow\nEXPOSE 0\nHEALTHCHECK --interval=30s --timeout=5s --start-period=2s --retries=3 \\\n  CMD bun /app/crewhaus.js doctor --liveness || exit 1\nENTRYPOINT ["bun", "/app/crewhaus.js"]\nCMD ["run", "/spec/crewhaus.yaml"]\n',
};
