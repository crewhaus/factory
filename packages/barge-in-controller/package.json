{
  "name": "@crewhaus/barge-in-controller",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Barge-in coordinator: watches inbound audio via vad-engine, fires interrupt() on adapter when user speaks during agent playback (Section 24 VOICE)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test src"
  },
  "dependencies": {
    "@crewhaus/vad-engine": "workspace:*",
    "@crewhaus/voice-runtime": "workspace:*"
  }
}
